[package]
name = "fux-vt"
version = "0.1.0"
edition = "2024"
rust-version = "1.95"
license = "MIT"
description = "Bounded terminal emulation for fux"

# Temporary diagnostic switch; removed with the differential scaffolding.
[features]
differential = []

[dependencies]
unicode-width = "0.2"

[dev-dependencies]
vt100 = "=0.16.2"

[lints.clippy]
unwrap_used = "forbid"
expect_used = "forbid"
panic = "forbid"
unreachable = "forbid"
todo = "forbid"
unimplemented = "forbid"
indexing_slicing = "warn"
