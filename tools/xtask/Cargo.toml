[package]
name = "fux-xtask"
version = "0.1.0"
edition = "2024"
rust-version = "1.95"
publish = false

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
tempfile = "3"
nix = { version = "0.30", features = ["fs", "poll", "signal", "term", "process"] }
vt100 = "=0.16.2"

[workspace]
