diff --git a/OBSERVATION-CONTRACT.md b/OBSERVATION-CONTRACT.md
index 38fda77..19b8131 100644
--- a/OBSERVATION-CONTRACT.md
+++ b/OBSERVATION-CONTRACT.md
@@ -57,4 +57,4 @@ The adapter reads bounded newline-delimited JSON `list` and `capture` responses
 
 Detection and the existing state machine remain in zor. Reports use the OSC v1 schema above, one report per newline on stdout. A consumer must apply a report-size limit before parsing, reject malformed output, and keep observer backpressure independent of pane input/output. Closing or killing the observer must not terminate the observed command.
 
-The fux control adapter negotiates control v1 by sending and verifying the eight-byte `FUXCTL1\n` preface before each RPC. A mismatch or stalled preface ends that sampling attempt without sending a command. Preface reads use an absolute two-second deadline. This is an independent wire consumer, not a fux library dependency.
+The fux control adapter sends and verifies the four-byte `FUX\n` preface before each RPC. A mismatch or stalled preface ends that sampling attempt without sending a command. Preface reads use an absolute two-second deadline. This is an independent wire consumer, not a fux library dependency.
diff --git a/src/observe.rs b/src/observe.rs
index de097b3..be85a1a 100644
--- a/src/observe.rs
+++ b/src/observe.rs
@@ -20,9 +20,9 @@ fn request(socket: &Path, value: Value) -> anyhow::Result<Value> {
         "control socket belongs to another user"
     );
     stream.set_write_timeout(Some(Duration::from_secs(2)))?;
-    stream.write_all(b"FUXCTL1\n")?;
+    stream.write_all(b"FUX\n")?;
     let deadline = Instant::now() + Duration::from_secs(2);
-    let mut preface = [0; 8];
+    let mut preface = [0; 4];
     let mut used = 0;
     while used < preface.len() {
         let remaining = deadline
@@ -38,7 +38,7 @@ fn request(socket: &Path, value: Value) -> anyhow::Result<Value> {
         used += n;
     }
     anyhow::ensure!(
-        &preface == b"FUXCTL1\n",
+        &preface == b"FUX\n",
         "incompatible fux control protocol"
     );
     serde_json::to_writer(&mut stream, &value)?;
@@ -90,6 +90,11 @@ pub fn run(
     let mut active_agent = forced.clone();
     // The observer may start immediately before its parent's control accept loop.
     let startup = Instant::now() + Duration::from_secs(3);
+    // The pane's screen and the output sequence it reflects are kept between samples, so an idle
+    // pane costs one `list` and no capture or re-emulation until fux reports a new sequence.
+    let mut screen: Option<crate::screen::Screen> = None;
+    let mut screen_seq: Option<u64> = None;
+    let mut screen_dims: Option<(u16, u16)> = None;
     loop {
         let listing = match request(socket, json!({"command":"list","id":1})) {
             Ok(value) => value,
@@ -138,16 +143,26 @@ pub fn run(
             .unwrap_or(82)
             .saturating_sub(2)
             .clamp(2, 1000) as u16;
-        let capture = request(
-            socket,
-            json!({"command":"capture","id":2,"pane":pane_id,"attrs":true,"scrollback":0,"max_bytes":131072}),
-        )?;
-        let text = capture
-            .pointer("/result/value/text")
-            .and_then(Value::as_str)
-            .ok_or_else(|| anyhow::anyhow!("invalid capture response"))?;
-        let mut screen = crate::screen::Screen::new(rows, columns);
-        screen.process(text.as_bytes());
+        let seq = pane.get("seq").and_then(Value::as_u64);
+        let size_changed = screen_dims != Some((rows, columns));
+        // Re-capture and re-emulate only when the pane's output sequence advanced (or its size
+        // changed, or fux does not report a sequence). Otherwise the retained screen still holds.
+        if size_changed || seq.is_none() || seq != screen_seq {
+            let capture = request(
+                socket,
+                json!({"command":"capture","id":2,"pane":pane_id,"attrs":true,"scrollback":0,"max_bytes":131072}),
+            )?;
+            let text = capture
+                .pointer("/result/value/text")
+                .and_then(Value::as_str)
+                .ok_or_else(|| anyhow::anyhow!("invalid capture response"))?;
+            let mut fresh = crate::screen::Screen::new(rows, columns);
+            fresh.process(text.as_bytes());
+            screen = Some(fresh);
+            screen_seq = seq;
+            screen_dims = Some((rows, columns));
+        }
+        let screen = screen.as_mut().ok_or_else(|| anyhow::anyhow!("screen not initialised"))?;
         screen.set_observed_title(
             pane.get("title")
                 .and_then(Value::as_str)
@@ -190,7 +205,7 @@ pub fn run(
         let verdict = agent
             .as_ref()
             .and_then(|agent| sets.iter().find(|set| set.id == agent.as_str()))
-            .map(|set| crate::rules::evaluate(set, &screen))
+            .map(|set| crate::rules::evaluate(set, &*screen))
             .map(|verdict| Observation {
                 state: match verdict.state {
                     crate::rules::RuleState::Working => ObservationState::Working,
