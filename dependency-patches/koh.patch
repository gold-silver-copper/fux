diff --git a/src/gateway/sessions_real_fux.rs b/src/gateway/sessions_real_fux.rs
index 8828f51..2cf8f4f 100644
--- a/src/gateway/sessions_real_fux.rs
+++ b/src/gateway/sessions_real_fux.rs
@@ -223,17 +223,11 @@ async fn frame(stream: &mut tokio::net::UnixStream) -> anyhow::Result<serde_json
 async fn hello(stream: &mut tokio::net::UnixStream) -> anyhow::Result<()> {
     message(
         stream,
-        serde_json::json!({"type":"hello","version":6,"rows":24,"columns":80}),
+        serde_json::json!({"type":"hello","rows":24,"columns":80}),
     )
     .await?;
     let value = tokio::time::timeout(Duration::from_secs(15), frame(stream)).await??;
-    assert_eq!(
-        value
-            .pointer("/hello/version")
-            .and_then(serde_json::Value::as_u64),
-        Some(6),
-        "{value}"
-    );
+    assert_eq!(value, serde_json::json!({"hello":{}}));
     Ok(())
 }
 #[cfg(feature = "cli")]
diff --git a/tests/gateway.rs b/tests/gateway.rs
index aee8829..99df5cd 100644
--- a/tests/gateway.rs
+++ b/tests/gateway.rs
@@ -177,17 +177,11 @@ async fn frame(stream: &mut tokio::net::UnixStream) -> anyhow::Result<serde_json
 async fn hello(stream: &mut tokio::net::UnixStream) -> anyhow::Result<()> {
     message(
         stream,
-        serde_json::json!({"type":"hello","version":6,"rows":24,"columns":80}),
+        serde_json::json!({"type":"hello","rows":24,"columns":80}),
     )
     .await?;
     let value = tokio::time::timeout(Duration::from_secs(15), frame(stream)).await??;
-    assert_eq!(
-        value
-            .pointer("/hello/version")
-            .and_then(serde_json::Value::as_u64),
-        Some(6),
-        "{value}"
-    );
+    assert_eq!(value, serde_json::json!({"hello":{}}));
     Ok(())
 }
 #[cfg(feature = "cli")]
