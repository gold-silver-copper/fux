diff --git a/src/gateway/sessions_real_fux.rs b/src/gateway/sessions_real_fux.rs
index 1ee4dc6..d59f8e6 100644
--- a/src/gateway/sessions_real_fux.rs
+++ b/src/gateway/sessions_real_fux.rs
@@ -171,16 +171,11 @@ async fn frame(stream: &mut tokio::net::UnixStream) -> anyhow::Result<serde_json
 async fn hello(stream: &mut tokio::net::UnixStream) -> anyhow::Result<()> {
     message(
         stream,
-        serde_json::json!({"type":"hello","version":2,"rows":24,"columns":80}),
+        serde_json::json!({"type":"hello","rows":24,"columns":80}),
     )
     .await?;
     let value = tokio::time::timeout(Duration::from_secs(15), frame(stream)).await??;
-    assert_eq!(
-        value
-            .pointer("/hello/version")
-            .and_then(serde_json::Value::as_u64),
-        Some(2)
-    );
+    assert!(value.get("hello").is_some(), "{value}");
     Ok(())
 }
 #[cfg(feature = "cli")]
diff --git a/tests/gateway.rs b/tests/gateway.rs
index 9b0b50c..903fa5b 100644
--- a/tests/gateway.rs
+++ b/tests/gateway.rs
@@ -177,16 +177,11 @@ async fn frame(stream: &mut tokio::net::UnixStream) -> anyhow::Result<serde_json
 async fn hello(stream: &mut tokio::net::UnixStream) -> anyhow::Result<()> {
     message(
         stream,
-        serde_json::json!({"type":"hello","version":2,"rows":24,"columns":80}),
+        serde_json::json!({"type":"hello","rows":24,"columns":80}),
     )
     .await?;
     let value = tokio::time::timeout(Duration::from_secs(15), frame(stream)).await??;
-    assert_eq!(
-        value
-            .pointer("/hello/version")
-            .and_then(serde_json::Value::as_u64),
-        Some(2)
-    );
+    assert!(value.get("hello").is_some(), "{value}");
     Ok(())
 }
 #[cfg(feature = "cli")]
