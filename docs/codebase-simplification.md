# Codebase simplification record

Tracks `docs/prompts/simplify-fux-codebase-prompt.md`. One row per batch: what changed, lines
removed, any behaviour difference, and the gate result. Line counts are `git diff --shortstat`
for the batch commit.

| Batch | What changed | Lines | Behaviour difference | Gate |
| --- | --- | --- | --- | --- |
| 1 | Hygiene: removed 2,351 tracked gate logs (`docs/verification/`, now gitignored) and 18 dated reports, indexed in `docs/verification.md`; root prompts moved to `docs/prompts/`; `HANDOFF.md`, `release-readiness.md` rewritten; `design.md` corrected (no Waits phase, four exclusive systems, real dirty-flag reason); orphan regex comment dropped. `tools/archive` kept: xtask provenance hashes it. | 2398 files changed, 566 insertions(+), 282938 deletions(-) | None. | fmt, clippy, all workspace tests, rustdoc green; release-package.sh fails on main already (local-ipc `read_exact_until` unpublished), fixed in Batch 9 |
| 2 | Correctness: single `CloseViewer` on queue overflow; `Viewer::attached_to` shared by both admission paths; unreachable `Inspect` arm removed and `layout_control::apply` split into `read`/`edit` (`is_read_only` deleted); `PaneId(0)` sentinel replaced by `Outcome::{Now, Deferred}`; `#[serde(default)]` on the six tracked-operation `instance` fields; `ManagerReply::into_descriptor` replaces two enumerated wildcard matches | 10 files changed, 247 insertions(+), 108 deletions(-) | One `CloseViewer` effect per overflowed viewer instead of two. A detaching viewer no longer counts toward the per-workspace viewer limit on the select/transfer path. The six tracked operations with `instance` omitted now fail validation with `invalid-request` ("tracked operations require a server instance") instead of a JSON field error, matching `events` and `split`. | fmt, clippy, boundary regenerated, fux/local-ipc/zor tests, rustdoc all green; release-package.sh skipped (preexisting failure, Batch 9) |
| 3 | Server/proto/daemon dedup: `ManagerAction`/`ManagerOutcome` mirrors deleted (`Inbound::Manager` carries `ManagerRequest`, outcomes are `ManagerReply` plus one `Attach` variant); `DESCRIPTOR_HOOK` global replaced by `paths`/`identity` on `Owner`; one `Register` channel for reply senders and outboxes; one `remove_stale_socket(path, probes)`; `same_user` and `authorize_peer` via local-ipc peer helpers, `connect_local` alias dropped; one `encode_line` behind `write_frame`, `write_frame_until`, `write_line`, `write_reply`; `PathError::Io` carries the source and `private_dir` matches `DirectoryError`, `local_ipc::absolute_path` shared; `Box<dyn ChildKiller>` replaced by `kill_process`; `serve_subscription`, `reader_pump`, `writer_pump` extracted; `Event::kind`, `EventKind`, `ErrorCode::Timeout` removed | 20 files changed, 587 insertions(+), 784 deletions(-) | None on the wire. `write_line` now refuses frames over the 1 MiB limit for every frame type, not only control replies. `manager.sock` stale recovery treats a connect error other than refused/not-found as an error instead of "not running" | fmt, clippy, boundary regenerated, all fux binaries (automation_integration 24/24, local_cli 19/19), local-ipc/zor tests, rustdoc green; release-package.sh skipped (Batch 9) |
| 4 | ECS request-handler ergonomics: `Failure { code, message }` replaces `failed(id, ..)` at 137 sites, the request id is attached once in `apply_control`/`control_reply`, and the `id` parameter left ~15 handler signatures (`Context` now carries it for deferred creations); `Context::pane/live_pane/tab` replace 60+ lookup chains with one message per kind; `apply_requests` and `apply_spawn_completions` read the batch in place via `resource_scope` (only queued viewer requests and matched manager requests are cloned); `SplitSpec`/`MoveSpec` replace 15- and 9-parameter signatures and both `too_many_arguments` allows; `Pane::is_required_process`, `despawn_viewer` moved to `support`, `attached_viewers` count, `update_pane_field` shared by rename and right-click, `Limits: Copy`, `ordered_workspaces` ranks by map, `summarize` takes a `Selection`, `Session::workspace_names` and `requests::notice` deleted, `FocusTarget: Copy`. | 14 files changed, 790 insertions(+), 1010 deletions(-) | Not-found messages are uniform (`pane not found`, `tab not found`); previously four wordings. Request validation now runs after the viewer context resolves, so a request from a viewer that vanished in the same step gets no reply instead of a validation reply (nobody to receive it). `ErrorCode`s unchanged. | fmt, clippy, boundary regenerated (requests, requests_control, transfer, support, resources, layout_archive, proto/control), all fux binaries (automation_integration 24/24, local_cli 19/19, ecs 85, lib 194), local-ipc and zor tests, rustdoc: green; release-package.sh skipped (Batch 9) |
| 5 | ECS-native: `Workspace.open`/`retiring` fields replaced by an `Open` marker and a `Retiring` component with the `Accepting` filter alias (`With<Open>, Without<Retiring>`) and `is_accepting`/`is_not_retiring`/`is_pending` helpers for exclusive code; `retire()` inserts; `on_remove` hooks on `Pane`, `Tab`, `Viewer`, `Workspace` release their `Ids` entries so the five manual removal sites are gone (`check_invariants` still verifies agreement); `ViewerExit::despawn` no longer takes `Ids`; `bypass_change_detection` on the history-view, capture and listing-refresh reads. | 12 files changed, 181 insertions(+), 150 deletions(-) | None observable. A viewer id released by a typed system now leaves `Ids` at the phase sync point instead of immediately; ids are never reused and no reader depends on the earlier release. | fmt, clippy, boundary regenerated (only components.rs, Arrivals, Scene moved), all fux binaries (zor_resume flaky, passed alone), local-ipc/zor tests, rustdoc green; release-package.sh skipped |
| 7a | Client: one `Step` enum replaces `KeyTransition`/`Completion`/`KeyEffect` and the IIFE in `Mode::key`; copy keys handled inline, `controller_copy.rs` deleted; text-entry modes collapse to `Mode::Text { kind, text }` and close dialogs to `Mode::Confirm(CloseKind)`; `effects::Identity` has named fields and is stored by `SwapPicker`, `Menu`, `Drag` and the text/confirm kinds, with one reconcile in `Controller::key` instead of per-arm re-validation; chooser mouse handling is one block over `Mode::selection_mut` with `hit()` and `drag::plain_wheel`; `commands::Target` replaces whole-`Frame` clones for menu actions and waiting commands; `Outbox` and `Signals` remove the seven repeated `effects.input(..)` calls, the readiness predicate and the `too_many_arguments` allow in the run loop; `Composed { buffer, cursor, hits }`, `Screen` swaps two buffers instead of allocating per paint, `paint` allocates the empty buffer only for a first frame, the backend styles from ratatui `Color`/`Modifier` directly (`backend::CellStyle`, `rat_to_vt`, `backend_style` deleted); `InputEvent::Mouse` drops its unused raw bytes; `workspaces_enabled()` and the `RESIZE_STEP` re-export removed. | e473251: 11 files, +821/−990; part 2: 7 files, +278/−281 | A pasted Enter no longer submits the tab-rename field (it already did not for the other three fields). A horizontal-wheel report no longer steps the swap picker or menu (the shared `plain_wheel` mask). A chooser press after its target was dismissed by reconcile adopts the left capture uniformly. | fmt, clippy `-D warnings`, boundary regenerated, all fux binaries (lib 194, ecs 85, automation_integration 24, local_cli 19, structure 13), rustdoc green |
| 7b | Client: one sequence grammar. `input::sequence_complete` (ESC / ESC ESC / open CSI or SS3 introducer wait; CSI body ends at its final byte or `MAX_SEQUENCE`) and one `Nav` table (`input::navigation`) are shared by `PrefixFilter` and the controller's modal parser, replacing the controller's own completion rule and its ten-arm arrow/page table. The full parser merge (a single byte parser emitting modal events) was not done: the modal parser has semantics the pane-forwarding parser must not have (Alt-key sequences swallowed inside a mode, pasted bytes typed into text fields as characters, raw bytes buffered for replay while a chooser loads, Escape-timeout dismissal), and the 2,600-line controller test suite plus the generative trace test drive the byte API directly; merging would have meant re-proving those guarantees in one file for a gain of about 100 lines. The duplicated grammar, which was the actual divergence risk, is gone. | 4 files, +79/−63 | ESC ESC inside a mode now waits for the Escape timeout and then dismisses the mode (before: dropped at once, nothing happened). ESC ESC followed by another byte stays a swallowed unknown sequence. Pinned by a test. | fmt, clippy `-D warnings`, boundary regenerated, all fux binaries (lib 195, ecs 85, automation_integration 24, local_cli 19, structure 13), rustdoc green |
