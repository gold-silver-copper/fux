# Zor agent runtime implementation ledger

Objective: execute `herdr-gap-closure-prompt.md`. Fux remains a generic multiplexer; all agent semantics belong in zor. The full objective remains open until implementation, verification, comparison and review are complete. The finite current acceptance checklist is [agent-workflow-completion.md](agent-workflow-completion.md); historical next-step notes below are not additional requirements.

Starting repository heads and pre-existing modifications are recorded in `agent-workflow-baseline.json`. Historical audit claims must be revalidated against this implementation. Three narrow comparative scenarios are recorded below; broader superiority remains unproven.

| Requirement | Reference / starting evidence | Owner | Status / required proof |
|---|---|---|---|
| Protocol composition | fux `FUXCTL2`, attachment 5; local companion edits disagree | zor/koh adapters | Local observation repaired; source reconstruction passes. Gateway runtime remains environment-blocked. |
| Stable incarnation-scoped handles | fux descriptors have nonce; control requests use bare pane IDs | fux | Implemented: optional instance precondition on all requests; deterministic repeated-ID/PID and real socket stale-operation tests pass. |
| Coherent conditional capture | list/capture are separate, no capture revision | fux | Implemented: coherent metadata, viewport offset, conditional revision, truncation. Local tests and slice review passed, including incarnation-scoped captures. |
| Snapshot/event synchronization | subscriptions had bounded queues but silently dropped output notifications | fux | Implemented: listing cursor, bounded replay, register-before-replay subscriptions, gap errors, generic invalidation and trailing output; zor now consumes unfiltered subscriptions with explicit invalidation and fresh-snapshot recovery |
| Ordered input receipts/deduplication | send-keys returned command completion | fux | Implemented: reserve/submit/status, incarnation guard, bounded deduplication, partial delivery and expiry tests; zor task submission consumes receipts with durable no-replay reconciliation |
| Retained final output/exit | natural pane close released terminal | fux | Implemented: bounded last-observed viewport/optional exit record via manager and CLI, TTL/capacity/authority tests; zor managed launch/stop and waits consume final evidence |
| Generic API discoverability | current CLI and typed protocol | fux | Implemented: typed control protocol, structured CLI results and `docs/local-control-protocol.md`; no additional discovery API is required |
| Automatic observation service | zor observed one explicit pane/PID | zor | In progress: zor serve/status share a bounded authenticated snapshot API; status --start adds background startup and shutdown is instance-checked. Event-driven invalidation/replay subscription and periodic discovery are implemented; selective event-driven refresh and observation/management separation pass; remaining recovery is tracked in the finite checklist |
| Bundled detection and explanation | no active bundled rules; herdr `src/detect/manifests` and integrations | zor | In progress: narrow provenance-backed Codex, Claude and OpenCode startup rules, derived negatives and atomic overrides/reload. Broad real-agent state/version coverage remains open |
| Evidence identities/freshness | zor state machine and OSC contract | zor | In progress: bounded producer lifetimes, heartbeat expiry, input-correlated native dashboard precedence and clean producer replacement are implemented with real OpenCode evidence. Required observation-loss/recovery coverage remains in the finite checklist; automatic human-answer re-correlation is optional when correlation is explicitly invalidated |
| Task/session/attempt model | herdr `src/api/schema/agents.rs`, `src/app/api/agents.rs` | zor | In progress: private bounded journal, managed launch/adoption, submission, CLI/service task operations and separate delivery/wait/task outcomes. Current result views and explicit sealed source verification are implemented; bounded groups and verified handoffs are implemented; a monolithic frozen result object is not required |
| Prompt correlation and waits | herdr `src/api/wait.rs` | zor | In progress: receipt-correlated waits, explicit prompt-scoped claims, human-input invalidation, bounded CLI follow and no-replay crash recovery pass synthetic real-fux scenarios. OpenCode1.18.29 automatic registration/arming/native binding and response/permission/question waits pass real local synthetic-provider runs. Required recovery and broader real-agent coverage remain open; shared service waiters are optional |
| Worktree ownership and rollback | herdr `src/worktree.rs` | zor | In progress: owned creation/reconciliation/removal, dirty/force guards, launch association, inode pins and bounded retained-process descendant cwd checks pass disposable tests. Unadopted local-fux active-use protection now passes; interrupted-removal recovery uses explicit external completion and reconciliation. Automatic destructive retry and branch/parent cleanup are outside the completion path |
| Fan-out/results/verification | herdr agent APIs and pane reads | zor | In progress: bounded check evidence, sealed named check/artifact policies, immutable artifact bytes, retained changed-file observations, hash-validated committed check inputs with atomic output capture, compact result views and separate bounded service check workers. Coherent sealed verification, verified-predecessor handoffs and durable manual/automatic group advancement are implemented. Three-worker dependency/admission/restart/cleanup fixtures and the full reviewed real-zor suite pass; reconstructed integration passes before environment-blocked koh checks. Group dashboard attention, including pending retirement after cancellation, passes targeted and reconstructed integration; broader recovery remains open |
| Recovery | herdr persistence and agent resume | zor | In progress: private task journal, receipt and managed launch/stop reconciliation; service restart preserves live panes. Recorded-stop and abandoned-check service recovery are implemented. Lost-history launch resolution, pending-intent resolution, broader automatic recovery and fux restart/resume policy remain open |
| Dashboard/notifications | herdr agent view and notifications | zor | Implemented standalone dashboard/JSON, task/group attention, evidence, generic pane focus and transition bell. Opt-in bounded desktop notification commands pass disposable-notifier UI tests; actual OS popup delivery and measured UX comparison remain unverified |
| Comparative prompt boundaries | herdr `src/api/wait.rs` and `src/app/api/agents.rs` | zor | Measured narrow explicit-report benefit without a working transition; old-evidence timeout and herdr working/blocked controls pass. See [comparison](comparisons/prompt-boundary.md). Other comparison categories remain open. |
| Comparative input retries | herdr agent.prompt and zor durable input receipts | zor | Measured one input after a lost consumed-input reply with same-operation retry; repeating herdr correlation ID produces two. Before-forward control produces one in both. See [comparison](comparisons/input-retry.md). Expiry, server restart and other failure boundaries remain open. |
| Comparative service failure | herdr headless PTY ownership and session restoration | zor | Three runs retain the original worker, session and delivered receipt after zor-service SIGKILL/restart, with no duplicate retry input and successful next prompt. Killing either fux or herdr PTY owner ends the worker in all three controls. These are different fault scopes; owner restart/session resume and real adapter recovery are untested. See [comparison](comparisons/service-failure.md). |
| Authorized remote control | koh generic gateway, retained-session grace | zor/koh | Open: separate control/attachment grants; real reconnect inside/beyond grace |
| Comparative outcomes | prompt section 8, seven metrics | validation | Open: equivalent fixtures, measured results, no unsupported wins |
| Minimal fux invariants | current ECS/structural tests | fux | Implemented boundary gate: reviewed AST declaration/macro inventory, semantic tripwires and opaque-agent-OSC terminal behavior. Whole-goal final full-diff review remains open |
| Packaging/toolchains/platform checks | owning repository check commands and CI | validation | Open: full final gate and clean combined reconstruction |
| Documentation and independent review | prompt section 9 | all | Open: final full-diff review, results and limitations |

## Composition reconciliation

The pre-existing zor edits changed its preface from `FUXCTL1` to four-byte `FUX` and introduced a screen cache seeking a `seq` field. The pre-existing koh edits removed attachment version fields and assertions. These files are in scope because they are the actual failing wire consumers. Restore the required current fux versions while retaining the screen-cache intent; add a coherent generic revision contract in the following API slice. No unrelated companion changes were found in the initial diff.

## Verification record

Whole-goal final verification remains incomplete. The records below contain slice verification and three narrow comparative scenarios; a passing slice does not close the objective.

### Composition/capture slice

- Restored explicit version negotiation; control schema now `FUXCTL3` for coherent capture metadata. Attachment stays v5 in this slice. koh fixtures explicitly send/assert v5.
- fux capture returns text, dimensions, revision, viewport offset, title, progress, unchanged/truncation flags atomically. Listing revision is only an invalidation hint. No agent data was added to fux.
- zor uses capture metadata, skips unchanged revisions, clears incomplete evidence, and recovers after truncation. Fixed pre-existing state-loss publication carrying an agent ID alongside state `none`.
- Companion diffs were inspected: their original wire-version removal was incompatible with fux. The screen-cache intent was retained and corrected. Source patches were reconciled deliberately.
- Combined CI now runs on ordinary push/PR events in its own job; standalone jobs retain independent builds.
- `cargo test --locked --lib --test ecs --test structure --test zor_integration -- --test-threads=1` with explicit ZOR_BIN and FUX_REQUIRE_ZOR_BIN: passed (76 + 19 + 8 + 1). Updated real-process snapshot assertions were subsequently exercised in the forced integration rerun.
- Strict fux clippy passed. zor library tests: 42 passed, including new cache/recovery/edge-geometry cases.
- Independent reviewer `review_capture`: initial P2 truncation shutdown fixed; incomplete docs corrected. Final review found no remaining confirmed slice findings; independently ran all 42 zor library tests successfully.
- Baseline `python3 tools/measure.py target/debug/fux --version 5 --samples 5` could not collect CPU/RSS because the environment rejects spawning `ps` with Operation not permitted. No baseline performance numbers or improvement claim are available from that run.

Next: incarnation-scoped pane access, ordered input receipts, event synchronization and final-output retention. Observation service, bundled fixtures, task workflows and comparative evidence remain open.

### Server-incarnation slice

- `Session` receives the OS-generated instance nonce. Listings expose it; every control request may require it. Both ECS operations and socket-managed subscriptions reject mismatches before effects.
- zor pins its initial listing incarnation and uses it on later listings/captures. It cannot silently transfer cached evidence to a replacement server.
- Added a deterministic replacement-server test with identical pane ID/PID and real socket stale capture/input/subscription rejection assertions.
- Attachment bumped to v6 because embedded control requests changed; koh and local attachment/detach harnesses migrated. This supersedes the v5 state recorded for the earlier capture-only slice.
- Strict fux/zor clippy passed; fux library/ECS/structure checks passed (76 + 20 + 8); forced real-zor integration passed. Real local CLI/viewer suite with v6 passed all 6 scenarios.
- Independent reviewer confirmed identity enforcement. Its P2 attachment-version finding was fixed; final review found no further identity/authority issue and requested refreshed patches/current readiness docs. Those were reconciled after review.
- Real koh gateway suite was rerun with explicit FUX_BIN and KOH_REQUIRE_FUX_BIN: both tests failed at netmon initialization, `Operation not permitted`. Runtime gateway verification remains blocked by this environment; source reconstruction success is not runtime evidence.

The full goal is still active. Next implementation slice: ordered input receipts/deduplication, followed by event synchronization/final-output retention and the zor runtime workflows listed above.

### Ordered input receipt slice

- Generic `input-reserve`, `input-submit`, and `input-status` require the server instance. Reserving writes nothing; retries of identical decoded input return the existing receipt. Conflicting bytes and intervening controller/viewer input reject before dispatch.
- Receipts distinguish reserved, queued, delivered, and failed with actual known bytes written. Delivery is PTY write/flush acceptance, not application processing. Terminal query replies intentionally do not advance the controller/viewer input sequence; capture returns that sequence alongside coherent output metadata.
- Retention is 60 seconds from reservation, at most 128 records server-wide, with no unexpired eviction or ID reuse. Expiry means unknown evidence and does not cancel queued writes. Records remain in memory and require an accessible workspace; final-output/workspace retention is still open.
- Per-pane input buffering is bounded at 4 MiB including the active write. Stalled and failed writers produce explicit tracked failures without blocking control requests. Shutdown closes the inbound receiver before joining producers and terminates a stalled child before joining its writer.
- Real `input_receipts.py` passed: discarded submission reply/reconnect, identical retry without duplicate bytes, conflicting retry, competing input, unknown operation, queue exhaustion, responsive control, and bounded shutdown.
- `cargo test --locked --lib --test ecs --test structure --test local_cli -- --test-threads=1`: library 78 and ECS 24 passed; local CLI exposed a migration-harness timeout. Separate structure run passed 8. Strict clippy and formatting check passed. Added protocol fixtures reject missing instances for all three commands; targeted hostile-frame test passed.
- Migration timeout reproduced twice: its final wait stopped draining PTY output. Keeping output drained preserves the original deadline/status/session assertions and passes. Subsequent full CLI run exposed a separate initial-render assertion before the status-bar paint completed; the harness now waits for the required pane-ID cells within its existing deadline. Final `cargo test --locked --test local_cli -- --test-threads=1`: all 7 passed. `git diff --check` passed.
- Independent reviewer found no substantive receipt issues; requested optional-instance wording was corrected. It independently passed partial-write, interference-exclusion, and protocol rejection checks, and reviewed the migration drain fix without findings.

Next: snapshot/event synchronization and retained final evidence. Zor observation, task coordination, recovery, worktrees, dashboard, and comparative proof remain open; no superiority claim is established.

### Event synchronization slice

- Workspace-owned ECS logs assign nonreused stream IDs and nonwrapping sequence numbers. Listings capture a cursor alongside their fields. `events` reads bounded replay; `subscribe after` registers before replay and suppresses duplicate live entries through that boundary.
- Unknown streams, future cursors, evicted intervals and exhausted sequences report `gap`. Recreated workspace names cannot reuse the stream. Filters apply identically to replay/live delivery; disconnect always requires replay or a fresh snapshot.
- Replay and subscriber queues are bounded by 1024 entries and 512 KiB serialized data per workspace/subscriber respectively. Active subscriber writes retain their byte charge until completion/drop; all overflow disconnects, including output notifications.
- `workspace.changed` invalidates silent layout/focus/rename changes and controller/viewer input sequences. Rate-limited output schedules a trailing notification, with no periodic wakeup for idle panes. Events remain generic invalidation evidence, not raw output, state patches, or application completion.
- Deterministic tests cover replay floor, byte/count eviction, foreign/future cursors, sequence exhaustion, wire round trips, subscriber budget release/filtering, silent mutation invalidation, and trailing burst notification. The first timer assertion was corrected for the harness's injected ten-millisecond step increment.
- Real `event_sync.py` passed snapshot/replay, filtered live events, registration race deduplication, eviction, explicit gap and resynchronization. Required combined run passed: library 83, ECS 25, local CLI 8, structural 8, real zor integration 1. Strict clippy, formatting and diff-whitespace checks passed.
- Independent reviewer raised missing silent-state invalidation; resolved with generic notifications and corresponding tests. Final review found no remaining confirmed issues and independently passed event-log, subscriber-overflow and ECS invalidation tests.

Next: retained final output/exit evidence, then zor's automatic observer and task workflows. The overall goal remains active and comparative superiority is unproven.

### Final evidence slice

- Pane release retains a plain-text final viewport (128 KiB cap), optional observed exit status, launch metadata, workspace stream, input sequence and monotonic release/expiry times in an ECS resource independent of pane/workspace entities.
- `fux final --instance INSTANCE PANE` and manager `request:final` read it after workspace retirement. Instance mismatch/live pane return conflict; unknown, evicted or expired evidence returns expired. Recreated workspace names cannot overwrite old pane records.
- Capacity is 128 records server-wide, evicting oldest, with a maximum 60-second lifetime and no minimum under pressure. The empty manager stays available while evidence remains; explicit shutdown bypasses retention. Records do not survive server restart. These are last-observed viewports, not scrollback transcripts, and null exit status does not establish complete output or success.
- Deterministic tests cover release, same-name recreation, incarnation guards, unknown/expired records, capacity eviction, idle expiry, forced-release unknown exit and capture truncation. Real harness covers the manager and CLI after the last workspace closes, reuse of the name, and explicit shutdown.
- Required combined run passed library 83, ECS 27, local CLI 9, structural 8 and real-zor integration 1. Subsequent test-only additions passed 5 targeted ECS tests and the real final-evidence harness. Strict clippy, formatting and diff checks passed.
- Reviewer found descriptor-based test cleanup missed a retained manager after workspace descriptors disappeared. Cleanup now obtains the PID from the live manager socket's kernel peer credentials; the real harness explicitly verifies this descriptorless case. Final independent review found no remaining confirmed issues and independently passed all 5 final-evidence ECS tests.

Next: zor's automatic observation service, bundled evidence, and task/prompt workflows. The complete goal remains active; no herdr-superiority result is yet established.

### Zor discovery foundation

- Extracted the authenticated bounded fux client; single-pane observation continues through it. Manager requests and default runtime discovery are now shared by zor's consumers. Connect uses nonblocking sockets with a deadline rather than a potentially blocking connect.
- Added foreground `zor watch [--once] [--runtime PATH]`: discovers workspaces and up to 128 panes without manually supplied pane/PID, emits full JSON observations, explicit removed identities, bounded problems, scan duration and conservative observation-age bounds. Read-only observation grants no pane/worktree lifecycle ownership.
- Cache identity includes server incarnation, workspace stream, pane/root PID, detected foreground PID, agent and capture revision. One scan pins a single incarnation. Missing or unmatched evidence is unknown, never implicit idle/task completion. Existing wrapper/protocol-only modes remain available.
- Reviewer identified cumulative serial RPC latency; fixed with a shared two-second RPC budget, rotating workspace order and freshness metadata. Native inspection/evaluation/stdout are not advertised as hard real-time. Removed the unbounded macOS ps environment fallback; native inspection remains and inaccessible environment evidence is unavailable.
- Bound rule identifiers to 128 bytes so repeated observation metadata cannot amplify arbitrarily large IDs. Documentation explicitly labels this as raw foreground discovery groundwork, with daemon/API, atomic rule reload, hysteresis integration, event-driven reconciliation and task semantics still unfinished.
- Zor library: 45 tests passed; protocol-only check and strict clippy passed. Added registry identity/loss and stalled-peer deadline tests. Real-fux/real-zor integration passed automatic two-workspace discovery and working/idle custom-fixture interpretation while preserving fux pane ownership. These synthetic rules do not prove support for real agent releases.
- Final independent source review found no remaining substantive findings; RPC deadline test independently passed. Companion source patches refreshed after inspecting all nested changed paths. Clean reconstruction and fux/zor builds passed; reconstructed ECS28/localCLI9/realZor1 passed. The full verify --build command failed at koh gateway runtime tests: 8 passed, 2 could not initialize netmon (Operation not permitted). This repeats the known environment restriction and does not establish gateway runtime correctness.

Next: finish the automatic service/API and ship provenance-backed bundled detection before building the prompt/task runtime. The original full objective remains open.

### Rule loading and atomic reload

- Zor now resolves the standard HOME configuration fallback when XDG_CONFIG_HOME is absent or relative. Explicit rule directories override defaults in argument order; sorted filenames make same-ID replacement deterministic.
- Catalog publication is atomic after all files validate. SIGHUP in `zor watch` requests reload outside the signal handler. Failure preserves the previous collection/generation and reports a rules problem; success increments rules_generation and invalidates cached verdicts even without terminal output. Signal registration is released when the loop returns.
- `zor agents` now lists the actual loaded IDs/process names/rule counts as JSON instead of always reporting no bundled sets. Agent IDs, effective process-name lists and aliases are validated/bounded; inherited names include the agent ID in their limit.
- Tests cover configuration fallback, override order, invalid-reload preservation, successful replacement, and inherited-name boundaries. Zor library 47 passed before the last boundary test; all 6 bundle tests, strict clippy and protocol-only check passed afterward. Real fux/zor integration verified invalid reload retains generation 1/idle, then valid reload reclassifies the same unchanged screen to blocked/generation 2 without restarting its pane.
- Independent review found no new confirmed issues. Companion patches exported and source reconstruction verification passed. Final independent review included the inherited-limit boundary fix and found no remaining findings; all six bundle tests independently passed.
- This does not complete real-agent detection: active bundled manifests, provenance/version inventory and positive/negative real-agent fixtures remain open, along with the daemon/API and task runtime.

### First real startup bundle and unknown-state correction

- Captured installed Codex CLI 0.153.4 through a disposable real fux server with no input, fresh HOME/XDG/CODEX_HOME, and an environment allowlist. Recorded binary hashes, UTC time, viewport geometry, revisions and elapsed sample times. Identical visible samples are grouped; their revisions continued advancing despite unchanged text. Reproduction tool and reviewed provenance live in zor/tools/capture_startup.py and zor/tests/fixtures/agents/README.md.
- Zor now embeds a narrow rule for the exact recorded Codex sign-in selection screen. Built-ins load before external configuration and remain replaceable by same-ID overrides. This establishes startup needs-input evidence only, not authenticated working/approval/idle, resize, exit, Claude Code or OpenCode coverage. No herdr superiority is claimed.
- Independent review found that enabling this set would expose the old evaluator's unmatched-idle fallback in wrapper/observe. Fixed the evaluator to return Unknown, mapped it through both consumers, and made the state machine clear prior state and pending idle confirmation while retaining internal process identity. OSC none reports and heartbeats omit agent as required by the protocol.
- Tests cover real startup plus derived quoted/shell/stale/altered/malformed negatives, default bundle loading and override replacement, blocked-to-unknown and pending-idle-to-unknown. Zor full suite passed: library 50, bundled fixture 1, CLI 2, OSC 6, passthrough 9. Strict clippy, protocol-only check, formatting and diff checks passed.
- Required real fux/zor integration passed, including observer blocked-to-none, watch blocked-to-unknown, wrapper blocked-to-none, and unchanged pane PID. Independent reviewer confirmed the unknown-state fix and passed state/fixture tests; final harness delta review found no remaining findings. Companion patches exported and source reconstruction verification passed.
- Full service/API, remaining real-agent coverage, task/prompt coordination, worktrees/results, recovery/dashboard and comparative scenarios remain open. Known gateway netmon and performance ps environment restrictions remain; neither affected check was rerun or claimed passed in this slice.

### Shared local observation service

- Added zor-owned `serve` and `status` commands. One scan worker publishes complete registry snapshots through a separately owned Unix socket; API readers do not start observers or acquire pane lifecycle authority. Fux production code is unchanged in this slice.
- Endpoint creation requires an owned private directory, a private regular no-follow lock file, and exclusive nonblocking flock. Duplicate startup preserves the running service; stale owned sockets recover under the lock; graceful cleanup checks the created socket inode. Both peers validate kernel UID.
- API v1 supports one snapshot or ping request per connection, with caller IDs, random service incarnation and nonwrapping scan sequence. Limits: 32 clients, 4096 request bytes, 512 KiB reply, three-second client lifetime. Nonblocking clients cannot stall the single scan worker or other admitted clients.
- Freshness includes scan duration and time since publication at response construction. Pending/overdue scans are explicit; stale observations become unknown and lose matched rules while retaining handles. Documentation requires callers to account for later transport/consumer delay. Full snapshots support reconciliation across skipped publications; removed is an internal-scan delta, not a client replay log.
- SIGHUP retains the prior collection on invalid reload and reclassifies after valid reload. Status reads the service even when caller-side rule files are invalid. SIGKILL/restart reattaches to unchanged pane identities; fux loss clears observations and reports a manager problem. No pane is restarted or terminated by stopping zor.
- Full zor suite passed: library 52, bundled fixture 1, CLI 2, OSC 6, passthrough 9. Strict clippy, protocol-only check, formatting and whitespace checks passed. Required real integration now runs both observer and service scenarios; it passed in 14.72 seconds.
- Independent review found no substantive issues. Corrected its minor framing wording: extra requests on a connection are unsupported, not guaranteed a validation response. Final documentation review matched freshness, reconciliation, lifecycle and harness coverage.
- Clean source reconstruction, fux/zor builds, ECS28, localCLI9 and expanded realZor1 passed. Full `python3 tools/dependencies.py verify --build` failed at koh gateway tests: eight passed, two failed netmon initialization with Operation not permitted. This is the existing OS restriction; gateway runtime correctness remains unverified. Companion patches were refreshed after the final documentation correction, and source verification passed.
- Still open: automatic background startup, event-driven observation, broader real-agent coverage, task/prompt journal and API, worktrees/results, managed recovery, dashboard, remote grants and comparative scenarios. This completes the foreground shared-observation foundation, not the overall agent-runtime objective.

### Background startup and service shutdown

- Added `zor status --start`: an existing compatible service wins without re-reading caller rules; only a missing/refused endpoint permits startup. Live incompatible, foreign or stalled endpoints are reported without replacement. A new service validates rules, inherits a private bootstrap socket, starts a separate Unix session, and uses absolute explicit rule paths from its private working directory.
- Startup preparation and activation are distinct. Caller loss before activation cleans the child service; once activation is received, a lost acknowledgement cannot revoke it. A reaper waits for the detached child without acquiring shutdown authority. Concurrent starters converge through the existing service lock and the winner's valid API response.
- Added `zor shutdown` and instance-checked API shutdown. Missing/stale identity rejects before changing the stop flag. A completed reply acknowledges stopping, not final process exit. Neither startup nor shutdown starts, stops or restarts fux pane processes.
- Independent review found a post-activation acknowledgement write error still caused teardown. Fixed it and added an explicit lost-acknowledgement test with the caller's read side closed before activation. Pre-activation caller-loss cleanup remains covered. Final review found no substantive issues; corrected the documentation qualification that existing-service status does not load rules.
- Full zor suite passed: library52, fixture1, CLI2, OSC6, passthrough9. Strict clippy, protocol-only, formatting and whitespace checks passed. Expanded real fux/zor integration passed in 15.01 seconds: concurrent startup over a stale socket, invalid startup config, caller loss on either side of activation, unchanged pane handle, caller-exit survival, stale shutdown and incompatible-endpoint rejection, plus existing observer/service regressions.
- Companion patches exported and clean source reconstruction passed. Reconstructed fux/zor builds, ECS28, localCLI9 and realZor1 passed. The full verify --build command failed on the same two koh netmon initialization errors (Operation not permitted); eight gateway tests passed, and runtime gateway correctness remains unverified. Overall task orchestration, journal/receipts, semantic waits, worktrees/results, broad agent evidence, dashboard and comparative proof remain open.

Next prompt-correlation baseline: re-inspected herdr src/api/wait.rs (prompt_agent). Its prompt activity gate includes an already-working pre-prompt state or a working/blocked response; the subsequent initial match does not require a newer state_change_seq. The pre-submission event cursor is retained for lifecycle replay. These source branches identify comparison cases to run (especially pre-existing blocked state and immediate replies), not a measured superiority result or a claim that all herdr waits fail. Zor's next slice must use its recorded fux input operation, revision/input sequence and explicit task outcome model rather than treating idle/blocked as completion.


### Durable task identity and unsent prompt intent

- Added zor task adopt/list/inspect/prepare/discard-prepared/forget. Adoption checks a live fux incarnation and records canonical runtime, workspace stream, pane/root PID and explicit adopted ownership. It never acquires pane/worktree cleanup authority. Tasks share matching sessions while retaining distinct attempts.
- Caller task and prompt IDs make retained identical retries idempotent; conflicting intent fails. Original absolute runtime spelling is retained so committed adoption replays even after that directory disappears. Preparation stores intent/deadline without input and excludes another active/uncertain prompt on the same pane target across tasks. Cancelled preparation does not reactivate on retry. Forget only removes no-input metadata and preserves shared sessions until unreferenced.
- Private durable state uses XDG_STATE_HOME/zor or HOME/.local/state/zor, separate from runtime sockets. Nonblocking flock serializes CLI processes; bounded typed JSON rejects unsafe files, duplicate IDs, bad relationships and unsupported versions. Candidate writes sync ancestry, file, atomic rename and directory. Post-rename sync errors explicitly report uncertain durability. Recognized bounded partial candidates are discarded under the lock without treating them as committed state or deleting unrelated files.
- Bounds: 128 tasks/sessions, 512 attempts, 1024 prompts, 4 MiB journal, 64 KiB text and 24-hour maximum stored timeout. Active/uncertain records are not automatically evicted. Delivery, wait outcome and task outcome are distinct; no command can mark a task verified or infer success from terminal state.
- Reviewer found adoption replay incorrectly depended on runtime canonicalization and first-use ancestry lacked sync. Both were fixed; the real harness now removes the runtime path during replay, and storage syncs ancestry before candidate creation. Final independent review found no remaining findings and independently passed all three journal tests.
- Full zor suite passed: library55, fixture1, CLI2, OSC6, passthrough9; strict clippy, protocol-only, formatting and whitespace checks passed. Expanded real fux/zor integration passed in 16.73 seconds, including independent-process replay/concurrency, shared-target exclusion, corruption preservation and metadata cleanup while pane PID/input sequence remain unchanged.
- Companion export and source reconstruction passed. Reconstructed fux/zor builds, ECS28, localCLI9 and realZor1 passed. Full verify --build failed on the same two koh netmon initialization errors (Operation not permitted); eight gateway tests passed. Gateway runtime verification remains blocked by this OS restriction. Prompt submission, receipt reconciliation, semantic waits, service task operations, managed launches, worktrees/results, dashboard and comparative evidence remain unfinished. The full goal remains active.

### Receipt-backed prompt submission in zor

- Added task reserve/submit/reconcile entirely in zor. A retained prompt records the server-issued reservation and a synced submitting phase before terminal input. Retries inspect and reuse that operation; reconciliation never sends input. Lost/expired receipt evidence remains uncertain rather than allocating replacement input. Known delivered/failed receipts survive later server unavailability in zor's journal.
- Initial input contract is explicit single-line UTF-8, rejecting controls and oversized escaped representation during preparation. Literal backslashes remain literal and submission appends one carriage return. Multiline/paste adapter support remains unfinished. Submission validates the stored server, workspace stream, pane and root PID; foreground-process identity and semantic response correlation remain separate unfinished work.
- Shared journal locking serializes cooperating zor callers. Human input invalidates older fux reservations and is never suppressed. Partial failure retains uncertainty/exclusion. Submission rechecks the prompt deadline after target validation and durable submission intent, and caps the RPC budget to remaining lifetime; previously accepted PTY input can complete later. Delivered means PTY writer/flush evidence only and leaves the semantic wait pending.
- Independent review found uncertain partial delivery could release writer exclusion and slow target validation could bypass the submission deadline. Both were fixed and covered by regressions. Preparation validation and stale task documentation were also corrected. Final source review found no remaining correctness findings in this slice.
- Full zor suite passed: library57, fixture1, CLI2, OSC6, passthrough9. Strict clippy, protocol-only, formatting and whitespace checks passed. The real fux/zor task harness passed with a lost submission reply, missing/expired receipt evidence without new reservation, same-operation retries, human intervention, delayed target check crossing the deadline, and a killed submitting CLI recovered by a new process without duplicate input. Synthetic partial-failure unit coverage proves exclusion policy; this is not real-agent response or comparative herdr evidence.
- Semantic waits, explicit resolution of submitted/uncertain history, multiline adapters, service task API, managed launches, worktrees/results, broad agent fixtures, dashboard and measured comparison scenarios remain open. The full goal remains active.
- Clean reconstruction passed fux/zor builds, ECS28, localCLI9 and expanded realZor1 (17.90 seconds). Full `python3 tools/dependencies.py verify --build` failed on the same two koh netmon initialization errors (`Operation not permitted`); eight gateway tests passed. Runtime gateway correctness remains unverified under this OS restriction. Final crash-test delta review found no issue and explicitly limited coverage to the tested boundary. Companion patches were re-exported after documentation updates and source reconstruction reverified.

### Explicit cancellation of zor coordination

- Added `zor task abandon OP` and `zor task cancel TASK`. Abandonment releases only the selected prompt's coordination; task cancellation atomically marks the task cancelled and releases its prompts. Both retain delivery evidence and adopted resources. Neither sends Ctrl-C, closes panes, retracts queued input, or claims success. Repeated cancellation/abandonment does not create a new generation.
- Released prompts keep `wait: cancelled` through successful or failed read-only receipt reconciliation. Reserve/submit reject their IDs even when delivery is already known. New prompts can follow explicit abandonment, while cancelled tasks refuse new preparation. Reserved/submitted history cannot be forgotten. Cancelled tasks without receipt history can be forgotten using the existing metadata-only policy.
- Schema validation now rejects cancelled tasks retaining unreleased prompt coordination. The existing shared journal lock serializes cancellation and submission; cancellation does not preempt a lock holder or promise to undo accepted input. A new prompt after explicit release may overlap late-arriving old bytes; this is documented operator responsibility, not claimed delivery cancellation.
- The integration test and independent review identified a stale unconditional cancelled-wait guard that prevented reconciliation of released nonterminal receipts. Removed it. Tests cover uncertain receipt release, failed lookup then recovered reservation, late reconciliation with another pending prompt, preserved cancelled state, immutable retained IDs, no extra input, unchanged pane PID, submitted-history retention and malformed cancelled-state preservation. Final independent review found no remaining findings.
- Full zor suite passed after fixes: library57, fixture1, CLI2, OSC6, passthrough9. Strict clippy, protocol-only, formatting and direct real-task integration passed. No fux production change was needed. Semantic waits, adapter correlation, managed cancellation/cleanup, service task commands, worktrees/results, agent coverage, dashboard and comparative proof remain unfinished; the full goal remains active.
- Companion export/source reconstruction passed. Reconstructed builds, ECS28, localCLI9 and realZor1 (18.49 seconds) passed, including the final malformed cancelled-record case. `cargo +1.91.0 check --manifest-path zor/Cargo.toml --all-targets --locked` passed. Full `verify --build` failed on the same two koh netmon initialization errors (`Operation not permitted`); eight gateway tests passed. This remains an environment verification limitation, not a whole-goal impasse.

### Prompt-scoped explicit response evidence and one-shot waits

- Preparation now retains a random per-prompt report token. `zor task report` records one immutable response/needs-input claim scoped to token, prompt, accepted fux operation, caller-asserted producer lifetime and sequence. Exact event retries preserve the original receive time; stale tokens, wrong operations, conflicting events and pre-submission/late reports fail. This is a single explicit integration event, not a producer heartbeat stream or proof of agent processing.
- `zor task wait` performs one bounded check without input or a sleeping waiter queue. It reconciles receipts, checks live target identity and input sequence, and accepts the prompt-scoped report only after delivered PTY evidence. No passive screen, echoed text, old blocked/idle state or working transition is used. A changed input sequence yields uncertainty. It can recover matching final process-exit evidence and retains its exit status, separately from delivery and task success. Missing/expired evidence never becomes success.
- Wait deadline expiry retains timed-out coordination until explicit abandonment. Response/needs-input/exit/timeout/cancelled outcomes are historical and survive later receipt failures. Cancellation retains its priority. Both initial and reconciled unsent reservations are rejected by wait evaluation. Journal validation requires matching delivered reports for semantic response outcomes and exit status for process-exited outcomes.
- Independent review identified the need to preserve terminal waits through the receipt-error path and to repeat the unsent guard after reconciliation. Both are present; regressions cover delayed queued receipt evidence through timeout, failed lookup and recovery, plus an uncertain operation restored to unsent. Final independent source/docs/test review found no remaining correctness findings in this slice.
- Real-fux task harness passed: no report from stale/echoed idle/blocked text, wrong-token/operation rejection, exact report retries, conflicting report rejection, immediate response without working, prior prompt token rejected for the next prompt, human input weakening a recorded claim, cancellation, timeout, and a real shell exit with status 17. Reports are manually supplied synthetic integration claims, not real-agent adapter coverage or a measured herdr advantage.
- Full zor tests passed: library57, fixture1, CLI2, OSC6, passthrough9. Strict clippy, protocol-only, formatting and Rust 1.91 all-target checks passed. Explicit real-agent hook wiring, producer binding/lifetime validation, continuous observation precedence/heartbeat expiry, blocking/service waiters, broader lifecycle failure boundaries and passive-adapter evidence remain unfinished. Task results, worktrees, managed lifecycle, dashboard, remote task services and comparative proof also remain open; the full goal remains active.
- Companion export and clean reconstruction passed. Reconstructed fux/zor builds, ECS28, localCLI9 and expanded realZor1 (19.59 seconds) passed with the final exit-reply validation. Full `python3 tools/dependencies.py verify --build` failed on the same two koh netmon initialization errors (`Operation not permitted`); eight gateway tests passed. No gateway runtime pass or full-goal completion is claimed.

### Bounded blocking CLI waits

- Added `zor task wait OP --follow [--timeout-ms N]`. It returns a terminal result, uncertainty, or a separate caller-deadline envelope. The caller budget defaults to 30 seconds and accepts 1 ms through 24 hours. It does not become a prompt/task timeout. The returned prompt is the last observed snapshot, with that limitation documented for contention/deadline cases.
- At most 32 callers per state directory acquire owned/private empty retained waiter lock files. OS flock releases admission on return or process death. Admission can fail busy; admitted callers retry only typed journal contention within their budget. Corrupt/unsafe state and other lock failures are not retried as contention. Polling sleeps up to 250 ms without holding the journal lock; active evidence transactions remain bounded by RPC budgets and native storage caveats.
- Follow callers pin attempt, creation time and report token. Single checks also pin identity across their unlock/reconcile/relock interval. Missing/replaced identities fail instead of inheriting a reused prompt ID. No fux production changes were required.
- Review found caller expiry during an RPC could downgrade prompt/delivery evidence to uncertainty. Separate caller-deadline propagation now bypasses those mutations and returns caller-deadline. Regression tests stall both listing and receipt lookup and require unchanged journal state. The receipt test initially bypassed its forwarding runtime; corrected the fixture target, then both cases passed. Final independent review found no remaining issues.
- Direct real-task integration passed delayed report completion, caller timeout distinct from prompt timeout, cancellation while waiting, killed-waiter recovery, and both stalled-RPC cases. Unit coverage verifies all 32 slots, exhaustion, retained inode/mode and invalid lock content. Full zor suite passed: library58, fixture1, CLI2, OSC6, passthrough9. Strict clippy, protocol-only, formatting and Rust 1.91 all-target checks passed.
- This is a local polling CLI foundation. Shared-service waiters/coalescing, actual agent integration and producer binding, remaining lifecycle failure boundaries, worktrees/results/managed tasks, dashboard, remote task control and comparative metrics remain unfinished. The full goal remains active.
- Companion export/source reconstruction passed. Reconstructed fux/zor builds, ECS28, localCLI9 and realZor1 (21.73 seconds) passed. Full `python3 tools/dependencies.py verify --build` failed on the same two koh netmon initialization errors (`Operation not permitted`); eight gateway tests passed. Gateway runtime verification remains restricted by the OS, while meaningful goal work remains available.

### Durable task operations through the zor service API

- Added the version-1 `op: task` envelope, requiring the current service incarnation before queue admission. Nested actions expose adopt/list/inspect/prepare/reserve/submit/reconcile/report/wait/abandon/cancel/discard-prepared/forget using the existing durable implementations. Transport IDs correlate replies; durable task/prompt IDs remain the retry authority. The single-check wait is exposed; CLI follow mode is not queued as a long-lived service job.
- One task worker executes at most one job with eight additional queued jobs. Task RPC/storage work never runs on the observation/API loop. Pending task sockets are omitted from readiness polls so disconnected peers cannot induce a POLLHUP spin. Request frames are bounded at 256 KiB, replies at 512 KiB, clients at 32, initial framing at three seconds and admitted task connections at 15 seconds from acceptance. Expired queued jobs have no effects; started jobs may finish after client loss/expiry. Graceful shutdown skips queued work and waits for active native work under documented limits.
- Global state-directory configuration now reaches foreground and background services; request payloads cannot choose another state root. API adoption uses the configured fux runtime. Existing retained targets preserve their own runtime. Same-UID authentication and service identity checks precede dispatch, and remote task exposure remains separately unauthorised/unimplemented.
- Testing caught serde accepting unknown fields on a fieldless list variant. Changed it to a strict empty struct variant and added a rejection test. Expired-job unit coverage proves no state directory creation. Final independent review found no remaining correctness issues.
- Real service tests passed CLI/API journal agreement, a prompt frame above the old limit, lost submission reply with retained operation, stale service rejection, service restart persistence, explicit state selection through concurrent background starters, task-worker stalls with responsive ping/snapshot, queue-overload IDs absent from the journal, and corrupt task storage without observer disruption.
- An oversized cancellation reply test proves `response-too-large` may follow a committed mutation: CLI inspection confirms cancellation, then stable-ID forgetting removes only prepared/no-input history. The test delta received a separate clean review. Documentation explains recovery rather than implying every failed reply had no effects; large-history pagination remains unfinished.
- Full zor tests passed: library59, fixture1, CLI2, OSC6, passthrough9. Strict clippy, protocol-only, formatting and Rust 1.91 all-target checks passed. No fux production or koh source changes were required. Actual agent integrations/producer binding, managed launches, worktrees/results, dashboard, event-driven observation/shared waiters, remote task services and comparative proof remain unfinished. The full goal remains active.
- Companion export and clean reconstruction passed. Reconstructed fux/zor builds, ECS28, localCLI9 and expanded realZor1 (33.70 seconds) passed. Full `python3 tools/dependencies.py verify --build` failed on the same two koh netmon initialization errors (`Operation not permitted`); eight gateway tests passed. This remains an OS verification limitation, not completion or a whole-goal impasse.

### Managed command launch and live reconciliation in zor

- Added CLI/service start and launch-reconcile operations. Zor commits bounded launch intent and a submitting phase before process creation; an argv environment marker permits reconciliation against fux's ordinary command/cwd listing. The real command runs in a fux-owned pane, without a zor wrapper PTY. Successful live reconciliation atomically attaches a managed session, task and attempt. Pending launches remain inspectable before task creation.
- Stable-ID retries reject changed intent. After creation may have been sent, retries only reconcile and never issue another creation request. Missing or nonunique live markers, process exit, or lifetime changes remain uncertain. This slice does not yet resolve immediate exits through retained final/event evidence, explicitly rejected creation, pending-launch cancellation, managed cleanup/resume, or history compaction.
- Independent review found adoption could reuse a managed session and creation could race workspace replacement between listing and dispatch. Adoption now reuses only adopted sessions; managed-ID adoption replay is rejected. Fux's generic new operation accepts a workspace stream precondition, checked in ECS before creation, and zor sends its recorded stream. No agent semantics were added to fux. Final independent source/test review found no further confirmed issues.
- Real launch tests passed marker visibility, distinct fux-owned pane identity, stable-ID replay/conflict, managed prompt submission, cancellation preserving the process, separate adoption ownership, lost creation reply, killed creator recovery, and no replacement after a request dropped before delivery. Generic event tests prove stale workspace-stream creation fails without command effects. Service tests passed managed launch/replay, adopted ownership separation and managed-session persistence across restart. These are synthetic command/protocol fixtures, not actual-agent correctness or comparative herdr evidence.
- Full zor suite passed (library59, fixture1, CLI2, OSC6, passthrough9), as did strict clippy in both repositories, fux library83/ECS28/localCLI9, and zor's no-default-features check. An initial attempted named protocol-only feature check failed because no such feature exists; the corrected no-default-features check passed. Clean reconstruction and toolchain-floor checks are recorded below when terminal.
- Full goal remains active. Actual agent integrations/producer binding, closed-launch recovery, worktrees/results, dashboard, event-driven observation/shared waiters, remote task services and measured herdr comparisons remain unfinished.
- Rust 1.91 all-target checks and formatting/whitespace checks passed. Companion patches were exported; clean source reconstruction passed fux/zor builds, ECS28, localCLI9, and expanded realZor1 (36.87 seconds). Full `python3 tools/dependencies.py verify --build` failed only at the same two koh netmon initialization errors (`Operation not permitted`); eight gateway tests passed. Gateway runtime verification remains restricted by the OS. Meaningful local work remains, so the full goal is neither complete nor at an impasse.

### Closed-launch evidence and buffered RPC replies

- Zor can reconcile an ambiguously created command that closed before live attachment. A retained creation reply supplies the pane ID; otherwise a unique matching pane.opened event after the launch cursor supplies it. Manager final evidence must match pinned incarnation, workspace stream, pane, exact marked command and cwd. Replay gaps, expired/missing finals and ambiguity remain uncertain without replacement creation.
- Closed recovery atomically records a managed session, finished attempt and still-open task. Historical PID is nullable only for a closed managed session; adoption and live input validation require a real PID. Compact final evidence retains optional observed exit status, input sequence, and at most 4096 UTF-8 bytes with an explicit truncation flag. Finished attempts reject prompt preparation. Zero exit and pane closure do not verify task success. Committed ownership survives post-rename sync errors.
- Real launch fixtures passed known-pane recovery, lost-reply event recovery, actual final text/exit17, and known-pane recovery after workspace deletion. Task-owned proxies test replay gap, duplicate marker events, wrong final stream, expired finals, and oversized multibyte evidence. No test claims actual-agent response guarantees. The first output-size fixtures were too small after viewport clipping; explicit synthetic oversized final text now tests the journal cap independently of geometry, with the byte boundary inside a multibyte character.
- The larger reply exposed a real zor RPC bug: reconfiguring socket read timeouts after peer closure failed on macOS despite buffered unread data. Independent native socketpair reproduction confirmed EINVAL for a valid SO_RCVTIMEO after closure, while the same timeout succeeded with the peer open. Response reading now polls a fixed deadline and drains nonblocking bytes, handling interrupted/would-block reads and EOF. An attempted microsecond-normalization fix was disproven and removed. The new large-reply integration passed after the polling fix.
- Full zor suite passed again (library59, fixture1, CLI2, OSC6, passthrough9), strict clippy and Rust 1.91 all-target checks passed. Initial reconstruction reproduced the RPC failure; a fresh reconstruction after the fix is recorded below when terminal. Source/test review and formatting remain part of the final slice checks.
- Remaining launch work includes lifecycle updates after successful live attachment, recovery when both pane ID and workspace event history are lost, explicit pending/failed intent resolution, resume and cleanup. Actual-agent integrations, worktrees/results, dashboard, shared event-driven observation/waits, remote task services and herdr comparisons remain unfinished. The full goal remains active.
- Final independent review found no confirmed issue in closed recovery, ownership/schema invariants, test cleanup, or fixed-deadline polling. It independently reproduced the Darwin socket failure and reviewed the UTF-8 boundary assertion. Formatting/whitespace and protocol-only checks passed. No fux production or koh source changes were needed for this slice.
- Fresh companion export and clean reconstruction passed fux/zor builds, ECS28, localCLI9, and realZor1 including the closed-launch/buffered-reply regression (37.45 seconds). Overall `python3 tools/dependencies.py verify --build` failed only at the same two koh netmon initialization errors (`Operation not permitted`); eight gateway tests passed. The affected gateway runtime checks remain unverified under this OS restriction; useful local work remains available.

### Lifecycle reconciliation for attached managed sessions

- Explicit launch-reconcile now refreshes an already attached managed session. It validates the pinned live handle or retrieves matching final evidence. A confirmed close finishes the existing attempt and records final evidence while preserving session/task identities, original PID, task outcome and all prompt receipts/waits. It never creates a replacement or equates closure with verified success. Identical start retries remain stored reads.
- Missing live/final evidence retains managed ownership and records an attempt uncertainty/problem. Live recovery clears that lifecycle problem and restores an uncertain attempt to active. New prompt preparation is rejected while the attempt is uncertain or finished. Prompt evidence remains independently reconciled; cancellation is retained through every lifecycle transition.
- Real fixtures passed unchanged live reconciliation, observation loss and live recovery with stable identity, prompted later exit with preserved prompt history, and cancelled-task closure preserving cancellation/receipts/waits. Testing exposed the legitimate interval between removal from live listings and final-record publication. A shared bounded retry asserts history preservation during uncertainty; the immediate-exit proxy now waits for actual final publication before replying. Independent review caught the equivalent natural-exit fixture race, and final review confirmed both test fixes. No production correctness finding remained.
- Full zor suite passed (library59, fixture1, CLI2, OSC6, passthrough9), strict clippy, Rust 1.91 all-target checks, protocol-only checks, formatting and whitespace checks passed. Final lifecycle harness passed after the fixture corrections. Companion export/reconstruction results are recorded below when terminal; the reconstruction started before the final fixture-only retry correction, with identical production source.
- Automatic background lifecycle reconciliation, managed cleanup/resume, unknown-pane recovery after loss of workspace history, pending/failed intent resolution, worktrees/results, real-agent integrations, dashboard, remote task services and measured herdr comparisons remain unfinished. The full goal remains active.
- Clean reconstruction passed fux/zor builds, ECS28, localCLI9 and realZor1 (38.54 seconds). Full `python3 tools/dependencies.py verify --build` failed only at the same two koh netmon initialization errors (`Operation not permitted`); eight gateway tests passed. Final fixture-only corrections passed the direct real-binary launch harness and independent review. Gateway runtime verification remains OS-restricted; meaningful local work remains available.

### Explicit managed pane termination

- Added CLI/service `task stop ID` entirely in zor. It requires a reconciled managed launch; adopted or unresolved launches grant no termination authority. A synced transaction records stop_requested and cancels task coordination/releases prompt waits before any external close request. The original server/workspace/pane/PID is validated and never replaced with a newly discovered target. Fux receives only its existing generic instance-scoped pane kill request.
- Success requires confirmed retained final evidence and phase closed. A lost/rejected close reply or publication lag leaves recoverable intent; retry the same ID. Repeated known-closed stop is a stored read. An initial transient validation failure followed by live recovery cannot falsely report a successful stop: it returns pending intent without a kill. That outcome edge was identified by independent review and tightened with a regression. Final independent review found no remaining confirmed issue.
- Real launch tests passed adoption rejection without mutation, persisted cancellation during observation loss without kill, transient recovery without false stop success, lost kill reply with one close request, killed stopper after accepted close with later recovery, and preservation of sibling panes. Existing prompt receipt evidence is retained with explicit cancelled waits. Process stop removes no directories/worktrees, grants no adoption cleanup authority, and does not claim agent task success or graceful application shutdown.
- Full zor suite passed (library59, fixture1, CLI2, OSC6, passthrough9), strict clippy, Rust 1.91 all-target, protocol-only, formatting and whitespace checks passed. Service coverage includes adopted-stop rejection and managed stop after service restart; its initial assertion used the wrong error-envelope shape and was corrected. Final service/reconstruction results follow below.
- Automatic execution of pending stops, unresolved launch intent handling, worktree creation/cleanup, results/verification, resume, real-agent integration, dashboard, remote task services and measured herdr comparisons remain unfinished. The full goal remains active; this is the process-termination part of managed cleanup, not completion of the full workflow.
- Final direct service harness passed managed stop after restart, adopted-stop rejection and sibling task preservation. The first reconstructed integration also caught a stale fixture assumption: stopping a sibling advances global journal generation. The full restart snapshot assertion now precedes stop; afterward all task fields except the global generation are checked unchanged, and the new snapshot remains the authority for the later background-service restart check. No production change was needed for either fixture correction. A fresh reconstructed integration is running against the corrected fixture.
- Final independent review accepted the service fixture correction. Fresh clean reconstruction passed fux/zor builds, ECS28, localCLI9 and expanded realZor1 (46.27 seconds). Full `python3 tools/dependencies.py verify --build` failed only at the same two koh netmon initialization errors (`Operation not permitted`); eight gateway tests passed. No fux production or koh source changes were needed. Gateway runtime checks remain OS-restricted; useful local goal work remains.

### Zor-owned worktree creation foundation

- Added CLI worktree create/list/inspect/reconcile and equivalent service task actions. New literal branches resolve a pinned base commit before mutation. Zor reserves a random private parent inside its state directory, pins repository/common-directory and parent identities, and records Allocating/Prepared/Creating before the corresponding effects. Repeated ambiguous creation only inspects git registration; it never repeats worktree add. Main checkout and unrelated worktrees are preserved.
- Git runs through argv without a zor shell or PTY, with noninteractive input, excluded system/global configuration, disabled hooks, 256 KiB per-output-stream bounds, per-phase deadlines and a private process group. Repository-local checkout filters retain native git semantics. Timeout/output errors kill the unreaped child group and reap the child; native OS calls are not claimed to have hard real-time bounds. A runner regression exercises a stalled subprocess and output pressure.
- Review found directory allocation before journal commit could leak unrecorded parents. Allocation intent now commits first; capacity rejection creates no directory. Explicit recovery may pin only an empty private directory at the exact reserved path under the selected state root. Nonempty/replaced directories are preserved. Review also found Ready-state errors were hidden; they now remain visible while retaining historical ownership and clear after valid reconciliation without rejecting legitimate worker commits.
- Initial readiness requires unique exact-path/branch registration, matching pinned HEAD/common directory, no git initialization/prunable marker and a clean tracked checkout. Testing accounts for caller death closing git output pipes: git may finish or roll back. The real crash fixture proves uncertainty/no-repeat behavior; a separate journal boundary injection proves completed git work can reconcile after a missing Ready commit. These are distinct guarantees.
- Independent review reproduced Git expanding @{-1} into a different branch. Zor now requires check-ref-format's normalized result to equal the literal requested branch before persisting intent. A real previous-checkout/deleted-branch regression proves no alternate branch or worktree record is created. Final source/test/docs review found no remaining confirmed issue in this creation slice.
- Disposable git tests passed branch conflicts, same-ID replay/conflict, main checkout preservation, symlink/identity failure and recovery, worker commits, incomplete checkout rejection, crash/no-repeat, post-git recovery, journal capacity without orphan allocation, and reserved empty-directory recovery while preserving nonempty contents. The fixture's final guard observes its owned git process group until disappearance without signalling a potentially reused group; that delta received independent review. Service create/list/inspect/reconcile and strict request-shape tests passed.
- Full zor suite passed (library60, fixture1, CLI2, OSC6, passthrough9), strict clippy, Rust 1.91 all-target, protocol-only, formatting and whitespace checks passed. The direct service test initially found the fux binary absent; rebuilding fux restored the test prerequisite and the service harness passed. Companion export completed; clean reconstruction results follow below.
- Task/attempt association, launching by worktree ID, active/dirty removal guards and explicit force behavior, owned branch/worktree cleanup, interrupted cleanup, results/verification, fan-out/handoffs, actual-agent integrations, dashboard, remote task services and measured herdr comparisons remain unfinished. The full goal remains active; this is creation provenance and recovery, not completed worktree workflows.
- Completed reconstruction passed fux/zor builds, ECS28, localCLI9 and expanded realZor1 (45.39 seconds). Overall `python3 tools/dependencies.py verify --build` failed at the two koh netmon initialization errors (`Operation not permitted`); eight gateway tests passed. The exact outstanding process was polled to terminal completion before starting the next slice.

### Launching managed tasks by zor worktree ID

- CLI `task start --worktree ID` and service start `worktree` select a ready owned checkout; exactly one of worktree and cwd is required. The launch durably retains the worktree ID, with the existing task → attempt → session → launch relationships. Journal validation rejects dangling worktree links and mismatched paths. Explicit cwd does not infer worktree ownership. No fux production or koh source change was needed.
- The current ready worktree is checked under the journal lock before initial launch intent and again before submitting a prepared launch. Missing, incomplete, replaced or mismatched worktrees cannot start a new worker. Dirty files and worker commits are allowed. External git/filesystem mutations remain outside zor's serialization; no cross-process filesystem transaction is claimed.
- Identical attached/closed retries remain stored reads even if the worktree disappears. Changing between an ID and a literal cwd, or changing IDs, is conflicting launch intent. Worktree creation and task launch remain separate recoverable operations. Removal/force guards, branch cleanup, interrupted cleanup, fan-out/results, actual-agent adapters, dashboard and comparative proof remain unfinished.
- Initial real-service integration passed worker actual cwd, main checkout preservation, symlink rejection, task/session association, CLI/service replay after checkout disappearance, and changed-selector rejection. Independent review found no production issue and identified a fixture race between cwd-file creation and writing; polling now waits for matching content. Added explicit unready-worktree rejection without mutation and malformed journal association preservation checks; final integration/reconstruction results follow when terminal.
- Full zor tests passed (library60, fixture1, CLI2, OSC6, passthrough9), strict clippy, Rust 1.91 all-target checks, protocol-only, formatting and whitespace checks passed. The full goal remains active.
- Final independent review accepted the fixture race fix and added rejection/preservation cases. The final direct service harness passed. Companion export and clean reconstruction passed fux/zor builds, ECS28, localCLI9 and expanded realZor1 (55.35 seconds). Overall `python3 tools/dependencies.py verify --build` failed only at the same two koh netmon initialization errors (`Operation not permitted`); eight gateway tests passed. The affected network runtime checks remain unverified under this OS restriction; useful local goal work remains.

### Guarded zor-owned checkout removal

- Added CLI worktree remove and service worktree-remove with a default-false force option. Only confirmed Ready owned checkouts grant removal authority. Normal removal refuses tracked, untracked and ignored data. Force bypasses only the dirty check. All non-Closed managed launches referencing the worktree or a cwd inside it block removal; task cancellation alone does not release process use.
- Review found two confirmed deletion risks: adopted sessions were omitted, and checkout directory replacement with the original .git pointer could evade parent-only identity checks. Both were fixed. First readiness now pins checkout device/inode, subsequent reconciliation/removal requires it, and confirmed/removing/removed records require that identity. Adopted targets use pinned fux lifetime/PID evidence and canonical/lexical reported cwd; unavailable evidence blocks removal. Exact retained managed-close evidence can settle an adopted view of the same target. Final independent review found no further confirmed issue within these boundaries.
- Synced Removing plus immutable force intent precedes git removal. Repeated remove/create/reconcile never repeats destructive removal after possible submission; absence of both checkout path and git registration confirms Removed. Partial/failed removal remains Removing with a problem and currently needs manual resolution. Branch, parent, siblings and task history remain retained. New managed launches using a literal cwd inside retained removal intent are rejected. No fux production or koh source changes were needed.
- Real disposable tests passed normal/dirty/ignored/force behavior, sibling/main/branch preservation, symlink and actual replacement-directory rejection, stable removed-ID retries, and synthetic pre-remove/post-git journal boundary recovery. Service tests passed independent adopted-worker and managed-worker refusal, unknown adopted runtime preservation, and successful force removal of a disposable dirty checkout. This is not OS-wide active-process detection: unadopted processes and later application cwd changes remain uncovered by fux creation-cwd evidence.
- Test fixes: the removal main-HEAD assertion now snapshots after the fixture's intentional filter commit; synthetic Allocating records clear the prior checkout identity; the service's intentionally retired proxy leaves a retained adopted target unresolved, so the test correctly requires preservation instead of successful removal. Final direct worktree/service harnesses passed after these corrections. Full zor suite passed (library60, fixture1, CLI2, OSC6, passthrough9), strict clippy, Rust 1.91 all-target, protocol-only, formatting and whitespace checks passed.
- Broader process-use detection, branch/parent cleanup, partial-removal resolution, task results/checks, fan-out, actual-agent adapters, dashboard, remote task services and measured herdr comparisons remain unfinished. The full goal remains active. Clean reconstruction results follow when terminal.
- Clean companion reconstruction passed fux/zor builds, ECS28, localCLI9 and expanded realZor1 (54.00 seconds). Overall `python3 tools/dependencies.py verify --build` failed at the same two koh netmon initialization errors (`Operation not permitted`); eight gateway tests passed. No gateway runtime pass is claimed. Meaningful local goal work remains available.

### Current root-process cwd evidence before worktree removal

- Removal now inspects current local root-process cwd for retained sessions whose creation cwd is outside the target worktree, including managed sessions. macOS uses exact-sized proc_vnodepathinfo with bounded NUL parsing; Linux reads /proc/PID/cwd. Paths must be absolute and canonicalizable. The pinned fux target is checked before and after the OS sample; unavailable evidence refuses removal. No fux or koh production change was needed.
- Real service fixtures on macOS passed managed and independently adopted commands launched outside a worktree that changed into it: both normal and force removal refuse. Added a live root in a subsequently unlinked directory to prove unavailable current-cwd evidence preserves the checkout; final fixture result follows when terminal. All processes, marker files and directories belong to the disposable fixture.
- Full zor suite passed (library60, fixture1, CLI2, OSC6, passthrough9), strict clippy, Rust 1.91 all-target, protocol-only, formatting and whitespace checks passed. Independent source review found no confirmed issue in FFI bounds, failure handling or target revalidation. Linux runtime behavior is not verified by this macOS run; no installed Linux target is available locally.
- This narrows the active-use gap; it does not close it. Descendant-process cwd, unadopted processes, PID interpretation across remote hosts and changes after preflight remain outside these checks. Broader active-use detection, remaining worktree cleanup/recovery, results, agent adapters, orchestration/dashboard and measured herdr comparisons remain unfinished. The full goal remains active.
- Final deleted-cwd fixture and independent review passed. Companion export/clean reconstruction passed fux/zor builds, ECS28, localCLI9 and expanded realZor1 (59.77 seconds). Overall `python3 tools/dependencies.py verify --build` failed only at the same two koh netmon initialization errors (`Operation not permitted`); eight gateway tests passed. The network runtime checks remain unverified under the OS restriction. Useful local goal work remains.

### Descendant process cwd before owned worktree removal

- Added a dedicated bounded process-tree inspection path in zor instead of using best-effort agent detection scans. It collects root/descendant cwd, caps traversal at 256 processes, repeats child-list inspection, and shares the three-second session preflight budget. Native OS/filesystem calls remain subject to their normal latency. macOS uses a fixed child-PID buffer and parent checks; Linux bounds threads, children and each procfs child-list read. Incomplete, denied, changed, cyclic, expired or oversized evidence refuses removal.
- Independent review identified that PID-only child lists could accept stale cwd evidence after PID reuse. Fixed by retaining OS start identities from child discovery through the queue, around cwd sampling, and across both passes. macOS captures birth and parent in the same process-info record; Linux uses procfs start ticks and parent from one stat record. Synthetic regressions cover a changed birth after cwd and a queued child whose birth differs from discovery. Final review found no remaining confirmed issue in this slice.
- The final real service fixture passed normal/force refusal with a retained root and intermediate process outside the checkout while their cat grandchild used it. The marker is written only after subprocess creation established cwd, and the task-owned pane is closed after the assertion. Existing root-chdir, deleted-cwd, unknown-target, successful outside-tree removal and service lifecycle scenarios also passed.
- Full zor suite passed after fixes (library61, fixture1, CLI2, OSC6, passthrough9), strict clippy, Rust 1.91 all-target, protocol-only, formatting and whitespace checks passed. Native execution was macOS; Linux runtime coverage remains unverified locally. No fux or koh production change was needed.
- Unadopted processes, descendants already reparented away before the snapshot, root-exit/orphan scenarios and subsequent forks/chdir remain outside this bounded preflight. Complete active-use detection, worktree branch/parent cleanup and partial-removal resolution, results/checks, orchestration, real-agent adapters, dashboard and herdr comparisons remain unfinished. The full goal remains active. Clean reconstruction results follow when terminal.
- Clean companion reconstruction passed fux/zor builds, ECS28, localCLI9 and expanded realZor1 (62.01 seconds). Overall `python3 tools/dependencies.py verify --build` failed at the same two koh netmon initialization errors (`Operation not permitted`); eight gateway tests passed. The affected network runtime checks remain unverified under this OS restriction, while useful local goal work remains available.

### Explicit task check command evidence

- Added zor CLI/API check execution and single-check inspection, plus compact task-inspect check summaries. Caller-selected argv runs in a managed task's recorded cwd without implicit shell/PTY. Stable check IDs persist Submitted before spawn and never replay commands. Finished captures invoked-process exit/signal and bounded stdout/stderr; Uncertain retains runner errors. Passing a check does not change TaskOutcome or prove verified task completion.
- The journal lock is released during execution, allowing CLI task inspection/cancellation. A cancelled task retains later check evidence; task stop/cancel does not terminate the check. Commands allow 1 ms through five minutes and use the existing bounded private-process-group runner, factored from git. CLI/service environments are inherited deliberately. The service still occupies one task worker during a check; asynchronous long-check service scheduling remains unfinished.
- At most 128 check records share the journal. Submitted checks reserve 64 KiB each within its four-MiB bound before execution. Review found that retaining that reservation after adding Uncertain error text could fail publication at capacity; publication now releases the reservation for either terminal evidence form. Review also found literal-cwd checks could bypass removal intent; they now use the same journal-wide cwd guard as launches. Unfinished checks block worktree removal. Final production review found no further confirmed issue.
- Real tests passed zero/nonzero/signal outcomes, UTF-8 output clipping, stable-ID conflict/no-repeat, timeout uncertainty, killed caller with no replay, task cancellation during a running check, removal refusal, literal-cwd historical launch plus removal-intent rejection without command effects, admission refusal before command effects at reserved capacity, and error publication at the exact journal byte cap. The initial capacity fixture violated the per-argument limit; padding now stays within each 4096-byte argument bound. A teardown review found that a process-group wait error could skip fux cleanup; unconditional nested server teardown fixes it and was independently reviewed.
- Service API tests passed captured failed-check evidence, retries/inspection and unchanged task outcome. Full zor suite passed (library61, fixture1, CLI2, OSC6, passthrough9), strict clippy, Rust 1.91 all-target, protocol-only, formatting and whitespace checks passed. Final direct harness adds explicit output-pressure and compact-summary assertions; final integration/reconstruction results follow when terminal. No fux production or koh source change was needed.
- CHECKS.md explicitly limits Finished to invoked-process exit; detached descendants may remain. Required-check policy, immutable source/artifact binding, verified outcomes, unresolved-check recovery, check cancellation, long-check service scheduling, compaction and full result collection remain unfinished, alongside other outstanding orchestration/agent/dashboard/comparison requirements. The full goal remains active.
- Final direct check harness and final test-delta review passed. Clean companion reconstruction passed fux/zor builds, ECS28, localCLI9 and expanded realZor1 including the required check harness (67.41 seconds). Overall `python3 tools/dependencies.py verify --build` failed at the same two koh netmon initialization errors (`Operation not permitted`); eight gateway tests passed. The affected network runtime checks remain unverified under this OS restriction; useful local goal work remains.

### Required command policy in zor

- Added CLI/API require-check to declare at most 32 task-scoped named argv requirements on managed tasks. Exact declaration retries are reads; changed commands fail. Policy additions seal atomically with the first check submission, including diagnostic checks. Execution IDs remain globally unique and never replay; a rerun uses a new execution ID and the same requirement name/argv. Wrong-task or mismatched commands fail before execution.
- Task inspect reports required and passed counts. Only each requirement's latest submitted execution counts, ordered by a unique persisted journal generation. Older late successes cannot override newer pending, uncertain or failed executions. Diagnostic checks never satisfy named requirements. This is command evidence only: immutable source/artifact binding and verified outcomes remain unfinished, and task outcome is unchanged.
- Direct checks and service integration passed policy retries, sealing, argv mismatch, task-local naming, malformed journal preservation and fail/pass/fail reruns. Added real concurrent older-success/newer-failure ordering and pending/timeout superseding earlier passes. Independent review found no confirmed production defect and requested proof the older command remains held after the newer failure; the fixture now asserts that before release. Final reconstruction tests this assertion.
- Full zor suite passed (library61, fixture1, CLI2, OSC6, passthrough9), strict clippy, formatting, Rust 1.91 all-target check and protocol-only all-target check passed. An initial protocol-only invocation used a nonexistent feature name; corrected to the repository's `--no-default-features --all-targets --locked` command and passed. Companion patches exported. No fux or koh production changes were needed. Clean reconstruction result follows when terminal; the full goal remains active.
- Final independent review accepted the held-process assertion with no further finding. Clean reconstruction passed fux/zor builds, ECS28, localCLI9 and the expanded realZor integration (72.71 seconds), including the final required-check concurrency fixture. Overall `python3 tools/dependencies.py verify --build` failed at the same two koh gateway netmon initialization errors (`Operation not permitted`); eight gateway tests passed. The subsequent gateway integration command was not reached. Network runtime verification remains restricted by the environment; source/artifact binding, results and the remaining full-goal requirements still need implementation.

### Retained output artifact bytes in zor

- Added CLI/API artifact-collect and artifact-inspect, journal Artifact records and compact task-inspect metadata. Open managed tasks can collect exact binary bytes beneath their recorded cwd; global stable artifact IDs are immutable reads on matching task/path retries, including after source deletion or task cancellation. Output bytes are stored atomically with task/attempt/path/time provenance in the private journal. Task outcomes remain unchanged.
- Descriptor-relative child opens reject symlink traversal and require intermediate directories. Final files must be regular and single-link; reads reject over 64 KiB and changed size/inode/timestamps. Linked worktree readiness plus opened-root inode checks retain ownership; removal-intent cwd is refused. A scoped OS adapter transfers each freshly opened raw descriptor into File ownership exactly once. No fux/koh production changes were needed.
- Limits are 128 records and 512 KiB retained bytes, subject to the four-MiB serialized journal and existing check-result reservations. Records retain exact bytes, not a project snapshot. ARTIFACTS.md explicitly documents concurrent mutation/rename limits, native I/O stalls, pre-publication retry behavior, missing compaction and unfinished source/check/result binding. No artifact collection is treated as verified success.
- Real CLI/service tests passed binary fidelity, stable retry after mutation/deletion, changed-intent refusal, compact summaries and post-cancellation reads. Real CLI tests also passed traversal, symlink/intermediate symlink, hardlink, FIFO and oversize rejection; malformed artifact attempt preserves the journal; valid exact-count and exact-byte-cap fixtures reject further collection without mutation. New unit coverage passed exact 64 KiB binary acceptance and oversized rejection. Initial independent review found no production defect and requested the now-added bound proofs and non-atomic snapshot wording.
- Full zor suite passed before the final test-only additions (library61, fixture1, CLI2, OSC6, passthrough9); the additional artifact unit passed separately. Strict clippy, formatting, Rust 1.91 all-target check, protocol-only all-target check and whitespace checks passed on final source. Companion patches exported; clean reconstruction and final test/docs review results follow when terminal. The full goal remains active: required artifacts, immutable source/check binding, assembled results and broader orchestration/agent/dashboard/comparison requirements remain unfinished.
- Final independent review accepted the boundary tests and documentation with no further finding. Clean reconstruction passed fux/zor builds, ECS28, localCLI9 and expanded realZor1 (70.76 seconds), including CLI/service artifact scenarios. Overall `python3 tools/dependencies.py verify --build` failed at the same two koh netmon permission errors; eight gateway tests passed and the subsequent gateway integration command was not reached. No network runtime pass is claimed. The ledger overview was refreshed to reflect implemented foundations while preserving all unfinished full-goal requirements.

### Assembled retained result evidence in zor

- Added CLI/API task result: a bounded read from one journal generation identifies task/attempt/session/agent/worktree, retained launch output, artifact/check references, required-check outcomes and prompt evidence. It neither reconciles nor reads live checkout/fux state. Pending launches without attached tasks remain inspectable. Existing task outcome is unchanged; verification is always explicitly unverified and changed files explicitly not collected.
- Required-check outcomes use latest submission generation, preserving missing, pending, uncertain, failed and passed distinctions. Prompt summaries are bounded to the latest 32 by wall-clock creation time then ID, with omitted count. Unresolved counts and blockers cover every prompt, including older omitted entries. Prompt text/report tokens, artifact bytes and full check stdout are omitted; individual inspection operations retain those evidence reads. Response serialization is capped at 256 KiB.
- Initial real CLI/API scenarios passed no journal mutation, stable repeated reads, missing/failed/passed/uncertain requirements, artifact references, cancellation and retained zero-exit output without verification. Final launch harness passed pending uncertain launch results. Extended service harness tests caller-claim/token separation, compact results despite oversized task inspection, and an older unresolved prompt outside the latest 32. Final direct service/reconstruction results follow when terminal.
- Independent review found no production defect and requested omitted-unresolved coverage plus explicit ordering/non-verification documentation. Those were added; final source/test/docs review accepted them with no further finding. Full zor suite passed (library62, fixture1, CLI2, OSC6, passthrough9), strict clippy, Rust 1.91 all-target check, protocol-only all-target check, formatting and whitespace checks passed. Companion patches exported. No fux/koh production change was needed.
- RESULTS.md describes evidence freshness, missing source/check binding, absent required-artifact policy, and empty blockers not implying readiness. This current-journal view is not a sealed completed result. Immutable source verification, required artifacts, changed-file evidence, handoffs/fan-out and the remaining full-goal requirements remain unfinished; the goal remains active.
- The extended service fixture initially shadowed its existing state() helper with a temporary dictionary; its result assertions passed but later observer assertions failed. Renamed the temporary to result_fixture, retained unconditional journal restoration, and obtained final review with no further collision found. The corrected direct service harness passed. An already-running reconstruction contained the old fixture and terminated with that exact failure after ECS28/localCLI9 passed; final-source reconstruction was started only after that run terminated.
- Final-source reconstruction passed fux/zor builds, ECS28, localCLI9 and expanded realZor1 (96.06 seconds), including all final result-view scenarios. Overall `python3 tools/dependencies.py verify --build` then failed at the same two koh gateway netmon permission errors; eight gateway tests passed, and the subsequent gateway integration command was not reached. These network checks remain unverified under the environment restriction. The complete goal remains active with substantial local implementation work still available.

### Retained changed-file observations in zor

- Added CLI/API changes-collect and changes-inspect with bounded journal Changes records and latest retained evidence in task result. Open managed tasks with linked owned Ready worktrees collect committed base-to-HEAD changes separately from index/worktree porcelain status, including untracked files and excluding ignored files. Stable global IDs read retained observations after cancellation; another task conflicts. Records retain task/attempt/worktree/base/HEAD/time/submission generation.
- NUL parsing preserves raw filename bytes; renames are represented as deletion/addition. Each record allows at most 256 paths, 4096 bytes per path and 32 KiB total filename bytes; at most 128 records share the existing four-MiB journal/check reservations. HEAD/diff/status are sampled twice under a shared six-second runner budget after worktree preflight, then checkout inode is rechecked. This is metadata stability evidence, not a content snapshot or check/source binding.
- Zor's git boundary disables fsmonitor and ignores replacement objects, alongside existing hooks/global-config/environment controls. Diff also disables external diff and text conversion. Repository-local clean filters retain native Git behavior. Review requested narrowing the initial no-helper claim to these specific protections; docs and test wording now do so. No fux/koh production change was needed.
- Real disposable CLI coverage passed clean/committed/unstaged/staged/untracked/ignored distinctions, newline/tab filenames, no-renames framing, immutable retries, latest result association, over-count refusal without journal mutation, cancellation reads, disabled fsmonitor/external diff and replacement-ref isolation. Native macOS rejected a filename containing invalid UTF-8; the physical fixture uses tabs/newlines and a separate passing parser unit covers invalid UTF-8 bytes and malformed NUL framing. No unsupported filesystem behavior is claimed. Service collection/retry/inspect/result association passed.
- Full zor suite passed before the final parser unit addition (library62, fixture1, CLI2, OSC6, passthrough9); the parser unit passed separately. Final strict clippy, formatting, Rust 1.91 all-target, protocol-only all-target and whitespace checks passed. Initial independent review found no confirmed production issue and requested replacement-ref coverage and scoped helper claims, now addressed. Companion patches exported; final review/reconstruction results follow when terminal.
- CHANGES.md and result/task/API docs describe retained observations, ignored files, Git/filter semantics, native I/O latency, sampling races and lack of content identity. Immutable source verification, artifact/check binding, sealed results and the broader agent/orchestration/dashboard/comparison requirements remain unfinished. The full goal remains active.
- Final independent review accepted replacement-ref coverage and the narrowed claims with no further finding. Clean reconstruction passed fux/zor builds, ECS28, localCLI9 and expanded realZor1 including the new required changes harness (91.59 seconds). Overall `python3 tools/dependencies.py verify --build` failed at the same two koh netmon permission errors; eight gateway tests passed and the subsequent gateway integration command was not reached. A final documentation-only artifact link correction was exported and byte-for-byte dependency reconstruction verified separately. Network runtime remains unverified under the environment restriction; useful local goal work remains.

### Reviewed multiplexer boundary gate

- Added tests/agent_boundary.rs with AST inventory of production structs/enums/type aliases, fields/types/variants and serialization/configuration/CLI attributes. The checked-in inventory includes 161 declarations and 261 macro definitions/invocations. Generic metadata or macro-generated state changes now require review even without an agent keyword. Test-only parser dependencies use already-resolved lockfile versions and add no runtime dependency on zor/koh.
- AST identifiers plus decoded string/byte/integer literals and macro tokens detect known agent/transport/worktree imports/names and agent OSC selectors. Direct cfg(test) items/doc attributes are excluded; other conditional code is scanned conservatively. Runtime terminal coverage proves byte-split OSC 7877 frames do not alter ordinary title/progress/capture/host replies. Existing structural checks continue to enforce dependencies, process-spawn ownership, bounded channels and ECS purity.
- Independent review found a confirmed initial blind spot: a generic macro-generated state type bypassed keyword checks and the declaration inventory. Fixed by recording macro definitions/invocations, and added a regression. Reviewer inspected the full declaration inventory and all 261 macro entries and found no prohibited agent policy. A migration-help mention of zor/koh is ordinary prose, not program invocation. Final review accepted the fix and documentation/tooling deltas with no further finding.
- docs/multiplexer-boundary.md documents the generic-consumer rule, candidate inventory update workflow, semantic review requirement and limitations. The inventory is a review aid, not proof against disguised policy in existing function bodies or generated code outside the scanned source. docs/design.md now accurately assigns task/worktree/check/result orchestration to zor. Clean reconstruction now explicitly runs both agent_boundary and structure test targets.
- Final local checks passed: library83, ECS28, structure8, boundary3, strict all-target clippy, Rust1.95 all-target check, formatting and whitespace checks. Standalone cargo package --locked --allow-dirty --offline passed; all three boundary tests also passed from the packaged source with an isolated target directory, without companion repositories. Companion source reconstruction verified. Full clean combined build/integration results follow when terminal. The overall goal remains active; immutable verification, broader orchestration/adapters/dashboard/comparisons and final full-goal review remain unfinished.
- Clean reconstruction passed fux/zor builds, boundary3, ECS28, localCLI9, structure8 and expanded realZor1 (92.69 seconds). Overall `python3 tools/dependencies.py verify --build` failed at the same two koh netmon permission errors; eight gateway tests passed and the subsequent gateway integration command was not reached. This does not block continued local goal implementation, but network runtime verification remains incomplete. No task or agent policy was added to fux by this slice.


### Bounded zor service check workers

- Check execution now uses two workers sharing one four-request queue, separate from the coordination worker and its eight-request queue. Inspection, result reads and cancellation can proceed while check commands run outside the journal lock. Counts apply per service instance; CLI execution and other services are independent. Journal preflight/publication may still return task-busy.
- Shared queue admission remains bounded and reports task-overloaded without durable command effects. Expired queued requests cannot create storage or execute commands. Graceful shutdown drops queued jobs and joins active checks so their outcomes can publish; fux and its panes remain alive. Cancelling a task prevents queued checks from starting but does not kill already submitted checks.
- Direct real-binary worker and service fixtures passed. The worker fixture proves two simultaneous held commands, four admitted queued requests, excess-request rejection, responsive inspection/cancellation, no cancelled queued command effects, and shutdown publication without queued effects. Independent review requested proof of shutdown queue admission; filling the queue and observing overload now establishes that boundary. Final independent review found no remaining confirmed issue.
- Full zor tests, strict clippy, Rust 1.91 all-target and protocol-only checks passed during implementation. Final expired-queue regression, formatting and whitespace checks passed after fixture strengthening. Companion patches exported; clean reconstruction results follow when terminal. No fux or koh production change was needed.
- This is a bounded synchronous execution pool, not durable asynchronous scheduling. The existing 15-second client reply budget can expire while a submitted check continues; stable check IDs retain no-replay semantics. Process cancellation, unresolved-check recovery, durable scheduling, immutable verification and the broader orchestration/adapters/dashboard/comparison requirements remain unfinished. The full goal remains active.
- Clean reconstruction passed fux/zor builds, boundary3, ECS28, localCLI9, structure8 and expanded realZor1 including the new required worker-pool fixture (96.00 seconds). Overall `python3 tools/dependencies.py verify --build` failed at the same two koh netmon initialization errors (`Operation not permitted`); eight gateway tests passed and the subsequent gateway integration command was not reached. Network runtime remains unverified under this OS restriction. Useful local work remains, so this is not a whole-goal blocker.


### Required artifact policy in zor

- Added CLI/API require-artifact declarations with at most 32 task-scoped names and exact relative paths on managed tasks. Additions seal on the first retained artifact (including optional collections) or submitted check. Exact declaration retries remain reads after sealing/cancellation; paths cannot be replaced and requirements cannot be removed. Existing required-check policy still seals at its first check submission.
- Artifact collection accepts an optional requirement name, validates the task/name/path before filesystem reads, and includes the name in immutable collection intent. Diagnostic collections never satisfy requirements. Retained records have unique positive journal generations; result views select each requirement's latest retained artifact and expose missing/collected status plus required-artifacts-missing. Task inspect reports sealed/required/collected counts.
- Failed fresh collection creates no record and does not supersede older retained bytes. This policy establishes availability of recorded output, not freshness, correct content or association with passing checks. Empty files count as collected. Task outcome remains unchanged and verification explicitly unverified. Source/check/artifact binding, sealed results and the broader orchestration/adapter/dashboard/comparison work remain unfinished.
- Expanded disposable CLI/service tests passed policy retries/conflicts, optional records not satisfying requirements, both sealing triggers, name/path mismatches, latest retained revision selection, missing output, cancellation reads, malformed associations/generations/limits without journal changes and exact 32-declaration capacity refusal. Full zor suite passed (library63, fixture1, CLI2, OSC6, passthrough9), strict clippy, Rust 1.91 all-target, protocol-only, formatting and whitespace checks passed.
- Independent production review found no confirmed defect and requested explicit failed-collection semantics; documentation and a regression now cover them. Final source review and clean reconstruction will verify the last fixture additions, including adopted-task refusal. Companion patches exported. No fux or koh production changes were needed. The full goal remains active.
- First clean reconstruction stopped at the existing viewer copy-notice fixture; an isolated rerun reproduced it. The fixture acknowledged split topology before waiting for child output, allowing an empty copy. It now waits for known pane text, acknowledges copy mode, selects explicit text and verifies the clipboard payload before checking notice expiry; the direct fixture passed. A direct service rerun exposed an unhandled FileNotFoundError during stale-socket unlink/rebind; the existing bounded recovery wait now retries that expected interval. Neither fix changes production behavior. Final fixture review and reconstruction are running.
- Final independent review accepted the fixture fixes and required-artifact documentation with no remaining confirmed issue. The corrected direct service fixture passed. Clean reconstruction passed fux/zor builds, boundary3, ECS28, localCLI9, structure8 and expanded realZor1 (101.60 seconds), including all final artifact-policy and synchronization assertions. Overall `python3 tools/dependencies.py verify --build` failed only at the same two koh netmon permission errors; eight gateway tests passed and the subsequent gateway integration command was not reached. Network runtime remains unverified under this environment restriction; useful local implementation work remains.


### Retained committed input for zor checks

- Added source-collect/source-inspect/source-file CLI/API. Managed tasks with owned ready worktrees can retain regular Git blob bytes and executable modes from an explicit resolved commit, separately from dirty index/worktree/untracked/ignored state. Stable source IDs bind task/attempt/worktree/requested revision and read retained content on retry, including after cancellation/removal. Compact inventories and result summaries omit full file bytes; individual file reads preserve exact bytes.
- Independent review found that the first implementation trusted object names without verifying retained data. Fixed with optional zor runtime SHA-1/SHA-256 dependencies: blob hashes validate bytes; recursively rebuilt Git trees validate paths/modes/blob mappings using Git's directory-slash sort; retained commit bytes validate commit identity and its tree. Source publication also rechecks checkout inode. Protocol-only/fux dependency boundaries are unchanged. Same-length byte changes, renames, mode changes and altered commit bytes are now rejected when opening the journal.
- Optional check --source binds an execution to its own task/attempt's earlier retained source. Submitted and a unique private state-root directory are durable before materialization/spawn. Each new execution receives fresh retained input; exact retries never reconstruct or rerun. Ordinary checks retain original launch-cwd behavior. Source directories remain after execution and can be modified by commands; no sandbox, dependency pinning, directory cleanup or proof of exclusive input consumption is claimed. Results remain unverified, and required-check counts do not yet select one common source.
- Bounds: 256 files, 64 KiB/file, 256 KiB total source file bytes, 32 KiB paths, 64 KiB retained commit object, 128 source records and 512 KiB aggregate source file bytes within the existing four-MiB journal/check reservations. Symlinks, submodules, special modes, unsafe/non-UTF-8 paths and unreconstructible explicit empty subdirectories are refused. Collection uses a shared ten-second Git command budget after worktree preflight while holding Store. Native I/O and command-written output retain documented limits.
- Direct source integration passed real SHA-1/SHA-256 repositories, binary/mode fidelity, nested-tree ordering, dirty/untracked/ignored exclusion, independent check copies, same-ID conflicts/no replay, different-task refusal, 128-record capacity/129 refusal, excess file count/size, corruption preservation, task cancellation and reads after owned worktree removal. Reviewer found the initial filter test lacked an attribute binding; a tracked .gitattributes now activates the configured driver and collection proves it does not run. The fixture corrected macOS /tmp canonicalization and retries documented stop/final-record visibility under a fixed deadline. Service collection/file reads/source-check/result associations passed.
- Full zor suite passed (library63, fixture1, CLI2, OSC6, passthrough9), strict clippy, Rust 1.91 all-target, protocol-only, formatting and whitespace checks passed after the integrity fix. Final independent review and clean reconstruction follow when terminal. Companion patches exported. No fux or koh production change was needed.
- Source-bound artifact evidence, coherent verification/result sealing, cleanup/cancellation/crash resolution and the larger orchestration/adapters/dashboard/remote/comparison requirements remain unfinished. The complete goal remains active.
- Final independent review accepted integrity, inode checks, fixture deltas and documentation. It requested qualifying the no-object-write claim for partial/promisor repositories; SOURCES.md now explicitly describes possible Git lazy fetching and object-store population. That documentation-only correction was exported and byte-for-byte reconstruction verified. Clean combined reconstruction passed fux/zor builds, boundary3, ECS28, localCLI9, structure8 and expanded realZor1 including the new required source fixture (106.98 seconds). Overall `python3 tools/dependencies.py verify --build` failed only at the same two koh netmon permission errors; eight gateway tests passed and the subsequent gateway integration command was not reached. Network runtime remains unverified under the environment restriction. Useful local goal work remains.


### Source-bound check artifact capture in zor

- Added repeatable CLI --artifact NAME=ID and strict API artifacts maps for up to eight declared required outputs on a source-bound check. Requests are immutable execution intent. Submitted reserves global IDs permanently, pending artifact slots/raw bytes and 300000 serialized bytes per capture in addition to the existing 65536 check-result bytes. Normal collection and other checks cannot take reserved IDs or capacity. Terminal publication releases capacity while retaining ID history; unresolved Submitted retains reservations.
- Materialization now returns an open root directory retained through execution. After an observed command exit, capture reads required paths descriptor-relatively from that original directory, rejecting symlinks, hardlinks, FIFOs, oversized or detectably changing files. Captured records link task/attempt/requirement/check/source. Successful captures, failures and command evidence publish atomically; artifacts from one check share its publication generation. Uncertain command outcomes skip capture and record explicit problems. A failed command can retain output; passed continues to describe command exit only.
- Task/result artifact_captures reports the latest check submission requesting each requirement, with pending/collected/failed status and blockers. This is separate from retained-byte availability. An older successful capture publishing late cannot hide a newer failed request. Same execution retries read recorded outcomes and never recollect or rerun; artifact-inspect reads captured bytes.
- Expanded real source fixture passed binary/batch capture, mixed success/missing outputs, immutable retry/conflicts, reserved-ID refusal, symlink/hardlink/FIFO/oversize failure, timeout evidence, malformed association rejection, renamed/replaced cwd preserving original descriptor capture, pending-state inspection and older success after newer failure. It also published a 64 KiB artifact plus maximally escaped command output at the exact four-MiB serialized-plus-reservation limit while another artifact write was refused. The artificial capacity journal is restored only after its live writer has exited. A fixture used nonexistent macOS /bin/true; corrected to /usr/bin/true. Check execution errors now preserve a bounded cause chain.
- Service capture/inspect/result association and retry/conflict scenarios passed. Full zor suite passed (library63, fixture1, CLI2, OSC6, passthrough9), strict clippy, Rust 1.91 all-target and protocol-only checks passed. Final strict duplicate artifact-map regression, formatting and whitespace checks passed. Independent review found no confirmed production defect and accepted the failure/concurrency tests; documentation was corrected to distinguish check outcome reads from artifact byte reads. Companion patches exported; final clean reconstruction results follow when terminal.
- Captures bind bytes observed after invoked-command exit in a source-initialized directory; files may already be present in input, and detached children/same-user writes remain outside atomic filesystem guarantees. Coherent source/check/artifact selection, sealed verification/results, cleanup/cancellation/recovery and the larger orchestration/adapters/dashboard/remote/comparison requirements remain unfinished. No fux or koh production change was needed. The complete goal remains active.
- Clean reconstruction passed fux/zor builds, boundary3, ECS28, localCLI9, structure8 and expanded realZor1 including bound capture, renamed-root, late-publication and capacity assertions (108.38 seconds). The final documentation-only byte-read clarification was exported and byte-for-byte reconstruction verified separately. Overall `python3 tools/dependencies.py verify --build` failed only at the same two koh netmon permission errors; eight gateway tests passed and the subsequent gateway integration command was not reached. Network runtime remains unverified under this environment restriction. Useful local goal work remains.


### Explicit sealed source verification in zor

- Added CLI `task verify TASK SOURCE` and service `verify` action. Requires an open managed task, reconciled attempt/launch evidence, resolved prompt coordination, no Submitted checks and at least one required check. Every latest required execution must finish with exit zero on one selected retained source without capture failures. Required artifacts must agree with both the latest capture request and latest retained bytes, and belong to selected required checks.
- One validated journal transaction records immutable task/attempt/source and selected check/artifact IDs with timestamp/generation, marks Verified and releases resolved coordination. Exact retries read the seal; another source conflicts. Journal load revalidates policy selection and generation relationships. New evidence/cancel operations refuse sealed tasks; managed stop preserves Verified and permits owned cleanup. Current result views expose the seal and its declared-policy scope, while later lifecycle evidence may still change.
- Real source integration passed missing-policy/execution rejection, diagnostic versus required evidence, unbound latest execution rejection, held Submitted rejection without journal mutation, pending-prompt rejection, resolved response history preservation, newer manual artifact refusal, immutable retry, malformed seal refusal, and stop/worktree removal retaining verification. Service integration passed including verify refusal for failed policy. Full zor tests passed (library63, fixture1, CLI2, OSC6, passthrough9), strict all-target clippy, formatting, Rust1.91.0 all-target check and protocol-only check. An initial abbreviated +1.91 invocation attempted toolchain sync and was permission-blocked; the installed exact +1.91.0 check passed.
- Independent review and real tests identified two old schema conflicts: stop required Cancelled, and released prompts required cancelled waits. Fixed stop to accept a sealed Verified outcome; resolved ResponseObserved/ProcessExited waits may remain released only for Verified tasks, whose prompts must all be released. Added regression tests and final independent source review found no further confirmed issue.
- This seals declared command outcomes and retained bytes, not semantic correctness, a sandbox, dependency pinning, agent termination or a frozen full result. Fan-out/handoffs, broader adapters/recovery/dashboard, remote services and measured herdr comparisons remain open. No fux or koh production changes were needed. Companion export passed; clean reconstruction result follows when terminal. Full goal remains active.
- Clean reconstruction passed fux/zor builds, boundary3, ECS28, localCLI9, structure8 and the full realZor integration (111.96 seconds), including sealed verification and cleanup. The final documentation corrections were exported and byte-for-byte reconstruction verified separately. Overall `python3 tools/dependencies.py verify --build` failed at the same two koh netmon permission errors; eight gateway tests passed and subsequent gateway integration was not reached. Log: `/tmp/fux-sealed-verification-build.log`. This environment restriction leaves remote runtime evidence unverified but does not block further local orchestration work.


### Verified predecessor handoff and two-worker composition in zor

- Added CLI `task handoff DEST --operation ID --from TASK ... --text TEXT` and matching service action. Up to eight distinct verified predecessors gate preparation of one ordinary durable prompt. The prompt pins predecessor verification generations, exact source/check/artifact references, instruction and canonical local journal path; a structured CLI prefix makes custom-state evidence access explicit. Preparation is atomic with existing writer exclusion. Submission, receipts, response claims, waits and cancellation reuse the existing protocol. No fux/koh agent state or production changes were added.
- Journal load reconstitutes and validates the exact handoff text against sealed predecessor evidence. Same-ID retries preserve coordination/deadline and never send implicitly; predecessor ordering is canonical. Changed intent/context conflicts, ordinary prepare cannot claim handoff intent, self/duplicate/unverified/oversized predecessor lists are refused. Instructions and journal path are bounded, and existing prompt/journal limits apply. Result summaries expose compact predecessor generations without instructions or tokens.
- Added required real-stack `zor_workflow.py`: two live scripted workers in isolated owned worktrees both receive work before collection; beta fails a declared check, preventing the handoff, then repairs under new prompt/source/check IDs. After both are verified, service API prepares the lead handoff in a nondefault journal; CLI retries preserve it and the supplied CLI prefix retrieves exact artifact bytes. Tested delivery retry, response distinct from verification, malformed text/generation/instruction/destination rejection, dirty removal refusal, force-owned cleanup and retained verified artifacts afterward. This is bounded composition of existing start/prompt/result APIs, not a durable group scheduler or real-agent adapter proof.
- Full zor tests passed (library63, fixture1, CLI2, OSC6, passthrough9), clippy with warnings denied, Rust1.91.0 all-target, protocol-only, formatting and whitespace checks passed. Existing task receipt/recovery/human-interference regression passed. Independent review identified missing custom-journal context, addressed with pinned CLI prefix and real evidence retrieval, and a fixture cleanup timeout that skipped fux teardown, addressed with unconditional finally cleanup. Final review/results follow below.
- HANDOFFS.md documents exact guarantees, bounds, local journal access, immutable relocation context and runnable scenario. Companion export passed; clean combined reconstruction is still running. Full objective remains active: durable orchestration/recovery, broader agent adapters, dashboard/notifications, remote authorization/runtime and measured herdr comparisons remain open.
- Final cleanup regression rerun passed and independent review confirmed unconditional fux teardown with no further issue. Clean reconstruction passed fux/zor builds, boundary3, ECS28, localCLI9, structure8 and the expanded full realZor suite including two-worker handoff (124.17 seconds). The final fixture-only cleanup correction was separately rerun; companion source reconstruction verified byte-for-byte. Overall `verify --build` failed at the same two koh netmon permission errors (eight gateway tests passed), leaving subsequent gateway integration unrun. Log: `/tmp/fux-handoff-build.log`. This remote-runtime restriction does not block further local goal work.


### Resume persisted managed stops in zor

- Added CLI `task recover [--after ID]` and API recover action, attempting at most one already-persisted stop_requested Attached launch under the same Store lock used for selection. Reuses normal stop ownership/identity/final-evidence checks; does not create intent, choose replacement panes, restart agents, replay prompts or remove worktrees. Reports selected ID, remaining candidate count and closed/unresolved evidence.
- Existing service coordination lane attempts one recovery on startup and at least one second after each completed attempt. In-memory roundrobin cursor advances past unresolved targets. Check workers and observer remain separate; task admission may be delayed under normal stop RPC deadlines, external CLI may receive Busy. Shutdown lets active recovery finish. Missing journal is not initialized; invalid or locked storage prevents effects. Broader automatic lifecycle recovery remains open.
- New required real-stack recovery fixture simulates the durable-before-kill crash point, proves stale-first rotation cannot starve a valid stop, and confirms cancellation-only/adopted/unrelated panes remain live. Actual observer service startup across multiple recovery cadences leaves absent task storage absent. Expanded two-worker scenario proves automatic stop preserves an existing Verified outcome. All targeted scenarios pass; full zor tests (library63, fixture1, CLI2, OSC6, passthrough9), clippy, Rust1.91.0 all-target, protocol-only, formatting and whitespace checks pass.
- Independent review found no production defect. Added actual observer-startup absence coverage as requested. Review identified possible Busy contention between background recovery and the workflow fixture's final manual cleanup; service now stops after automatic Verified recovery is proved, before manual cleanup. Final rerun/review follow below. RECOVERY.md, TASKS.md and SERVICE-API.md describe scope, authority and admission delays. No fux/koh production change was needed.
- Companion export passed; clean combined reconstruction is running. Full objective remains active, including broader recovery/orchestration, real-agent adapters, dashboard/notifications, remote services and measured herdr comparisons.
- Final workflow rerun passed after stopping background recovery before manual cleanup; final independent review confirmed the race fix and found no further issue. Clean reconstruction passed fux/zor builds, boundary3, ECS28, localCLI9, structure8 and the expanded full realZor integration including recorded-stop recovery. Final fixture-only cleanup sequencing was separately rerun. Companion source reconstruction also verified byte-for-byte. Overall `verify --build` failed at the same two koh netmon permission errors; eight gateway tests passed and subsequent gateway integration was not reached. Log: `/tmp/fux-recovery-build.log`. Remote-runtime evidence remains unavailable under this environment restriction; further local goal work remains.


### Zor dashboard and machine-readable attention view

- Added `zor dashboard [--once] [--bell] [--directory]`, using existing single-command service startup. No fux production change: dashboard is an ordinary terminal client, and navigation verifies the retained live handle before generic pane focus in its workspace. It does not switch a viewer attached to another workspace. Non-TTY interactive invocation is rejected before starting a service.
- New read-only service `overview` action supplies task/launch attention rows with actual service runtime/state-directory and journal generation. Dashboard reads observer and task views under the same service incarnation, retaining separate observation sequence/task generation rather than claiming one atomic snapshot. Task outcomes, attention labels, verification details and passive observations remain separate. Required-check failures, latest required-artifact capture failures (including diagnostic captures), prompt uncertainty/input, and launch/worktree problems surface as attention. No prompt text, report tokens or retained file bytes are exposed by overview.
- TUI supports selection, attention filter, evidence detail, optional attention-transition bell, resize and quit. One bounded fetch worker/channel keeps input separate from snapshot RPCs; target focus retains its bounded synchronous RPC. Terminal output is bounded and nonblocking with a deadline. Raw/alternate screen, cursor and descriptor flags restore before joining an in-flight fetch on exits/errors. Labels are bounded ASCII-safe text; original Unicode remains in JSON. Extremely stalled terminal restoration output remains best effort.
- Independent review identified a freshness defect: queued snapshots were timestamped at dequeue and observation ages did not advance. Fixed by retaining monotonic production time, exposing source age, and applying elapsed age before rendering, bells and every Enter action. Near-expiry/old-queue and tiny-terminal tests pass. Review also prompted capture/worktree attention and accurate Age-at-fetch wording. Actual task_outcome is now explicitly separate from attention status. Final independent source review found no further confirmed issue.
- Real disposable PTY fixture passed custom-journal service configuration, immutable reads, checks-failed/blocked attention, generic focus, bell deduplication, resizing, q/SIGTERM restoration and service-loss clearing. macOS baseline differences were independently reproduced: canonical restoration sets transient PENDIN (masked as in existing platform tests), and first terminal output adds a kernel write-history flag (fixture writes/drains a baseline byte before descriptor comparison). These are fixture corrections; mutable terminal modes/flags remain checked. A diagnostic required-artifact failure assertion was added for a final rerun.
- Full zor tests passed (library66, fixture1, CLI2, OSC6, passthrough9); strict clippy, Rust1.91.0 all-target, protocol-only, formatting and whitespace checks passed. DASHBOARD.md, README and service API describe exact bounds, source freshness, local focus scope and remaining limitations. Companion export and byte-for-byte reconstruction passed. Clean combined run is still live; final outcome follows below. Full objective remains active: broader adapters/orchestration/recovery, desktop notifications, remote services and measured herdr comparisons remain unfinished.
- Final expanded dashboard PTY fixture passed, including diagnostic capture failure, distinct task outcome, age wording and bell deduplication. Clean reconstruction passed fux/zor builds, boundary3, ECS28, localCLI9, structure8 and full realZor integration including dashboard (138.02 seconds). Final display/schema refinements were separately built, linted, toolchain-checked and exercised by the targeted dashboard fixture/unit tests; final companion sources reconstructed byte-for-byte. Overall `verify --build` failed at the same two koh netmon permission errors; eight gateway tests passed and subsequent gateway integration was not reached. Log: `/tmp/fux-dashboard-build.log`. The full goal remains active with meaningful local work remaining.


### Real Claude startup capture and unresolved cleanup

- Captured installed Claude Code 2.1.263 in an isolated HOME/XDG/CLAUDE_CONFIG_DIR at 80×23, without input. Observed empty, partial welcome, and full theme-selection screens. OpenCode was absent from PATH. No Claude rule or published positive fixture was added: the capture server consistently required forced cleanup. Broader real-agent coverage remains unfinished.
- Agent-free tests retaining a copied PTY slave in an independently owned helper passed in both canonical and raw terminal modes. They did not reproduce the failure and do not justify a PTY pump redesign. The temporary experimental fixture was removed.
- Temporary fux logging located the stall after reader EOF: input writer joined, the reaping gate reached zero holders, and child try_wait repeatedly returned Ok(None). Initial SIGHUP/SIGKILL calls succeeded; later group signals returned EPERM. A separate 45-second cleanup trial also timed out. macOS denied stack sampling. The root cause remains unresolved; these observations do not establish whether it is a product defect or environment restriction. All capture server handles terminated after bounded force-kill/reap; child reaping was not confirmed. No personal process was signalled. Temporary logging was removed and the ordinary fux binary rebuilt successfully.
- Startup capture now accepts completed unit RPC replies, attempts a private-pane close, and preserves diagnostic screens on failure without letting diagnostic publication skip server cleanup. Failed or nonzero server cleanup cannot publish startup.json. Independent review found version-output decoding could bypass cleanup; moved decoding before server launch. Mocked normal, diagnostic-write failure, pane-close failure, and server-timeout branches passed before that refinement; final verification follows. Diagnostic captures remain local under /tmp/zor-claude-*. No comparative advantage is claimed.
- Final targeted checks passed: `python3 zor/tools/test_capture_startup.py` (seven failure/success subcases), `cargo test --manifest-path zor/Cargo.toml --locked --test bundled_rules`, Python compilation, root formatting and whitespace checks. Real Codex 0.153.4 capture succeeded with a zero server exit at `/tmp/zor-codex-cleanup-20260907/startup.json`. Export/reconstruction passed before the final README-only test-command addition; final review/export follow. The tests deliberately mock timeout/error paths and make no claim to resolve the Claude reaping failure.
- Final independent review found no remaining confirmed issue in the capture-tool slice and independently reran all seven mock subcases successfully. Signal-error and ProcessLookupError handling was source-reviewed without dedicated tests. Final companion export/reconstruction and whitespace checks passed. Full goal remains active; Claude cleanup and the broader adapter/comparison requirements are unresolved.


### Explicit PTY release closes the master before reaping

- Isolated direct Python PTY experiments narrowed the Claude cleanup finding: a session without a controlling terminal exited/reaped on termination, while a controlling-terminal trial reproduced the wait timeout outside fux. Closing the parent master allowed reaping. This identifies a controlling-terminal lifetime dependency; it does not establish its kernel/runtime internals.
- Fux now drops the read descriptor at EOF and drops its control descriptor during explicit release after termination, before joining the pumps. Its master is optional only to permit that early close; resize on a released handle returns BrokenPipe. No agent concepts, policy, RPCs or dependencies were added. Dropping only the reader was insufficient; dropping both allowed the real Claude Code 2.1.263 capture to finish with zero fux exit at `/tmp/zor-claude-drop-master-20260907/startup.json`. Natural EOF alone still retains the control handle.
- Generic Python/Node inherited-stdin readers, kqueue, raw-mode, and flow-control probes did not reproduce the OS stall. /dev/tty-open probes were denied by this environment and were not valid regression evidence. A deterministic agent-free unit fixture models the observed control-handle/reaper dependency with bounded waiting and no OS signals; it is not claimed as a reproduction of the platform-specific OS behavior. Initial fixed-order test passed; deliberate old-order probe and broader validation follow.
- Independent source review found no confirmed teardown regression and verified portable-pty owns separate master/read/write descriptors. Final review will include test behavior and the declaration inventory change from Box to Option<Box>. The broader goal remains active; a successful startup capture does not establish working/approval/idle agent coverage.
- Deliberately retaining master through join made the new fixture fail at its five-second deadline; restored fixed ordering passed. Final independent review found no confirmed issue in the implementation, fixture or documentation. Root validation passed library84, ECS28, localCLI9, structure8, boundary3, strict all-target clippy, Rust1.95.0 all-target, formatting and whitespace. Clean combined reconstruction is running with log `/tmp/fux-pty-release-build.log`; its final outcome follows when terminal.
- Clean reconstruction passed fux/zor builds, boundary3, ECS28, localCLI9, structure8 and realZor1 (138.82 seconds) with the PTY release fix. Overall `verify --build` again failed at the two koh netmon permission errors; eight gateway tests passed and the later gateway integration was not reached. No network-runtime pass is claimed.

### Claude startup rule and production screen normalization

- Added recorded Claude Code 2.1.263 theme-selection startup evidence and a default bundled rule. Preserved the executable's invoked symlink name in the capture tool; the recapture has argv claude and successful server cleanup. Empty and partial welcome screens remain unknown. OpenCode and broader authenticated state coverage remain open.
- Live default identification found claude correctly but initially returned unknown. Existing fixture tests used raw strings, missing the production ScreenView's trimmed row padding/final newline. Tests now reconstruct positive screens through the production terminal model; both Codex and Claude patterns match its normalized text. Claude's exact prefix/suffix/Unicode-length conjunction preserves whole-screen specificity under existing 512-byte regex limits. Synthetic negatives remain matcher tests, not raw terminal-input proof.
- The production-view test exposed a zor BoundaryTracker bug: UTF-8 continuation 0x9d in the visible ❯ marker was interpreted as OSC, removing its row. Added strict incremental UTF-8 tracking, including lead-specific ranges. Continuations no longer introduce/end C1 strings; every encoded payload byte still counts against limits, and incomplete UTF-8 is not a safe injection boundary.
- A split22 test then independently reproduced dropped text directly in vt100/vte without zor's filter. The inspected local vte0.15 and [upstream partial-character path](https://raw.githubusercontent.com/alacritty/vte/master/src/lib.rs) consume a valid prefix after printing only its first character, which can skip the space before another partial character. Zor now completes a pending character before bulk parsing using UTF-8 state without another byte buffer. Root fux shares the dependency; equivalent raw-chunk handling there remains an open correctness risk to investigate separately. No upstream source was edited or issue posted.
- Initial screen and bundled tests passed after these fixes. Final live observations, full zor verification, independent review and companion export/reconstruction follow. Full goal remains active.
- Full zor verification passed library68, bundled fixtures2, CLI2, OSC6, passthrough9, strict clippy (after checked fixture JSON reads), Rust1.91.0 and protocol-only checks. Capture-tool tests passed eight cases including preservation of an invoked symlink. Independent review reproduced and then confirmed the UTF-8 split fix; no remaining production finding in zor. Live default-rule observations passed for both Claude and Codex without configuration overrides, with startup blocker rules and no reported problem; both capture servers exited zero. Records are under `/tmp/zor-claude-final-live-20260907` and `/tmp/zor-codex-final-live-20260907`. Final companion export and byte reconstruction passed.
- Clean reconstruction with the new zor rules/parser passed through realZor integration, then failed at the same two koh netmon permission restrictions. The following fux UTF-8 fix was developed after that reconstructed source snapshot and needs its own final checks.

### Generic split UTF-8 output preservation in fux

- Confirmed the shared dependency risk in a real agent-free fux capture: Python writes `prefix ` plus an incomplete Ü, then its suffix and ` 🐝 end`; the old binary returned `prefix Ü🐝 end`. Added a one-byte parser-tail state and completion-before-bulk adapter in ServerTerminal, sharing the existing continuation-count helper with the control-string filter. No terminal bytes are newly retained, no agent logic or API fields were added, and existing parser panic containment remains.
- Expanded the existing every-split Unicode unit case and added a required real CLI fixture that captures the incomplete prefix before releasing the child via a task-owned file. The fixed binary passed and the old binary failed with the missing space. A harness JSON-to-TOML surrogate-escape issue was corrected with direct UTF-8 encoding. Independent review found no production issue and identified a partial-suffix capture race in the test; the fixture now waits for the complete expected text within its deadline and retains the last actual text for diagnostics. Final validation/review follow.
- Final root tests passed library84, ECS28, localCLI10 (including deterministic split capture), structure8 and boundary3, strict clippy, Rust1.95.0, formatting, Python compilation and whitespace. Final independent review accepted the fixture race correction and generic inventory additions. The previously open root dependency risk described above is addressed by this tested adapter; comparative performance still needs measurement.
- Final reconstruction initially rejected a stale zor patch because generated Python bytecode had been exported from an unignored tools cache. Added Python cache exclusions to root/zor .gitignore, regenerated the patch without bytecode, and verified reconstruction again after Python compilation. Source bytes now remain reproducible across those tests. No cache files were deleted. The final combined run is live at `/tmp/fux-unicode-final-build.log`; its terminal outcome follows.
- Final clean combined reconstruction passed fux/zor builds, boundary3, ECS28, localCLI10, structure8 and realZor1 (137.44 seconds). Log: `/tmp/fux-unicode-final-build.log`. Overall `python3 tools/dependencies.py verify --build` failed at the same two koh gateway netmon Operation-not-permitted errors; eight gateway tests passed and the subsequent gateway integration was not reached. All process/test handles are terminal. Final source and fixture reviews found no remaining confirmed issue in this slice. Full goal remains active: authenticated adapter states, broader orchestration/recovery/remote coverage and measured herdr comparisons are still unfinished.


### Abandoned check-runner recovery in zor

- Added one private persistent `check-runners.lock`. Cooperating runners hold shared admission from before durable Submitted intent through publication/retry; service and explicit task recovery take nonblocking exclusive admission under the journal lock. With no runners, one bounded transaction marks all remaining submissions Uncertain, records unavailable artifact captures, and releases pending byte capacity while retaining execution/artifact IDs. No command, signal, artifact read, or fux change is involved.
- Missing runners establish neither child termination nor absence of effects. Uncertain records retain worktree removal guards and cannot satisfy required checks. Optional uncertain checks cease blocking verification as pending work; seals still attest only selected required evidence. Any live runner defers all abandoned-check reconciliation, potentially indefinitely with continuously overlapping runners. Resolution of uncertain effects, process cancellation, scratch cleanup and compaction remain open.
- Real `zor_checks.py` killed its own caller while its child remained alive, recovered once without replay, checked read-only subsequent recovery, and preserved a live runner's eventual result. Review identified a masked cleanup assertion; the corrected crash case has a separate owned worktree and already-closed launch, with no other unfinished check to cause refusal. The isolated cleanup assertion passed. `zor_sources.py` injects a valid durable artifact-intent crash point and proves unavailable capture, absent retained bytes, and reserved-ID refusal; this synthetic case complements the real runner-death test.
- Targeted check, source and managed-stop recovery fixtures passed. Full zor tests passed library69, bundled2, CLI2, OSC6 and passthrough9; strict clippy, Rust1.91.0 all-target check and no-default-features protocol-only check passed. An initial attempted `protocol-only` feature flag was rejected because the package has no such feature; the corrected no-default-features command passed. Independent production review found no confirmed defect; final test review and clean reconstruction results follow.

- Final independent review accepted the isolated cleanup and artifact-intent fixtures, with no remaining confirmed defect in this slice. Companion export/reconstruction and whitespace checks passed. Clean combined build `/tmp/fux-check-recovery-build.log` passed fux/zor builds, boundary3, ECS28, CLI10, structure8 and realZor1 (141.51 seconds). It then failed on the same two koh gateway netmon `Operation not permitted` cases: forced_quic_loss_redials_without_reopening_or_repeating_application_input and real_fux_keeps_its_pane_and_applies_input_once_across_five_quic_losses; eight gateway tests passed, subsequent gateway integration was not reached. A later documentation-only TASKS.md recovery cross-reference was exported and byte-reconstruction verified separately. All run handles are terminal. This does not establish a herdr comparison or complete the broader goal.


### First real herdr prompt-boundary comparison

- Built the clean herdr0.8.2 reference in a disposable git archive using checksum-verified Zig0.15.2 and locked Cargo dependencies. Installedherdr0.8.0 was excluded. The first build's generated-helper path failed with mixed /tmp/private/tmp paths; canonical paths/private caches succeeded. All2452 tracked build-copy files were then verified against reference regular bytes/symlink targets, and tested binary SHA256 is pinned alongside source/archive/manifest hashes. Reference source remains unchanged.
- Added tools/comparisons/prompt_boundary.py and docs/comparisons/prompt-boundary.md plus raw runtime/build JSON evidence. Final21 cases (three repetitions each) passed with zero own-server exits and confirmed worker PID disappearance. The same compiled synthetic worker runs under both real multiplexers; external explicit reports use each product's own contract. This is not Claude detection/real-agent coverage or a latency benchmark.
- Both stacks rejected the old evidence tested: herdr timed out on preexisting global idle, zor timed out on a new silent prompt despite retaining a completed previous prompt's report. Herdr also rejected already-blocked prompts before input (3/3); there is no false stale-blocked-success claim. Fresh idle without a working transition timed out3/3 in herdr; zor's prompt-scoped response resolved3/3 without working evidence. Herdr's working-to-idle positive control succeeded3/3; zor uses the same response-report path in that case. All zor task outcomes stayed Open.
- Independent review required clearer stale-evidence/provenance claims. Strengthened zor's fixture with an actual retained previous response and verified the clean built source separately from the harness's supplied binary hash/referenceHEAD. Final harness review found no confirmed defect; final documentation review follows. This establishes a narrowly scoped integration-contract benefit, not universal superiority. The broader seven comparison categories and implementation goal remain open.

- Final independent documentation/evidence review passed with no confirmed findings. Reviewer independently validated all21 JSON cases, harness/runtime/build binary/archive/log hashes, clean reference status, and all2452 tracked build-copy bytes/symlink targets. Repeated idle preserved herdr state_change_seq1 while working-to-idle advanced1→2→3, supporting the activity-gate explanation. Python compilation and whitespace checks passed. No production or companion-patch code changed in this comparison slice, so production gates were not rerun; the previous koh permission blocker is not a result of this harness. All build/test handles are terminal. Full goal remains active.


### Real lost-request/reply retry comparison

- Added tools/comparisons/input_retry.py, importing the unchanged synthetic worker/RPC helper from the prior comparison, and docs/comparisons/input-retry.md/JSON. Same verified reference0.8.2 binary; artifact hashes match prior clean-build provenance. No product implementation or companion patch changed.
- Real private forwarding sockets drop the first submission either before upstream forwarding or after upstream reply plus worker-consumption evidence. The caller observes EOF/transport failure, then retries on a new connection. Herdr retries byte-identical agent.prompt with the same request ID; zor uses a fresh CLI with the same durable operation ID and state directory. Proxy traces retain request hashes/IDs and fux operation IDs.
- Final12 cases (three repetitions per product/fault) passed. Before-forward: zero input before retry and one afterward in both. After-consumption: one before retry, two afterward in herdr and one in zor. Zor's after-consumption retry uses receipt lookup without another input-submit; before-forward retries its same reserved input operation. Wait remains Pending and task remains Open. These correlation-ID vs durable-operation contracts are not a shared idempotence promise.
- Independent review found no confirmed defect and recommended final input verification after cleanup. Implemented that assertion and reran all12 cases successfully; each owned server exited zero and worker PID disappearance was confirmed. Cases do not cover expiry/eviction, killed mid-write callers, multiplexer/service restart, human input, remote transport, real agents or all disconnect boundaries; no durable exactly-once application guarantee is claimed. Final documentation/evidence review follows.

- Final independent review passed with no confirmed finding. Reviewer validated all12 rows/tablecounts, final input and process-cleanup assertions, identical herdr request hashes/IDs, zor submission counts and retained operation IDs, Pending waits, harness/helper hashes and clean-build binary/reference agreement. Python compilation, whitespace and reference clean-status checks passed. All comparison handles are terminal. Production gates were not rerun for this harness/documentation-only slice. The full objective remains active.


### OpenCode default startup observation

- Downloaded the official OpenCode v1.18.29 arm64 macOS release into a disposable path, verified its size and SHA-256, and recorded release/binary provenance. No credentials or input were supplied. A six-second capture reached its 80×23 home input screen; a second stayed blank (diagnostic retained, zero fux exit); a 20-second capture reached the same layout with another placeholder. The capture tool now accepts a bounded `--duration` of 1–30 seconds, defaulting to six, and records the successful observation window.
- Added a zor-owned bundled startup-input rule. Conjoined patterns at fixed line offsets cover the normalized layout, including Build/Big Pickle, connection tip, border, logo, shortcuts and version. Only a bounded cwd label without controls and three tagged upstream placeholder literals vary. Two placeholders have real captures; the third remains source-backed. Idle denotes input readiness only. Blank, altered, quoted, stale and malformed matcher strings remain unknown. The negative mutations do not establish real authenticated coverage or raw OSC handling.
- Live default `zor watch --once` identified OpenCode as idle with `opencode-1.18.29-startup-input`, input sequence zero and no problem; fux exited zero. The live observation and blank diagnostic are retained alongside successful captures and release JSON. No fux API/state or koh transport changes were made.
- The first full suite exposed an existing SIGUSR1 fixture's fixed 100 ms startup assumption. The wrapper terminated by SIGUSR1 before handler installation, with empty stderr. The test now uses bounded nonblocking reads to await forwarded child output, then signals its own wrapper, bounds exit and cleans up errors under private HOME/XDG. Production signal policy is unchanged.
- Corrected full zor verification passed: library 69, bundled 3, CLI 2, OSC 6 and passthrough 9; strict clippy, Rust 1.91.0 all-target and protocol-only checks also passed. Capture-tool tests cover nine cleanup/duration cases plus invalid duration values. Final independent review found no confirmed defect and independently passed the matcher, capture-tool and corrected signal tests. Clean combined reconstruction remains pending.

- The additional live capture exercised the third placeholder and matched the idle rule with input sequence zero and no problem; fux exited zero. Added that capture and live observation, updated the fixture test to require three positive screens, and removed the source-only coverage limitation. This closes placeholder coverage for the exact recorded home layout, not other OpenCode states.

- Final third-placeholder fixture test, strict clippy, companion export/byte reconstruction and whitespace checks passed; independent review accepted the additional capture and live observation. The clean combined run at `/tmp/fux-opencode-build.log` passed fux/zor builds, boundary 3, ECS 28, CLI 10, structure 8 and real-zor integration 1 (152.02 seconds). It then failed at the same two koh gateway netmon permission errors; eight gateway tests passed and subsequent gateway integration was not reached. The later third-placeholder artifact/test addition was verified with its targeted matcher test and fresh byte reconstruction; production rules were unchanged from the combined build. All process handles are terminal. Working, approval, response, resize and broader agent integration coverage remain open, as does the full objective.

### OpenCode message and tool-continuation evidence

- Added a zor-owned, repeatable event probe using the real OpenCode 1.18.29 TUI in a real fux pane, private HOME/XDG/configuration, and a deterministic loopback provider. Two fixed prompts use distinct generic input receipts; both reach delivered with complete byte counts and input sequences 1 and 2. This is application integration evidence with a synthetic provider, not an authenticated model run or a working zor report adapter.
- The recorded 46 hook events identify two distinct consumed user messages in one session. Both hooks omit `input.messageID` and provide `output.message.id`. Assistant updates carry the corresponding `parentID`. The second prompt produces two distinct completed assistant messages with `finish: stop`: the first owns a completed read of the disposable fixture, and the later message supplies the final text. This disproves using completed time plus stop alone as a universal final-response condition. Session idle remains insufficient without prompt ancestry and continuation handling.
- Initial direct/headless exploration passed; the first TUI attempt timed out because its project config was outside the pane's startup cwd. Explicit config and cwd fixed the harness. An initial deny-all tool run recorded an unavailable-tool error; it was not published as successful read coverage. The final fixture allows only read and requires its actual output. Source inspection additionally established that `chat.message` runs before saving the user message and async event hooks are not awaited by the dispatcher.
- Independent review caught provider header parsing before socket timeout installation and insufficient proof of actual final text/read output. Fixed the timeout boundary and strengthened validation, then reran the real probe successfully. A second pass found missing tool-name/path/session validation; fixed it, added negative cases, and reran the real capture with the final harness. Eight adversarial validator tests pass for old-parent substitution, missing continuation/text, unrelated text, failed reads, wrong tool/path/session and intervening/partial input. The final fixture is `zor/tests/fixtures/agents/opencode-1.18.29/events.json`, with binary/harness/plugin hashes and zero fux exit/provider shutdown recorded. Final review and companion reconstruction follow.
- No fux or koh production changes were needed. Zor-owned managed-launch/input/message binding, fast-hook reconciliation, ordered reporting, producer lifetime/restart, permission/child/error paths and broader real-agent coverage remain required. This slice informs that implementation rather than claiming to complete it.

- Final independent review passed with no remaining confirmed finding. The reviewer independently ran all eight tests and checked harness/plugin/release hashes, recomputed outcomes and cleanup evidence. Python compilation, root/zor whitespace checks, companion export and clean byte reconstruction passed. All probe and reconstruction handles are terminal. Production build/integration gates were not rerun for this harness/fixture/documentation-only change; the previously recorded two koh netmon permission failures remain unresolved. The full objective remains active.

### Durable native-message binding in zor

- Added `task bind-report` and its structured service operation. A caller names the exact prompt/token, retained input operation, producer sequence and native session/message. New binding requires an unresolved possibly submitted prompt on an open managed task, accepted receipt status, matching live server/workspace/pane/PID and current input sequence, and an unexpired prompt deadline. Receipt status and binding are committed atomically. No reserve or submit operation is invoked, including when reconciling durable Submitting/Uncertain state after a lost reply.
- The journal retains immutable native ancestry per prompt. A native message cannot bind multiple retained prompts in the same managed/native session, including under a restarted producer. A producer stays within one managed/native session and advances a shared retained sequence across consumption bindings and responses. Bound reports must carry the exact native ancestry, producer and input operation. Unbound reports cannot claim native ancestry, and bound producers cannot mix unbound claims into that lifetime. IDs remain caller-attributed, not authenticated registration. Explicit forgetting ends retained-history protection.
- Exact binding/report retries return the original record without refreshing time or consulting fux, even after later prompts, release, expiry or process exit. New binding refuses adopted panes, unsent/failed/missing receipts, stale live identity, human input, resolved/cancelled prompts and deadlines. Human input can still race publication; the existing independent wait input-sequence check remains required. Native ancestry does not establish final response or verified task success.
- Added `tests/verify/zor_bindings.py` to required real-zor integration. It proves status-only recovery at a synthetic durable Submitting crash point with real fux delivery, no extra input, repeated prompt text with distinct ancestry, old-message rejection across producer restart, sequence/scope conflicts, immutable offline/closed retries, malformed retained state rejection, human input before/after binding, live PID/incarnation/stream mismatch, deadline and adopted rejection. Initial runs reached cleanup but the fixture expected only one of the two documented pending-stop error forms; corrected the bounded same-launch retry and passed the full fixture. Added a strict structured-message schema test.
- Initial and final independent reviews found no confirmed production issue; the reviewer independently passed the real-fux fixture and schema test. Full zor tests passed (library70, bundled3, CLI2, OSC6, passthrough9). Strict clippy found four style issues, corrected without semantic changes. Toolchain-floor, final lint/review and clean combined reconstruction results follow.
- Automatic producer registration, operation/token delivery and arming before input, OpenCode event reduction/report queuing, restart/loss/permission/child-session handling and broad agent integration remain required. No fux or koh production changes were needed for the binding API.

- Final strict clippy, Rust1.91.0 all-target and protocol-only checks, formatting, Python compilation and root/zor whitespace checks passed. Independent review accepted the final lint-only changes with no new finding. Companion export and clean reconstruction succeeded. The combined run at `/tmp/fux-native-binding-build.log` passed fux/zor builds, boundary3, ECS28, CLI10, structure8 and required realZor1 (149.69 seconds, including the new binding fixture). It then passed eight koh gateway tests and failed the same two netmon-monitor EPERM cases; later gateway integration was not reached. All handles are terminal. The full goal remains active.

### Automatic OpenCode registration, prompt arming and reports in zor

- Added optional `task start --integration opencode` and structured Start integration selection. A private content-addressed zor plugin runs in OpenCode's runtime; a transparent exec step preserves inline JSON properties/plugins and inherited config paths without adding a wrapper PTY or editing personal configuration. Fux receives only ordinary argv/env and owns the real application PTY. No fux or koh production behavior changed in this slice.
- The plugin registers a random producer lifetime against the managed launch marker and pinned live pane through a private same-user socket. Registration is asynchronous; configured submission refuses until registration succeeds. Zor journals the exact prompt/producer/input-operation arm before bounded delivery, requires a matching acknowledgement before Submitting or input, and retries only the identical unconsumed arm. Native chat hooks snapshot that arm before async lookup, reject child sessions or transformed input, consume it once, and bind exact native session/user-message ancestry to the retained receipt. Producer replacement and consumed-arm replay are rejected.
- Bounded ordered reports associate root permission requests with known native assistant ancestry. Response evidence requires idle plus the latest matching completed stop message and a fresh bounded native-message read with nonempty text and no tool/subtask/error/summary. This catches tool parts observed before their message update and text removed after earlier events. Neither PTY delivery, hook invocation, stop, idle, nor a report establishes verified task success.
- The initial independent review identified an idle/read liveness race: an idle for a newer assistant could be discarded while an older native read was pending. Added bounded rechecking and a deterministic delayed-read regression. The reviewer independently passed the updated Node adapter tests and final real-fux binding fixture, including wrong/dropped acknowledgement faults, no input before exact acknowledgement, producer lifetime checks and retained-state validation. No confirmed finding remains in the reviewed slice.
- A fresh real OpenCode1.18.29 run through fux and real zor prepare/submit/wait passed three prompts: plain response, read-tool continuation with intermediate completed stop, and an unanswered root shell permission. Outcomes are ResponseObserved/ResponseObserved/NeedsInput with delivered input sequences1/2/3, distinct native user messages and matching producer sequences1–6. The task remains Open, fux exits0, and the provider thread stops. Published `zor/tests/fixtures/agents/opencode-1.18.29/integration.json` matches final harness, plugin and local binaries by SHA-256. It uses a deterministic loopback provider, not an authenticated model or passive detection fixture.
- Strengthened publication validation cross-checks arm/receipt/report identities against independent native hook events, actual completed read output, final text, root permission ancestry and unanswered permission UI. Nine validator tests reject foreign ancestry, partial delivery, failed read, missing text, answered permission, missing arm acknowledgement and cleanup failure. The Node test covers repeated text, child input, consumed arms, early tool events, removed text, foreign/oversized native reads, delayed-read idle, permission ancestry and stalled sockets. Required combined verification now includes the Node adapter test; standalone fux builds remain independent.
- Documentation describes actual CLI/API setup, strict JSON inline configuration, bounded queues/requests/history, asynchronous registration, SDK error-parser limits and OS timeout caveats. Producer replacement, disarming abandoned unconsumed arms, reconnect/heartbeat policy, child/question/structured-output support, history recycling, plugin/socket garbage collection and broader agent/version coverage remain unfinished. Lost adapter lifetimes or abandoned unconsumed arms currently require an explicit fresh managed launch, with no blind prompt replay. The full objective remains active.

- Final zor suite passed: library70, bundled3, CLI2, OSC6 and passthrough9. Strict clippy, Rust1.91.0 all-target check, protocol-only check, formatting, Python compilation and root/zor whitespace checks passed. After the test build replaced the local executable, a preserved post-test binary (`/tmp/zor-adapter-reviewed-bin-udw9ynk8/zor`) ran the final capture again at `/tmp/zor-opencode-integrated-final/integration.json`; all three outcomes passed. The published57-event trace is byte-identical, and independent final review verified its harness/plugin/preserved-zor/OpenCode hashes and all nine validator tests. Companion export and fresh byte reconstruction passed after publication.
- The clean combined run `/tmp/fux-opencode-adapter-build.log` passed fux/zor builds, the Node adapter test, boundary3, ECS28, CLI10, structure8 and required realZor1 (145.55 seconds). It then passed eight koh gateway tests and failed the same two netmon-monitor EPERM cases; later gateway integration was not reached. Production source was unchanged after that reconstruction began; the final fixture-only replacement was independently reviewed, revalidated and exported afterward. All slice process handles are terminal. Adapter recovery and the other ledger requirements remain open; this does not complete the full goal.

### Retiring abandoned unsent OpenCode arms

- `task abandon` now commits released/cancelled coordination before attempting safe retirement of a configured arm that this zor journal never attempted to submit. `PromptArm.input_started` is persisted with Submitting before any input-submit RPC and never cleared by receipt reconciliation. A later Reserved status therefore cannot make possibly in-flight input eligible. Existing submitted/uncertain byte evidence is retained; no fux input cancellation or PTY control was added.
- Initial retirement verifies the pinned live target and exact Reserved receipt, then journals `disarm_requested` before the adapter RPC. Exact producer/operation/token/input-operation acknowledgement records `disarmed`. Lost/wrong acknowledgements retry the same payload from retained intent without needing the receipt to remain available, while still checking the live target. Completed retries are offline retained reads. Receipt reconciliation after retirement intent returns its frozen unsent proof rather than updating it into an incompatible current state. Release permanently prevents further reserve/submit under that prompt ID.
- The plugin retains bounded retirement tombstones, including for an arm that never arrived. They reject delayed arms, invalidate delayed chat snapshots, preserve another active prompt's arm, and support immutable disarm retries. Retirement does not cancel a fux reservation or remove terminal text. Initial Reserved status is point-in-time evidence; another independent controller can still submit that generic operation. Possibly submitted arms, unavailable initial receipt proof, lost producer/target and broader lifetime recovery remain outside this safe retirement path.
- Independent review found a retained-proof validation gap: a corrupted Reserved retirement receipt could claim nonzero written bytes. Added a zero-byte invariant and corrupt-one-byte negative. The first real OpenCode retirement probe then found JSON object-key-order sensitivity in the adapter's prompt hash: Python's injected arm and Rust's disarm had identical field values but different insertion order. Hashes now use an explicit tuple of all five protocol identity fields; reordered arm/disarm tests pass. A reviewer concern about retirement-token redaction was rejected after rereading the current tail, which already included it.
- Expanded Node tests pass retirement retries, key ordering, absent-arm tombstones, delayed chat callbacks across a new identical-text arm and old-disarm retry isolation. Expanded real-fux binding tests pass wrong/dropped arm and retirement replies, zero input before acknowledgement, retained proof retry after synthetic receipt-identity unavailability, corrupted retirement flags/bytes, Submitting→Reserved monotonic barrier and later prompt input. These are synthetic fault cases with real fux and explicit adapter fixtures.
- Final real OpenCode1.18.29 capture injects the durable pre-submit crash boundary in a private journal, obtains an actual plugin arm acknowledgement without journaling it, invokes real zor abandonment, verifies a late arm is rejected and input remains0, then runs all three native response/permission prompts in the same process. Outcomes remain ResponseObserved/ResponseObserved/NeedsInput, taskOpen, fux0 and provider thread stopped. The injection is labeled; it is not an actual killed caller/OS crash. Published `zor/tests/fixtures/agents/opencode-1.18.29/integration.json` matches `/tmp/zor-opencode-retirement-reviewed/integration.json` and preserved `/tmp/zor-retirement-reviewed-bin-83k7yibh/zor` plus current plugin/harness hashes. All14 evidence validator tests pass, including false retired-input and reactivation mutations. All prompt tokens are redacted.
- Full zor tests pass (library70, bundled3, CLI2, OSC6, passthrough9), along with strict clippy, Rust1.91.0 all-target check, protocol-only check, formatting, Python compilation and root/zor whitespace checks. Documentation and companion patch export are updated. Final independent review and clean combined gate results follow. No fux or koh production change was required. The full objective remains active.

- Final independent review passed: reviewer independently ran Node tests, all14 evidence tests and the expanded real-fux binding fixture against the preserved final executable; verified artifact equality, hashes, redaction and limitations. Both confirmed findings are fixed; the stale redaction finding was retracted. Fresh companion byte reconstruction passed. The clean combined run `/tmp/fux-opencode-retirement-build.log` passed fux/zor builds, Node tests, boundary3, ECS28, CLI10, structure8 and required realZor1 (146.91 seconds). It then passed eight koh gateway tests and failed the same two network-monitor EPERM cases; later gateway integration was not reached. All handles are terminal. Event-driven observation/gap recovery, producer-lifetime replacement and the remaining full-goal requirements remain open.


### Event-driven observation and resynchronization in zor

- Continuous `zor watch` and the observation service now subscribe to fux's existing generic event API from each coherent listing's incarnation/cursor. Unfiltered events require consecutive sequences in the same workspace stream. EOF, replay rejection/gap, duplicates, reordering, unknown/malformed events and oversized frames drop continuity and require fresh listing/subscription. Existing connections retain their processed cursor rather than skipping buffered events when another listing is taken. Fux APIs, agent semantics and pane ownership were unchanged.
- Event invalidation wakes a full scan after100ms anchored to the first dirty event; repeated events cannot defer that window. Fallback scans are due three seconds from scan start to discover silent process changes and new workspaces. Service invalidation clears affected states/rules while keeping original evidence ages. Publication includes subscription setup duration and exposes active event-stream count plus cumulative event/setup failures. Incomplete workspace scans and failed subscriptions mark affected observations unknown. CLI one-shot observation remains a single scan without subscriptions.
- Stream admission and read ordering rotate. Limits are64 streams,64KiB frames,64KiB plus8KiB buffered input per stream,256KiB new reads per round and32 parse/read iterations per stream. Socket polling checks stop/reload between bounded rounds with at most100ms waits. Discovery/capture retains its two-second RPC budget; discovery plus subscription setup shares four seconds. Native probes, allocation/parsing and OS scheduling are not hard real-time guarantees. Normal events currently invalidate all caches in the changed workspace, so unchanged siblings can be recaptured; selective refresh and broad scaling measurements remain open.
- Independent review identified a liveness bug: four-second setup could exhaust the three-second fallback interval and cause zero event-drain iterations, starving healthy streams beside a stalled subscription. The wait now performs a bounded nonblocking drain even when overdue. A repeated stalled-subscription regression verifies healthy sequence11 then12 both drain. Six event-client tests also cover cursor identity/order, fragmentation and size rejection, buffered replay after acceptance, EOF and interruption. A mock accepted socket needed explicit blocking mode on macOS; a first path-length hypothesis did not explain the failure. The corrected fixture passes.
- Added required `tests/verify/zor_events.py` with real fux/zor, private HOME/XDG/sockets and a counting/fault proxy. It verifies idle revision reuse, fallback discovery, event refresh, input-sequence changes, injected disconnect/gap and duplicate-cursor recovery, preserved pane/process identity and SIGHUP wakeup. Initial fixture rule used a nonexistent visible_input field; corrected to schema-backed visible_blocker/visible_idle. A4.2-second idle window recorded1 workspace listing and0 captures; event refresh took0.118s locally and0.127s in independent review. These are single-fixture local observations, not a herdr comparison or broad performance claim. Fault frames are explicitly injected by the proxy.
- Full zor tests passed (library76, bundled3, CLI2, OSC6, passthrough9), as did strict clippy, Rust1.91.0 all-target check, protocol-only check, formatting, Python compilation and root/zor whitespace checks. Final independent review accepted the overdue-drain fix and documentation with no outstanding confirmed finding; the reviewer independently passed all6 event tests and the real-fux fixture. The proxy now also records accept-thread failures instead of allowing a hidden thread exception. Companion patches exported; clean reconstruction/combined results follow. The full goal remains active.

- Fresh companion byte reconstruction passed. The clean combined run `/tmp/fux-zor-event-observation-build.log` passed fux/zor builds, Node adapter tests, boundary3, ECS28, CLI10, structure8 and required realZor1 (151.51 seconds, including the new event fixture). It then passed eight koh gateway tests and failed the same two netmon-monitor EPERM cases; later gateway integration was not reached. The build handle is terminal. Shared service waiters, selective/scalable observation, broader real-agent coverage, orchestration/recovery and the remaining comparison requirements still prevent whole-goal completion.
- Final test cleanup inspection found that an early cleanup exception could skip later owned-process cleanup. The fixture now collects cleanup errors and attempts every action before failing. Review caught an unguarded stderr diagnostic in that sequence; it now also runs under the collector and decodes invalid UTF-8 with replacement. The targeted fixture passed again after the collector change (1listing/0captures over4.2s,0.116s event refresh, loss recovery and same pane). The subsequent diagnostic-only correction passed Python compilation and whitespace checks; it does not change the exercised production behavior or invalidate the completed combined source gate.
- Final independent cleanup review accepted the guarded diagnostic and all-action cleanup ordering with no outstanding finding. All slice process/reviewer handles are terminal. The full objective remains active.


### Comparative worker continuity after orchestration failure

- Added `tools/comparisons/service_failure.py` and source/hash-linked evidence in `docs/comparisons/service-failure.{md,json}`. All nine cases passed: three zor-service SIGKILL/restarts preserved the worker PID, adopted session and delivered receipt; same-operation retry added no input and the next prompt reached that worker. A fixture report supplied after restart resolved the pending wait for the pre-crash response; the task remained Open. This is not automatic adapter recovery or verified completion.
- Both fux-server and herdr-server SIGKILL controls ended the original worker in every repetition. Fault roles are explicit: zor is an external controller; herdr combines orchestration and PTY ownership. Herdr session restoration/resume exists and was neither disabled nor measured. Original worker continuity and session resume are distinct claims.
- Initial harness runs exposed an invalid zor producer identifier and a herdr request ID type mismatch; corrected both. Independent review confirmed the latter and identified cleanup masking/orphan lifetime concerns. The fixture now attempts all cleanup actions, preserves the primary exception, and gives every synthetic worker a 45-second lifetime. A setup-time bound excludes that alarm from the crash-observation interval. No raw worker PID is signaled.
- Three repetitions passed with the final harness; Python compilation and whitespace checks passed. The reference binary SHA matches the separately verified clean herdr build. No fux, zor, koh or reference production source changed. Final independent review of source, documentation and recorded evidence is pending. Existing combined-gate koh netmon permission failures remain; no new full combined-build claim is made for this comparison-only change. The full objective remains active.

- Final independent review found no remaining confirmed issue, verified all published hashes and raw-evidence equality, and independently reran all three fault scopes successfully (`/tmp/service-failure-independent-final.json`, exit 0). The documentation now explicitly excludes PTY-owner persistence conclusions while retaining the measured zor-journal recovery claim. All comparison process/reviewer handles are terminal.


### Root OpenCode question blockers in zor

- Added prompt-correlated `question.asked` handling to the zor-owned adapter. It uses the same bound native root-session and known assistant-message evidence as permissions and retains one immutable claim per prompt. Child sessions, unknown/old assistant messages, missing tool ancestry and duplicate events do not create reports. No fux or koh production change was required.
- Parameterized the real OpenCode integration harness with `--blocker question`. The new `zor/tests/fixtures/agents/opencode-1.18.29/question.json` records automatic registration, unsent arm retirement, plain response, tool continuation and a final unanswered root question in one real OpenCode1.18.29 TUI with a local synthetic provider. The final wait is NeedsInput; the task stays Open. Native message/session/call identity, exact question/options, running tool state, visible UI, receipt bytes/input sequence and process cleanup are required. The pinned production binary is preserved at `/tmp/zor-question-reviewed-bin-gs2be52m/zor`; raw final evidence is `/tmp/zor-opencode-question-final/integration.json`.
- A repeated real capture showed that owned cleanup can reject a pending question and emit a tool error. The harness now records a pre-cleanup event boundary and retains later shutdown events. It prohibits answer events throughout the run and rejects pre-cleanup rejection, session error or ended blocker tools. Independent review found that a running tool followed by an error could pass the validator; fixed it and added that exact adversarial mutation. Twenty-eight evidence tests pass, including retained permission evidence and new question mutations. The combined dependency gate now runs these evidence tests automatically alongside Node adapter tests.
- The real question run passed with the final harness; a separate real permission regression passed with the same production adapter. Node tests, full zor tests on Rust1.91, strict all-target/all-feature clippy, formatting, Python compilation and whitespace checks passed. Protocol-only and final independent review/reconstructed combined checks follow. Answering/resuming questions, child questions, heartbeat expiry, producer recovery and broader agent coverage remain open. This is real local application integration with a synthetic provider, not authenticated model quality or verified task completion. The full objective remains active.

- Final independent review passed with no remaining finding: Node and all28 evidence tests independently passed, raw/published trace equality and harness/plugin/preserved-binary hashes matched, and all four retained prompt tokens were redacted. The final trace has57 pre-cleanup events and no later events in this particular run; earlier diagnostic capture demonstrated shutdown rejection. Rust1.91 protocol-only tests and standalone package verification also passed (104files,1.0MiB unpacked). Reconstructed combined integration remains running.

- Reconstructed combined gate `/tmp/fux-opencode-question-build.log` is terminal: fux/zor builds, Node adapter tests, all28 recorded-evidence tests, boundary3, ECS28, CLI10, structure8 and realZor1 (151.84seconds) passed. The run then passed eight koh gateway tests and failed the same two netmon-monitor EPERM cases; subsequent gateway integration was not reached. Protocol-only strict clippy also passed. The combined command exited1 and is not claimed green. Final source/fixture review is complete; all slice handles are terminal. Full-goal gaps, including heartbeat/producer recovery, remain open.


### Explicit adapter endpoint availability in zor

- Added `task adapter-status TASK` and the equivalent service action. The read-only probe checks the pinned fux server/workspace stream/pane/PID before and after a same-user protocol-v1 hello from the registered producer, with a shared two-second I/O budget. It reports Reachable, Unavailable with a bounded stage/problem, NotConfigured, NotRegistered, Inactive or Changed. It does not send input, arm, replace a producer, refresh prompt claims, mutate the journal, or infer heartbeat health.
- The journal lock is released during I/O and reacquired to compare generations. Any concurrent commit makes the sample Changed, including an unrelated task commit; contention is an explicit operation error. Retained result integration summaries expose historical kind/producer/registration time with availability NotProbed. A reachable endpoint can be blocked on a question or belong to cancelled coordination; agent readiness is separate.
- Expanded required real-fux binding tests cover correct/mismatched producer, protocol mismatch, EOF, missing socket/fux endpoint, timeout, lock-free concurrent real adoption, unchanged journal/input, unregistered/unconfigured/stopping states and unprobed retained results. Structured service dispatch/rejection is covered in the required service fixture. The real OpenCode question capture now includes two Reachable probes, after registration and while NeedsInput, with the same retained producer. New evidence mutations reject wrong producer, changed generation and unavailable probes; all31 evidence tests pass.
- Initial real/fixture runs passed; a stop request can return before pane release, so its fixture accepts the documented retry-required result while checking durable Inactive status. Strict clippy rejected an unreachable panic macro; replaced it with a normal error. Final Rust1.91 full tests/clippy, formatting, Python compilation and repeated binding tests passed. The final real question trace is `/tmp/zor-opencode-availability-final/integration.json`, published as `zor/tests/fixtures/agents/opencode-1.18.29/question.json`, with production binary preserved at `/tmp/zor-availability-final-bin-y8ckdfr_/zor`. Final review, protocol/package and combined reconstruction follow. Continuous heartbeat expiry, producer recovery and the full objective remain open.

- Final independent availability review found no remaining issue, independently passed all31 evidence tests and the expanded real-fux binding fixture, and verified raw/published equality, harness/plugin/preserved-binary hashes and all four token redactions. Both recorded probes are Reachable, including at NeedsInput. Rust1.91 protocol-only tests and strict clippy plus standalone package verification passed (104files,1.0MiB unpacked). Combined real-zor integration remains running.

- The first combined run failed at an existing single-shot CLI inspection in the service fixture, whose subprocess exception hid stderr. A diagnostic-only correction preserved the assertion and exposed future failures; the complete service fixture then passed against the exact reconstructed binaries. Source review confirmed that the recovery lane can contend for the exclusive nonblocking journal lock, and an owned-lock experiment reproduced the exact Busy diagnostic. That establishes a valid contention race, not the cause of the earlier hidden failure. The fixture now retries only exit1 with that exact Busy message under its existing bounded poll budget, immediately failing on other errors. Final independent retry review and the complete affected service fixture passed.
- The second reconstructed combined run `/tmp/fux-adapter-availability-build-final.log` passed fux/zor builds, Node adapter tests, all31 evidence tests, boundary3, ECS28, CLI10, structure8 and realZor1 (154.65seconds). It then passed eight koh gateway tests and failed the same two netmon-monitor EPERM cases; later gateway integration was not reached. That run used the final production source and diagnostic-only test snapshot; the subsequent exact-Busy retry change passed its complete targeted service fixture and separate review. No production source changed after reconstruction. Both combined handles and all targeted/reviewer handles are terminal. Whole-goal requirements, especially heartbeat expiry and producer recovery, remain open; no fully green combined gate is claimed.


### Durable prompt groups in zor

- Added group-create/inspect/list/step/cancel CLI and structured service actions. Up to 32 retained groups contain 1..8 prepared operations on existing managed tasks. Durable admission counts unverified tasks; dependencies require retained verification and cycles across active groups are rejected. Admission and rotation commit before receipt-backed submission. Explicit step calls are required; no automatic group scheduler or worker launch concurrency is claimed. Fux and koh production code did not change for this slice.
- A real three-worker disposable-worktree fixture covers capacity, verified dependencies, concurrent stepping, failed-check repair, service SIGKILL/restart, one-input receipt behavior, cancellation, retained artifacts and owned cleanup. Extra invalid-journal cases cover admission/capacity, record/member limits and cancellation cursor validity. The service retries the same canonical creation plan.
- Independent review found alternate socket routes could bypass target comparison, and group cancellation could strand an unsent adapter arm. Fixed route-independent incarnation/workspace/stream/pane/PID identity for admission and writer exclusion; real cleanup exposed and fixed the same mismatch in closed-target worktree checks. Cancellation now commits first and retires at most one eligible never-started arm per call with a durable fair cursor and visible pending count. Started arms are never retired.
- The expanded binding fixture exposed a cancellation invariant violation when an older prompt had a response but the task remained Open. Fixed cancellation to retain response/exit evidence, cancel unverified coordination and preserve already verified resolved waits. The fixture asserts all three distinctions plus dropped retirement reply, exact retirement retry, no extra input and subsequent independent submission. Both rebuilt fixtures independently passed final review with no remaining confirmed issue. Strict clippy additionally required replacing indexing and a nested conditional; final checks and reconstruction follow.
- GROUPS.md documents manual stepping, original preparation deadlines, retained capacity after uncertainty, cancellation without worker/resource destruction, bounded retirement retries, and absent group compaction. This is scripted-worker protocol evidence, not demonstrated superiority over herdr or completion of the whole objective.

- Final all-feature Rust1.91 tests (76 library, 3 bundled-rule, 2 CLI, 6 OSC, 9 passthrough), strict clippy, protocol-only tests/clippy, docs and standalone package verification passed. Package contains106 files. Exported companion patches reconstruct byte-for-byte. Final small lint/schema-fixture changes passed separate independent review.
- The first combined run `/tmp/fux-zor-groups-build.log` passed fux/zor builds, Node and31 evidence tests, boundary3, ECS28, CLI10 and structure8, then failed the existing service fixture: deduplicating live worktree-use checks by route-independent identity could skip an unavailable retained socket route. Restored route-specific live checks while retaining route-independent matching only for confirmed Closed managed identities. The existing refusal assertion remains intact; independent review accepted the correction. Targeted service/groups and the corrected combined run follow.

- Corrected targeted service and group fixtures passed, as did strict all-feature clippy and separate final review of the live-route correction. The second reconstructed combined run `/tmp/fux-zor-groups-build-final.log` passed fux/zor builds, Node adapter tests, all31 evidence tests, boundary3, ECS28, CLI10, structure8 and the complete realZor1 suite (167.21seconds), including the new groups and expanded binding fixtures. Koh then passed eight gateway tests and failed the same two netmon-monitor EPERM cases; later gateway integration was not reached. The combined command exited1 and is not claimed green. All slice test and reviewer handles are terminal; broad goal requirements remain open.


### Registered producer heartbeats in zor

- Added heartbeat-adapter CLI/service action and a separate heartbeat view in adapter-status and task result. A newer pulse requires the registered producer/launch marker, attached non-stopping managed ownership and a live pinned fux target. Target I/O releases the journal lock; the launch and sequence are rechecked before storage. Exact retries return the original pulse without extending freshness; older sequences fail. Prompt reports, waits, outcomes and verification remain unchanged. No fux or koh production change was needed.
- Heartbeats use one private atomic file of at most1024 bytes per retained managed launch (at most128), instead of rewriting the4MiB task journal every two seconds. Journal generations do not change and cannot cache heartbeat freshness. Same-user ownership/mode, regular single-link files, identity, size and strict schema are checked; corrupt data remains untouched and is reported separately from readable task evidence.
- The bundled OpenCode adapter emits independent sequence pulses every two seconds, skips ticks while one bounded child is outstanding and stops/awaits that child on disposal. The six-second freshness view uses the larger wall/monotonic elapsed age; negative ages and more than one second disagreement are inconclusive. Independent review found that wall-only TTL could revive after partial rollback; fixed with monotonic age and an exact regression. A further test-timing correction derives both mutated clocks from one saved sample. Clock-reset handling is conservative, not a universal machine-boot identifier. Endpoint probes still independently validate fux incarnation.
- Expanded real-fux binding tests pass for actual expiry, nonrefreshing exact retry, stale/foreign identity, partial/full rollback, corrupt/oversized/symlink sidecars, stopped launches, unchanged journal/input and structured service retry/schema. Node tests pass for periodic independent sequence, no overlapping slow children, responsive adapter RPC and no post-disposal pulses. Rust1.91 full tests, strict all-feature clippy, formatting and protocol-only tests pass.
- The final real OpenCode1.18.29 question trace is `/tmp/zor-opencode-heartbeat-reviewed/integration.json`, published as `zor/tests/fixtures/agents/opencode-1.18.29/question.json`, with preserved production binary `/tmp/zor-heartbeat-final-bin-h4lpsn6v/zor`. It requires current heartbeats after registration and a newer pulse received after the unanswered question's NeedsInput report; task remains Open. New adversarial validators reject expiry/age/sequence/registration errors, missing pulses and pre-blocker pulses. This proves producer freshness while blocked, not readiness, authenticated health or task success. Final evidence review and reconstructed checks follow.
- Continuous agent-state precedence, replacement/recovery of producer lifetimes, broader adapter/platform coverage and the full objective remain open. Historical prompt-response evidence does not expire or become verification merely because a producer heartbeat changes.

- Final independent review passed with no remaining confirmed finding. Reviewer independently passed Node tests, all37 evidence tests and the expanded real-fux binding fixture; verified raw/published trace equality, harness/plugin/preserved-binary hashes, four token redactions and a heartbeat strictly newer than NeedsInput. Protocol-only strict clippy, standalone package verification (107files,1.1MiB unpacked), docs and final byte reconstruction also passed.
- Reconstructed combined run `/tmp/fux-zor-heartbeat-build.log` passed fux/zor builds, Node adapter tests, all37 evidence tests, boundary3, ECS28, CLI10, structure8 and complete realZor1 (170.29seconds). Koh then passed eight gateway tests and failed the same two netmon-monitor EPERM cases; later gateway integration was not reached. The combined command exited1 and is not claimed green. All slice handles are terminal. Producer replacement/recovery, agent-state precedence and remaining whole-goal requirements remain open.


### Native current observations and dashboard precedence in zor

- Extended registered producer heartbeats with an optional bound native state claim and generic input sequence. Zor validates the managed launch, producer, prompt/native message and receipt, checks current pinned fux input under a shared two-second budget with the journal lock released, and rechecks retained identity before recording. Exact retries require identical claims and do not refresh age. Timestamp sampling precedes target I/O. The same bounded private sidecars hold state; immutable reports, task outcomes and journal generations remain separate. No fux or koh production change was required.
- The OpenCode adapter tracks Unknown/Working/Blocked/Idle for its current bound root message, independently of one retained prompt outcome. Reply clears a blocker, current native plain-response proof can establish idle, and later message/part changes invalidate idle. Native events and heartbeat receipts remain same-user observations, not authenticated success. Unarmed root input clears current ancestry; later generic terminal input invalidates the old correlation until a new managed prompt. Synthetic continuation tests do not establish human-answer re-correlation or producer recovery.
- Dashboard and dashboard --once merge fresh native claims with passive pane/process identity and input sequence. Configured integration loss, missing claims or input mismatch yields Unknown even if passive rules say Idle. Unconfigured panes retain passive rules. JSON evidence includes provenance, claim, heartbeat age/sequence, correlation and secondary passive state. The six-second producer TTL and five-second dashboard age bound both apply. Raw status/watch/service snapshot remain passive; overview supplies bounded integration records. Retained task/result pulse freshness is not later live input correlation.
- Independent review found three async invalidation problems: late idle message/part mutations, delayed binding restoring older state, and delayed binding cleanup deleting the newer current assistant. Fixed state invalidation, input generation guards and cleanup placement; bounded Node regressions cover all three, including an old held binding completing after a newer reported idle response. Independent full Node review passed with no remaining confirmed source finding.
- Expanded real-fux binding coverage passes native identity/operation rejection, exact-claim retry, unchanged journal, intervening input rejection and a conflicting passive-idle rule: native Blocked wins, then actual producer expiry produces Unknown and attention while passive remains Idle. Rust1.91 all-feature tests (77 library,3 bundled,2 CLI,6 OSC,9 passthrough), strict clippy, formatting, protocol-only tests and all46 integration evidence/mutation checks pass.
- Final real OpenCode1.18.29 question trace is /tmp/zor-opencode-native-state-final/integration.json, published as zor/tests/fixtures/agents/opencode-1.18.29/question.json with matching preserved binary /tmp/zor-native-state-final-bin-rz9_llpg/zor. It requires a current bound Blocked claim at input sequence3, the same fresh correlated dashboard observation, TaskOpen/NeedsInput history and owned service cleanup. Harness/plugin/binary hashes and all four token redactions match. This proves the unanswered native blocker path with a synthetic provider, not model quality or a comparative herdr win. Final export/reconstruction, package gates and published evidence review follow.
- Producer replacement/recovery, native event-gap recovery, broader real-agent state coverage, human-answer re-correlation and the remaining whole-goal requirements remain open.

- Final independent review found no remaining confirmed issue, independently passed all46 evidence tests and the expanded real-fux binding fixture, and verified published/raw equality, harness/plugin/preserved-binary hashes, four token redactions and clean companion byte reconstruction. Protocol-only strict clippy, documentation build and standalone package verification (107files,1.1MiB unpacked) also passed. Combined reconstructed integration remains running.

- Reconstructed combined run /tmp/fux-zor-native-state-build.log passed fux/zor builds, Node adapter tests, all46 evidence tests, boundary3, ECS28, CLI10, structure8 and complete realZor1 (181.15seconds), including native precedence/expiry and existing recovery/group/worktree scenarios. Koh then passed eight gateway tests and failed the same two netmon-monitor EPERM cases; later gateway integration was not reached. The command exited1 and is not claimed green. All slice process and reviewer handles are terminal. The full objective remains active; this slice adds no broad superiority claim.


### Clean producer lifetime recovery in zor

- register-adapter now permits a new producer after it answers on the configured same-user endpoint and the pinned fux target passes checks before and after hello under one two-second I/O budget. Registration releases the journal lock during I/O, then rechecks launch/target/current registration before atomic replacement. Same-producer retry preserves the original registration; retired IDs cannot reactivate. Up to16 retired lifetimes retain registration/retirement times, with an explicit limit and no recycling. All policy remains in zor; fux and koh production source did not change.
- Replacement retains old bindings/reports and their exact historical retries. New old-lifetime bindings/reports/input are fenced. Nonterminal unresolved armed prompts become Uncertain and retain writer exclusion until explicit abandonment; existing terminal waits remain preserved and pending waits recheck retirement around reconciliation. Old pulse files cannot lend freshness/ancestry to the replacement. Retired unsent arms need no RPC to a missing producer and do not acquire invented disarm acknowledgements; group retirement queues exclude them. Registration neither replays terminal input nor replaces worker processes or task outcomes.
- Added required tests/verify/zor_producers.py using the real bundled Node adapter, real zor/fux and a synthetic native SDK inside one fux-owned Node process. It proves two clean reloads with the same PID/target, no extra input, pending and unsent uncertainty, writer exclusion, explicit abandonment, successful later prompts, old report preservation, old event/input/heartbeat rejection, new pulses without old ancestry, rejection of a second live endpoint owner, duplicate-history validation and bounded lifetime retention. Initial and strengthened targeted runs passed; initial independent source review and independent real fixture passed with no confirmed finding.
- Added --reload-adapter to the real OpenCode capture harness. Test-only instrumentation wraps the plugin factory, delegates actual SDK/hooks and disposes/reinstantiates it after the first response. The final OpenCode1.18.29 trace /tmp/zor-opencode-producer-reload-final/integration.json is published as zor/tests/fixtures/agents/opencode-1.18.29/producer-reload.json, with preserved binary /tmp/zor-producer-final-bin-z3njr8vw/zor. It proves unchanged pinned target/hook PID, no input during reload, preserved first report, fresh producer sequences for the subsequent tool response and unanswered question, native Blocked dashboard evidence and TaskOpen. Harness/plugin/binary hashes and four token redactions match. All85 evidence/mutation tests pass, including worker/input/producer/history/instrumentation mutations and the question safety checks under two lifetimes.
- This establishes controlled clean plugin replacement with the real native API and a synthetic local provider. It does not establish upstream automatic hot reload, abrupt stale-socket recovery, pending native-message reconstruction or fux/process restart recovery. The plugin never unlinks an existing endpoint. Those limitations and explicit uncertain-prompt reconciliation are documented in INTEGRATIONS/TASKS/RECOVERY/README. No comparative herdr win is claimed for this slice.
- Rust1.91 all-feature tests (77 library,3 bundled,2 CLI,6 OSC,9 passthrough), strict all-feature clippy, protocol-only tests/clippy, formatting, Python compilation and docs passed. Final package, patch reconstruction, combined integration and published-evidence review follow. The full objective remains active.

- Final independent review found no remaining confirmed source issue, independently passed all85 evidence tests, and verified published/raw trace equality, instrumentation/production/preserved-binary hashes, four redactions, two producers and unchanged target/hook PID/input at reload. Clarified documentation that existing terminal wait outcomes remain preserved. Full Node adapter tests, package verification (108files,1.2MiB unpacked), and final companion byte reconstruction passed. Combined integration is running against the final production snapshot; the later wording-only clarification was re-exported and byte-verified.

- Reconstructed combined run /tmp/fux-zor-producer-recovery-build.log passed fux/zor builds, full Node adapter tests, all85 evidence tests, boundary3, ECS28, CLI10, structure8 and complete realZor1 (184.13seconds), including the new producer reload fixture. Koh then passed eight gateway tests and failed the same two netmon-monitor EPERM cases; later gateway integration was not reached. The command exited1 and is not claimed green. All slice process/reviewer handles are terminal. Abrupt endpoint/process recovery, broader agent coverage and the other whole-goal requirements remain open.


### Automatic group advancement in zor

- Added task group-run/group-pause and structured service actions. Explicit run intent, its generation and bounded failure diagnostic persist on the group. The CLI starts zor's service when absent, checks actual task-state directory and service incarnation before enabling, and rejects cancelled groups. Pause can be recorded directly while the service is absent. Unchanged mode retries do not rewrite the journal. No fux or koh production change was required.
- One separate service scheduler selects at most one eligible operation per tick, then waits one second after completion; selection rotates across runnable groups. Admission and member cursor persist before existing receipt-backed submission. Capacity and dependencies still require independent task verification and original Delivered receipt. Service restart resumes enabled groups, while manual/paused groups stay unchanged. Original prompt deadlines and uncertain-delivery rules remain authoritative. A paused group's already-selected submission may finish.
- Non-Busy submission errors and normal responses without Queued/Delivered receipts pause automatic advancement with a bounded diagnostic. A pause/resume generation prevents stale failures from overriding newer intent. If failure persistence is blocked, the scheduler retains a bounded pending pause in memory and retries its write before another submission. Crash before that write may lose the diagnostic; restart does not replace a retained receipt to bypass ambiguity, but ordinary initial reservation remains possible when none was persisted. Graceful shutdown joins the bounded in-flight scheduler action.
- Added required zor_group_scheduler.py, reusing the full three-worker/worktree/verification workflow with automatic advancement. It covers single-command service startup, another broken group failing without starving valid work, persisted enabled intent across service restart, pause while a dependency becomes ready, resume without group-step, wrong task-state rejection, cancelled-group rejection, original-input counts and owned artifact/worktree cleanup. Manual variant remains required and passes. Initial failure injection used task stop, which cancelled eligibility; fixed to generic owned-pane kill. The next run correctly refused worktree cleanup for the unreconciled killed target; fixed the fixture to record its owned closure without weakening removal guards. Corrected automatic runs and independent full-workflow review passed.
- Independent review found no confirmed source issue. Clarified documentation that recovery can create an initial reservation when none persisted; it never replaces a retained receipt to evade ambiguity. GROUPS/SERVICE-API/RECOVERY/README describe opt-in scheduling, bounds, failure/crash limits, scheduling intent versus live service health, and original verification semantics. This is scripted-worker workflow evidence, not real-agent model quality or a herdr superiority claim.
- Rust1.91 full tests (77 library,3 bundled,2 CLI,6 OSC,9 passthrough), strict all-feature clippy, protocol-only tests/clippy, formatting, Python compilation and all85 native integration evidence tests pass. Final Node/package/docs and clean reconstructed combined checks follow. The full objective remains active.

- Final Node adapter tests, documentation build, standalone package verification (108 files,1.2 MiB unpacked) and companion byte reconstruction passed. The first combined run `/tmp/fux-zor-group-scheduler-build.log` passed adapter/evidence and fux boundary/ECS/CLI/structure checks, then failed the service fixture when `abandon` returned the documented structured `task-busy` response. The fixture now retries only that validated response with unchanged payload/IDs within a bounded window; other errors remain immediate, and Busy cannot satisfy an expected-negative assertion. Independent retry review found no confirmed issue. The complete targeted service fixture passed (`/tmp/fux-zor-service-contention.log`); the corrected reconstructed combined run is pending.

- The next combined run `/tmp/fux-zor-group-scheduler-retry-build.log` passed the service fixture, then failed automatic-group inspection on another valid Busy response. Added bounded identity-checked retries for the group's idempotent structured actions. Review correctly rejected applying those retries blindly to `group-step`: admission/submission can commit before its final inspection returns Busy, and another step can select a different member. The fixture now inspects admission first and resumes only the original prompt operation when already admitted. Independent controlled exercises proved Busy-before-admission retries and Busy-after-admission does not issue another step. Reviewed manual and automatic real-worker fixtures both passed. GROUPS.md now states this error/reconciliation contract explicitly. Final full integration and reconstructed checks are pending.
- Ran the previously unreached standalone koh gateway integration explicitly with required real-fux configuration (`/tmp/fux-zor-group-gateway.log`). Both tests failed during netmon initialization with EPERM, before their network assertions. Verified task-owned endpoints were no longer listening and removed only this run's two disposable directories. This is an environment blocker, not a passed transport or remote-control test.

- Reconstructed run `/tmp/fux-zor-group-scheduler-final-build.log` passed builds, adapter/evidence checks, fux boundary/ECS/CLI/structure and complete realZor (204.40 seconds), then koh passed eight library gateway tests and failed the same two netmon EPERM cases. Its test snapshot precedes the final step-specific fixture correction; production scheduler code is unchanged. A separate current-fixture integration run failed at a heartbeat assertion that hid a non-success response (`KeyError: value`). Improved its diagnostic; a targeted reproduction then exposed exact Busy from dashboard sampling. The bindings fixture now retries only the identical heartbeat claim/sequence on validated Busy and allows Busy dashboard reads to retry under the existing freshness deadline. Other failures retain their response diagnostics. Final reviewed fixture verification remains pending; the combined command is not claimed green.

- Follow-up source audit: `dashboard::overview` currently emits task and unattached-launch rows, but no group rows. Automatic scheduling failures are inspectable through `group-inspect` and are not yet surfaced as group-level dashboard attention. Add that zor-owned view and its recovery/notification behavior in the next bounded slice; do not claim the scheduler completes the dashboard requirement.

- Final reviewed real-fux/real-zor suite passed in 206.44 seconds (`/tmp/fux-zor-group-reviewed-integration.log`), covering all 17 required fixtures with the final service/group/bindings corrections. Independent final reviews found no remaining confirmed issue; manual/automatic targeted fixtures and the final bindings fixture passed. Final companion byte reconstruction and whitespace checks passed. All slice process/reviewer handles are terminal. Koh network verification remains blocked by netmon permissions; the complete objective stays active, with no broad herdr-superiority claim.


### Group attention in zor's dashboard

- The existing zor overview now includes at most 32 group rows, using the same journal snapshot and canonical group view as group-inspect. Rows distinguish scheduling intent (manual, automatic, paused) from coordination state and service health, with member/admission/concurrency counts, journal generation and bounded scheduling diagnostics. Incomplete paused groups and canonical needs-attention states enter the existing attention filter/bell transition handling. No group row has a pane target or task outcome. Fux and koh production source did not change.
- Review identified cancelled groups with unfinished unsent-arm retirement: cancellation is recorded, but a lost retirement reply can still block a subsequent prompt. Group rows now expose the total pending count and up to eight operation IDs and remain attention-worthy until retirement completes. Only already-cancelled groups suggest retrying group-cancel; active groups suggest inspecting retained operations. Cleanly completed/cancelled groups are quiet. Viewing does not perform retirement, submission, cancellation or other recovery.
- Required manual/automatic group fixtures passed dashboard checks for broken-group failure, manual intent, pause, restart, counts, completion, cancellation and read-only viewing. The real bindings fixture passed pending-retirement attention after a dropped reply, no journal mutation or extra retirement RPC while viewing, and attention clearing after explicit retry. Final review found no remaining confirmed semantic issue.
- The overview bound now allows 288 task/launch/group rows, plus up to 128 observation rows in the dashboard. Existing response byte limits still apply. DASHBOARD/GROUPS/SERVICE-API/README describe the API and its limits. Strict clippy initially rejected unchecked JSON access; replaced it with checked typed extraction. Existing dashboard UI fixture then encountered exact journal Busy on its read-only view; the fixture now retries only that command and diagnostic under a deadline. The corrected real-PTY UI fixture passes focus, attention, resize/restoration and service loss. Full Rust tests and strict default-toolchain clippy passed; Rust1.91, packaging and reconstructed combined gates are running.
- This completes neither desktop notification delivery nor the remaining recovery/comparison requirements. The full objective stays active; no new herdr superiority claim is made.

- Final Rust1.91 all-feature tests (77 library,3 bundled,2 CLI,6 OSC,9 passthrough), strict all-feature and protocol-only clippy, protocol-only tests, documentation and standalone package verification (108 files,1.2 MiB unpacked) passed. Independent final review found no remaining actionable issue and independently verified both companion exports. Formatting and whitespace checks pass.
- Reconstructed combined run `/tmp/fux-zor-group-dashboard-combined.log` passed fux/zor builds, Node adapter checks, all85 evidence tests, boundary3, ECS28, CLI10, structure8 and the complete 17-fixture realZor suite in 210.58 seconds. Koh then passed eight gateway library tests and failed the same two netmon EPERM cases; the command exited1, and later gateway integration was not reached. Final companion byte reconstruction passes. All slice process/reviewer handles are terminal. The full goal remains active, with desktop notifications and the other whole-goal requirements still open.


### Opt-in desktop notification delivery in zor

- Added dashboard --notify and optional --notification-command executable. Notify conflicts with --once; a custom executable requires notify. The dashboard, not fux or koh, owns attention policy and delivery lifetime. Payloads contain only an aggregate new-attention count and a dashboard hint, with no task titles, prompts, paths, output or diagnostics. Native backends use a fixed macOS AppleScript plus separate arguments or Linux notify-send argv. Custom commands receive exactly two arguments, inherit the dashboard environment, use null stdio and never undergo shell expansion.
- One process runs at a time, with a two-second deadline checked by the dashboard loop and at least five seconds between attempts. Pending keys remain a subset of the bounded current view, coalescing new transitions while removing resolved/aged entries. Service errors clear pending delivery. Failures are visible without stopping the UI or workers and are not automatically replayed. Same-key attention changes do not notify again until leaving/reentering attention; restarting the dashboard starts a new notification history. Successful process exit means backend handoff, not proof of a visible popup.
- Timed-out or still-running commands are killed through their process group and the direct child reaped. Review prompted cleanup before snapshot-thread join. Custom commands must stay foreground; exited/daemonized descendants are not supervised, and native filesystem stalls or synchronous terminal/focus work are outside strict wall-clock bounds. This is not a general process sandbox. Documented these limits in DASHBOARD.md and updated README usage.
- Real-PTY fixture passes fixed argument shape, aggregate privacy, null-output isolation, unchanged-attention suppression beyond the cooldown, invalid flag combinations, nonzero exit, timeout, reaping, quit during delivery and missing executable, alongside existing focus/resize/restoration/service-loss checks. Disposable notifier executables were used; no real desktop popup was sent. A harmless osascript invocation verified two separate argv values, and osacompile validated the exact notification script without executing it. Actual OS popup rendering remains unverified on both platforms.
- Extracted attention policy and added deterministic stateful regressions for cooldown coalescing, resolving one pending key while adding another, stale/lost pending evidence suppression and later reentry. Both passed independently. Initial and final independent reviews found no remaining issue after cleanup ordering/documentation changes. Rust1.91 all-feature tests (79 library,3 bundled,2 CLI,6 OSC,9 passthrough), strict clippy, protocol-only tests/clippy, docs and standalone package verification (110 files,1.2 MiB unpacked) passed. Final reconstructed combined integration is running. No new herdr superiority claim is made; the full objective remains active.

- The first reconstructed notification run `/tmp/fux-zor-notifications-combined.log` passed builds, adapter/evidence and fux checks, then failed the existing handoff fixture at a valid structured task-busy response. The fixture now retries only that exact handoff request with unchanged durable operation ID/plan under a deadline and validates version/request/service identity. Independent review confirmed prepare_store returns the existing matching prompt without creating an operation or refreshing its deadline. The full targeted workflow passed (`/tmp/fux-zor-notifications-workflow-retry.log`). A corrected combined run is in progress; no notification production defect was established by this failure.

- The corrected intermediate combined run `/tmp/fux-zor-notifications-reviewed-combined.log` passed all17 real-zor fixtures in 220.02 seconds before koh's eight passing/two netmon-EPERM library results. Final lifecycle review then confirmed that try_wait errors (notably ECHILD) cannot establish unreaped PID ownership. Cleanup now signals only on Ok(None); unknown ownership preserves the polling error and does not signal a potentially reused PID/group. Added a real-process regression: explicitly reap a leader outside std::Child, observe ECHILD, and verify its bounded surviving group is not killed. The regression and independent rerun pass. Final-binary real-PTY notification tests, Rust1.91 tests/clippy and package checks pass; a final reconstructed run includes the ownership fix and new regression. The test module follows both platform implementations to preserve Linux lint ordering.

- Final reconstructed run `/tmp/fux-zor-notifications-final-combined.log` passed fux/zor builds, Node adapter checks, all85 integration evidence tests, fux boundary3/ECS28/CLI10/structure8 and all17 real-zor fixtures in 215.63 seconds. Koh then passed eight gateway library tests and failed the same two netmon EPERM cases; later gateway integration was not reached, and the combined command exited1. Final Rust1.91 library count is80 after the ownership regression; strict clippy and final standalone package verification pass. Independent final review found no remaining finding and verified exported patches; final byte reconstruction and whitespace checks pass. All slice process/reviewer handles are terminal. Actual desktop rendering, broader real-agent states/recovery, remaining comparisons and the full goal remain open.


### Finite completion audit and coherent fixture contention

- Added `docs/agent-workflow-completion.md`, preserving prompt requirements with seven finite remaining items and concrete stopping criteria. Corrected stale top-level status for incarnation capture, generic API documentation, event-driven observation, bounded groups/handoffs, frozen results and shared waiters. Optional additions are outside the completion path; required detection, recovery, remote and comparative evidence remains open.
- Consolidated exact CLI/API Busy classification and bounded retries in `tests/verify/zor_contention.py`. Every retry path explicitly permits reads or unchanged retained operations. Unknown commands, transport errors and semantic failures are not silently replayed; Busy cannot satisfy a different expected rejection. Existing freshness loops keep their shorter deadline.
- Fixed generic CLI retry of `group-step` in the concurrent group fixture. Each caller now attempts once; after joining, inspect admission and resume only the existing gamma prompt. Multiple selections of that same admitted-undelivered prompt are valid; retained receipts and exact final input logs prove deduplication. A newly added selection-count assertion was rejected against `group::candidates`/`step_mode` and removed after the first targeted run exposed it.
- Independent reviewer found nested group recovery calls and dashboard/large-result reads reset or exceeded their outer call budget. Threaded one deadline through step inspection/submission/reinspection and capped subprocess calls to remaining time. Final independent review found no remaining confirmed issue; reviewer independently passed all seven controlled policy tests.
- `python3 tests/verify/test_zor_contention.py`: 7 passed. The required integration launcher now includes these policy checks alongside the 17 real scenarios. Python compilation, `cargo fmt --all -- --check`, `git diff --check`, and `python3 tools/dependencies.py verify` passed (both companions byte-identical).
- Targeted current-source batch: service, manual groups, automatic groups, bindings, workflow, producers and dashboard all passed. Log `/tmp/fux-zor-contention-reviewed-targeted.log`; exact tested fixture/binary hashes `/tmp/fux-zor-contention-verification.json`. Initial log `/tmp/fux-zor-contention-targeted.log` retains the rejected new assertion failure. No production changes or dependency patch changes in this slice; no reconstructed build or unchanged netmon-blocked check rerun. Prior applicable production verification is reused.
- R1 is complete. R2–R7 remain as the finite checklist specifies; no whole-objective completion or new comparative advantage is claimed.


### R2: unadopted worktree use and recovery acceptance

- Added `zor/src/tasks/worktree_use.rs`. Extracted the existing retained-session cwd/process-tree/identity guard unchanged, then applied it to a bounded census of the default fux runtime and runtimes referenced by sessions without confirmed managed closure. Discovery uses existing manager/workspace APIs; no fux/koh production change. Bounds: shared three-second inspection deadline, 64 workspaces/runtime, 32 tabs/workspace, 256 census panes, 256 directory entries when checking absent manager. Unavailable, malformed, oversized or changing evidence refuses deletion, including force. Absent runtimes preserve standalone worktree use. Unknown nondefault runtimes, unrelated OS processes and reparented descendants remain outside the documented census.
- Required service fixture now proves refusal before adoption for launch cwd, changed cwd and grandchild cwd, plus missing-manager uncertainty. Checks preserve the owned checkout and add no removal intent. Initial new equality assertion saw the prior symlink-test diagnostic legitimately cleared by preflight; corrected fixture setup to explicitly reconcile before establishing its baseline. No product guard was weakened.
- Added the worktree-created/pane-not-created acceptance case: temporarily unavailable task-owned control endpoint rejects start without losing the ready worktree or recording a launch; restoring availability allows the exact original request. Existing launch fixture proves received creation with lost reply/killed creator reconciles without duplicate panes. Existing standalone worktree fixture proves interrupted removal retains intent and recovers after explicit external completion. These fulfill R2's boundary cases; automatic destructive retry is not required. Fux-restart/resume remains R3.
- Rust 1.91.0 `cargo test --locked`: 80 library, 3 bundled, 2 CLI, 6 OSC, 9 passthrough passed. Strict all-target/all-feature clippy, protocol-only tests, formatting and `cargo package --allow-dirty --locked` passed (111 files, 1.2 MiB). Logs `/tmp/zor-unadopted-{owner-tests,clippy,protocol,package}.log`.
- Ten affected real fixtures passed: final service (`/tmp/fux-zor-unadopted-service-final.log`), standalone worktree (`...-standalone.log`), launch (`...-launch.log`), and workflow/manual groups/automatic groups/checks/sources/changes/bindings (`...-workflows.log`). Existing generic fux/observer checks remain applicable because their source and contracts did not change. Final reconstructed whole-goal gate remains R7; unchanged koh netmon restriction was not rerun.
- Reviewer `review_service_failure` could not execute this slice because its usage limit was reached. Per prompt §9 fallback, performed a separate explicit complete slice review of the new module, extraction against the prior exported source, retained/closed alias behavior, discovery bounds, refusal paths, test setup/cleanup, documentation and companion patch. No remaining confirmed defect found. This is self-review, not independent review; R7 still requires the final complete intended-change review.


### R3 progress: explicit lost ownership and submission boundaries

- Managed `launch-reconcile` now distinguishes an unavailable original endpoint (Uncertain) from a confirmed different live fux incarnation (Lost). The diagnostic/attempt state persists through later outages. Access to the exact original live target can restore Active. Historical target, PID, prompts/receipts, task outcomes and attached launch ownership remain unchanged; Lost is not a fabricated exit record or cleanup permission. No replacement target is adopted or sent input.
- Extended required `zor_recovery.py`: temporary control outage restores the original session; actual owned fux shutdown/restart creates a different incarnation; reconciliation retains Lost through another outage; retry of the original Reserved prompt fails with the same receipt and replacement input sequence zero. Passed `/tmp/fux-zor-restart-recovery.log`.
- Extended `zor_tasks.py`: drop the actual reservation reply before it reaches zor, prove Prepared/no receipt/no input, then safely obtain a different bounded reservation; disconnect before forwarding input-submit, prove zero bytes/unchanged receipt, then continue the existing post-submission lost-reply and killed-caller recovery cases. Passed `/tmp/fux-zor-restart-boundaries.log`. Existing launch recovery fixture passed `/tmp/fux-zor-restart-launch.log`.
- Added `docs/prompt-recovery-boundaries.md`, mapping submission, acknowledgement, receipt, observation and server/producer-loss boundaries to real fixtures or clearly identified synthetic/source evidence. Lost-ACK testing plus acknowledgement-before-submission commit ordering covers the durable boundary; no new instruction-by-instruction crash requirement is imposed. R3 remains open for service-startup reconciliation of all retained managed tasks and explicitly authorized agent-session resume. Native session IDs alone do not establish resumability.
- Rust 1.91 owner tests passed: 80 library, 3 bundled, 2 CLI, 6 OSC, 9 passthrough; strict all-target/all-feature clippy passed. Logs `/tmp/zor-restart-{owner-tests,clippy}.log`. Formatting, diff checks and Python compilation passed; companion export and byte verification passed. Protocol-only implementation is unchanged and prior applicable results are reused. No reconstructed build or unchanged koh runtime blocker rerun for this bounded partial-R3 slice.
- Independent reviewer remains unavailable from the recorded usage-limit failure. Per prompt fallback, performed a separate full-slice review of state transitions, identity-only unpinned probe, preservation of final/cleanup authority and prompt history, retained Lost evidence, test failure injection and owned-process cleanup. Fixed loss being overwritten by a subsequent outage before final tests. No remaining confirmed slice defect found; full-goal review remains R7.


### R3 progress: finite service-startup launch reconciliation

- Added a bounded `recovery::Startup` sweep to the existing coordination lane, alongside recorded-stop recovery. Its first successful journal open snapshots at most 128 Submitting/Uncertain/Attached managed launch IDs; each tick reconciles at most one under the existing launch identity/final-evidence rules. Prepared/Closed launches are excluded. Busy does not advance the cursor; ordinary reconciliation errors do not starve later IDs. Each ID receives one attempt, then the sweep ends. Missing journal is never created. Storage errors retain their normal journal diagnostics; observations cannot be promised when publication itself fails.
- No new service thread, fux API, process creation, input operation or cleanup authority. Explicit stop intents remain the only recovery path that can terminate owned panes. Passive observation of adopted panes remains distinct from managed launch reconciliation. The ordinary caller queue shares the bounded coordination stages, as documented.
- Required `zor_recovery.py` now restarts the service after actual fux replacement and waits for inspection to report Lost without an explicit per-task launch reconciliation. It retains the existing receipt/identity/no-input assertions and observation-only no-journal check. Seven targeted real fixtures passed: recovery, service, manual groups, automatic groups, bindings, producers and dashboard. Log `/tmp/fux-zor-startup-reconciliation.log`.
- Rust 1.91 owner tests passed (80 library, 3 bundled, 2 CLI, 6 OSC, 9 passthrough); strict all-target/all-feature clippy passed. Logs `/tmp/zor-startup-{owner-tests,clippy}.log`. Formatting, Python compilation, diff checks and exported companion byte verification passed. No unchanged koh runtime restriction or reconstructed full gate rerun for this partial-R3 slice.
- Independent reviewer remains unavailable from its recorded usage-limit failure. Per permitted fallback, separately reviewed the complete slice: snapshot/queue bounds, Busy cursor behavior, phase exclusions, revalidation under the journal lock, no-replay calls, existing worker shutdown/queue behavior, failure-test ownership/cleanup and documentation. No remaining confirmed slice finding. Final full-goal review remains R7.
- Updated the boundary inventory and completion checklist. R3 remains open for explicitly authorized agent-session resume with sufficient adapter metadata and real evidence; retained native ancestry alone is not resume proof.


### R3 progress: real OpenCode session-resume contract

- Added `zor/tools/capture_opencode_resume.py` and retained `zor/tests/fixtures/agents/opencode-1.18.29/resume.json`. Runs actual OpenCode 1.18.29 in an owned fux pane with disposable HOME/XDG/cwd, fixed prompts and a bounded loopback synthetic provider. After clean owned fux shutdown/restart, starts OpenCode with the native `--session ID` argument in the same storage namespace. No zor task resume policy is claimed by this capture.
- Real evidence: distinct fux incarnation and agent process, same native session, distinct user message IDs, prior response visible before new input, zero input sequence before each lifetime's fresh prompt, one fully delivered input per lifetime, correlated completed responses, and original conversation included in the resumed provider request. Both fux exits were zero and provider thread stopped. OpenCode executable hash matches retained official release provenance. Reference herdr already plans OpenCode resume through the same native flag (`references/herdr/src/agent_resume.rs`); no comparative win is claimed.
- Initial probe `/tmp/zor-opencode-resume-contract/resume.json` passed. Added explicit independent evidence validation and reran the real capture successfully at `/tmp/zor-opencode-resume-contract-reviewed/resume.json`; its harness SHA matches the published source. Logs use the corresponding `.log` paths. Nine retained evidence/mutation tests in `zor/tools/test_opencode_resume.py` passed; the combined reconstruction gate now invokes that validator. Python compilation, diff checks and companion export/byte verification passed. No Rust production change or unchanged runtime gate rerun in this evidence slice.
- Separately reviewed fixture input/data bounds, source/version provenance, exact session/receipt/history assertions, failure output versus validated success, owned-process and provider cleanup, and validator mutation coverage. Independent reviewer remains unavailable from its recorded usage-limit failure; final whole-change review remains R7.
- The boundary inventory now cites the existing real integration's acknowledged-but-not-journaled arm retirement case, rather than overlooking that stronger evidence. R3 remains open: zor still needs explicit resume authorization, sufficient retained storage/session metadata and the task/attempt transition that consumes this native contract. No assumption that a session ID alone establishes resumability.


### R3 progress: retained attempt evidence prerequisites

- Added explicit historical launch ownership in zor's journal. Source, check, artifact and change records validate against their recorded attempt/session/launch rather than requiring the current task attempt. Managed sessions have one attempt and matching task ownership; historical launch parents must be current launches. Existing bounded storage remains: 128 current launches/sessions, 512 total launches/attempts and the journal byte cap. No fux or koh production changes.
- Historical launch IDs remain inspectable evidence but are refused as stop/reconcile targets and excluded from startup/recorded-stop sweeps. Task policy stays sealed across retained evidence. Review found current requirement summaries could count old checks/artifacts; fixed inspection, result and capture summaries to use current-attempt evidence. Check result summaries now expose attempt IDs. Verification already rejects sources from another attempt.
- Extended the real source fixture with an explicitly model-level second-attempt journal transition; it does not launch another process. Original sources, checks, artifacts and changes remain inspectable, current requirements report missing, old verification is refused without mutation, historical stop/reconcile is refused, and corrupted associations/duplicate managed attempts are rejected. The original journal is restored before the remaining workflow checks.
- Six targeted real fixtures passed: sources, changes, checks, launch, recovery and groups. Log `/tmp/fux-zor-attempt-history-reviewed.log`; source/binary hashes `/tmp/fux-zor-attempt-history-verification.json`. Rust 1.91 owner tests passed (80 library, 3 bundled, 2 CLI, 6 OSC and 9 passthrough); strict all-target/all-feature clippy passed. Logs `/tmp/zor-attempt-history-reviewed-{tests,clippy}.log`. Formatting, Python compilation, whitespace checks, companion export and byte reconstruction passed. No unchanged koh runtime check or full reconstructed gate rerun.
- Independent reviewer remains unavailable from the recorded usage limit. Separate complete-slice review compared all eight changed Rust files against the previous exported companion source, inspected the entire new fixture section, task/group/verification/cleanup call sites and documentation, then verified the exported companion bytes. Confirmed summary/ownership findings were fixed and affected checks rerun. Final whole-change review remains R7.
- R3 remains open. This is retained-history infrastructure, not an authorized resume API or a real same-task resume transition. Native storage/session metadata, durable explicit authorization and actual replacement-process attachment still need implementation and acceptance evidence. Existing group membership pins its attempt; resume must respect that invariant rather than silently replay group input.


### R3 progress: actual registered agent storage metadata

- Managed OpenCode handshake now snapshots the running adapter's HOME and four XDG config/data/state/cache variables. Missing XDG values are explicit nulls; no controller defaults, credentials, inline config or arbitrary environment are recorded. Valid metadata is published with the producer registration after the existing pinned target/marker/endpoint checks. Unknown, oversized, relative/control-character paths and test-home override remain unavailable in the bundled adapter. Rust validates exactly the five permitted fields and the 2048-byte path aggregate; the plugin additionally caps serialized metadata to 3072 bytes within the existing 4096-byte response limit.
- Namespace metadata is preserved through an unchanged producer reload. A changed or previously unavailable namespace stays unavailable for the remainder of that launch: a plugin reload cannot prove an already-open application database moved. This does not block ordinary registration or prompt observation. Existing immutable prompt bindings retain the native session/message identity; neither metadata nor a session ID grants resume authority or proves current database availability.
- Real OpenCode 1.18.29 capture with synthetic provider passed at `/tmp/zor-storage-reviewed-integration/integration.json`, retained as `zor/tests/fixtures/agents/opencode-1.18.29/integration-storage.json`. Same process/target and namespace through a real SDK plugin reload, original/fresh native message responses, no replay during reload, and an unanswered question blocker. The fixture records its exact executed binary hashes and matches the capture harness hash. The owner test command subsequently regenerated the debug binary; retained evidence is attributed to its recorded binary, not a later path hash.
- Bundled Node adapter tests passed (`/tmp/zor-storage-adapter.log`), including absent XDG and snapshot stability. Real-fux producer fixture passed (`/tmp/zor-storage-reviewed-producers.log`), including changed-HOME reload disabling metadata while new prompts still complete. All 88 retained integration evidence/mutation tests passed (`/tmp/zor-storage-evidence-tests.log`), including absent/foreign/changed storage metadata. Rust 1.91 owner tests passed: 82 library, 3 bundled, 2 CLI, 6 OSC, 9 passthrough; strict clippy passed (`/tmp/zor-storage-reviewed-{tests,clippy}.log`). Fixed a new test's denied indexing lint and an incorrect new mutation-test fixture key before these final passes.
- Separate complete-slice review inspected production/schema/handshake bounds and registration race checks, generated metadata, all fixture changes and docs against the previous exported companion source. It added monotonic invalidation when a producer's namespace changes and the corresponding real-process assertion. Existing independent reviewer remains unavailable from the recorded usage limit; no independent review claimed. Formatting, Python compilation, whitespace checks and companion export/byte verification passed. Source hashes: `/tmp/zor-storage-verification.json`. No unchanged koh blocker or full reconstructed gate rerun.
- R3 remains open for the explicit resume authorization and durable replacement-process/same-task attempt transition. The new metadata is a required prerequisite, not a replacement for those acceptance criteria. Fux and koh production sources are unchanged in this slice.


### R3 complete: explicit same-task OpenCode resume

- Added `zor task resume TASK --operation OP --instance INSTANCE` and the structured `resume` service action. Zor persists a stable operation, original attempt, native session and storage environment before creation. Eligibility requires an open managed task, reconciled Lost/Finished attempt, proven absent original root PID when retained, native binding from the registered OpenCode producer and available namespace. Existing group membership, unresolved checks, conflicting session flags and option terminators refuse recreation. Runtime/workspace/cwd/argv and task policies are retained; no arbitrary replacement command or prompt replay is authorized.
- Reused the managed launch submission/reconciliation path. Prepared intent is retried only by explicit command; Submitting/Uncertain intent performs identity/final-evidence reconciliation without sending another creation. Attachment atomically archives the original launch and advances the same task to a new session/attempt, preserving policies and prior records. Completed operation retries create nothing. Startup's finite sweep includes submitted resume candidates; it never submits Prepared intent. Pending registration receives the existing bounded not-ready retry instead of a wrong-marker failure. Journal bounds reserve session/attempt slots for pending launches.
- Review found current verification still required historical prompts to be released, preventing verification after resume when an old reservation remained. Scoped this invariant to the current attempt. The final real fixture now uses an owned worktree, collects source, runs a declared source-bound check and explicitly verifies the resumed attempt with the old reservation unchanged. Lost historical ownership still provides neither final exit evidence nor automatic worktree cleanup authority.
- New `zor/tools/capture_zor_resume.py` uses actual OpenCode 1.18.29 and owned fux restart with a synthetic provider. The final trace `/tmp/zor-explicit-resume-final-reviewed/resume.json` is retained as `zor/tests/fixtures/agents/opencode-1.18.29/zor-resume.json`. It proves distinct fux/process/session/attempt identities, same task/native session/conversation, zero initial input in the replacement pane, no replay of an old reservation, fresh correlated response and current-attempt verification. A proxy forwards creation then drops its response: zor records uncertainty and retry attaches the one created pane. Refusals cover live state, conflicting operation instance, missing metadata, actual cancelled task and option terminator. Both fux lifetimes exit zero; provider/proxy threads stop; tokens and disposable paths are redacted.
- Nine retained evidence/mutation tests pass (`/tmp/zor-resume-final-evidence.log`) and are mandatory in the combined reconstruction gate. The captured executable hashes and current harness/plugin provenance are retained; the later owner test build can regenerate the debug binary path, so capture claims use the recorded hashes. Initial direct cancellation-state mutation failed journal validation because it left active prompt coordination; fixed the fixture to use actual task cancellation before checking refusal. No product assertion was weakened.
- Real targeted launch/source/recovery/producer/group fixtures passed (`/tmp/zor-resume-targeted.log`). After the verification invariant fix, sources and the complete two-worker workflow passed again (`/tmp/zor-resume-final-workflows.log`). Final Rust 1.91 tests passed: 82 library, 3 bundled, 2 CLI, 6 OSC, 9 passthrough; strict clippy and standalone package verification passed (`/tmp/zor-resume-reviewed-{tests,clippy,package}.log`, 119 packaged files, 1.5 MiB). Protocol-only tests passed (`/tmp/zor-resume-protocol.log`). Formatting, Python compilation, whitespace checks and companion export/byte reconstruction passed. No full reconstructed gate or unchanged koh netmon blocker was rerun; final whole-source gate remains R7.
- Separate complete-slice review compared changed launch/model/integration/recovery code to the prior companion export; inspected the complete new resume module, CLI/API wiring, register races, histories, verification/cancellation/group/cleanup call sites, capture/mutation fixtures and docs. Fixed the verification history defect, reserved record capacity, pending registration handling and session option-terminator issue, then reran affected checks and reviewed the resulting slice. Independent reviewer remains unavailable from the previously recorded usage limit; no independent review is claimed. Source/evidence hashes: `/tmp/zor-explicit-resume-verification.json`.
- R3 is complete within the documented OpenCode resume policy. Other adapters remain explicitly unavailable for native recreation; broader Claude/Codex detection evidence is R4. Remaining comparison measurements, remote runtime acceptance and final whole-change review/gates remain R5–R7. This is not a new comparative superiority claim. Fux and koh production code are unchanged in this slice.


### R4 progress: herdr inventory, real resize evidence and generic final-record fix

- Audited all 21 built-in herdr detection manifests with source hashes, versions, rule IDs, declared states/regions and aliases. `tools/comparisons/detection_inventory.py` generates `docs/comparisons/detection-inventory.{md,json}` and records PATH/explicit-binary availability. Found Claude, Codex, downloaded OpenCode and Pi; the other searched names were absent from PATH. This is source/inventory evidence, not measured detection accuracy, authentication availability or an installation blocker. No reference source was changed.
- Captured installed Pi 0.80.10 with existing no-input startup harness. Retained `zor/tests/fixtures/agents/pi-0.80.10/startup.json` shows no configured models and startup/update text. No working state or new Pi rule is inferred. Corrected the fixture index's stale OpenCode integration gaps while preserving the still-missing passive/real-agent coverage.
- Added bounded no-input lifecycle capture for initial Claude 2.1.263, Codex 0.153.4 and OpenCode 1.18.29. Each real process renders 80→39→80-column viewports; an owned temporary split drives resize. The final explicit pane kill retains output with unknown exit status and owned fux shutdown exits zero. Published versioned `lifecycle.json` files have exact agent/fux/harness hashes and 15 captures per stage. Logs `/tmp/zor-{claude,codex,opencode}-lifecycle-final.log`. These are real resize and forced-release observations, not natural exit, authenticated working or task success. Four lifecycle evidence/mutation tests pass and are mandatory in the combined gate. Four bundled-rule tests pass, including real narrow captures remaining Unknown through the production terminal model.
- Capture exposed a generic fux correctness defect: `close_tab` despawned the ECS tab before its terminating pane, so the later final-record path lost workspace/stream ownership. Reproduced without agents using an owned `/bin/cat`; final evidence was absent. Retain closed tab metadata/internal ID while panes drain, keeping it absent from public membership/selection; release it with the last pane. This is generic multiplexer lifecycle ordering, with no agent policy in fux and no new periodic scan. Existing input/state guards and tab membership checks remain authoritative.
- New deterministic regressions prove final output after last-pane kill with unknown exit status, delayed known exit/output after a non-final tab closes, no terminating-pane input, no closed-tab rename, and eventual internal-ID/entity cleanup. An initial implementation removed the internal tab ID too early; the existing randomized invariant test rejected it. Fixed ID/entity lifetime together without weakening the invariant. Final ECS suite: 30 passed (`/tmp/fux-closed-tab-final-reviewed-ecs.log`).
- Fux Rust 1.95 floor build, 3 boundary tests, 10 CLI tests, all-target/all-feature clippy and package verification passed (`/tmp/fux-closed-tab-{cli-boundary,final-clippy,package}.log`); package has 157 files, 2.2 MiB. Cargo reported a nonfatal registry-cache write restriction, then verified the package successfully. Real generic final-record plus zor managed-launch and recovery fixtures passed (`/tmp/fux-closed-tab-integrations.log`). Zor Rust 1.91 bundled-rule tests and strict clippy passed (`/tmp/zor-lifecycle-{bundled,clippy}.log`); unchanged production owner results remain applicable. Lifecycle mutation tests passed (`/tmp/zor-lifecycle-evidence.log`).
- Separate complete-slice review covered tab membership/internal identity, pane drain/final/workspace retirement ordering, creation rollback interactions, control input/capture/tab routing, deterministic regressions, capture bounds and owned cleanup, evidence provenance, manifest inventory and documentation. Avoided adding a per-tick cleanup scan; cleanup runs on pane release. No remaining confirmed slice defect. Independent reviewer remains unavailable from its recorded usage limit; final whole-change review stays R7. Formatting, Python compilation, whitespace checks and companion export/byte verification passed. Source hashes: `/tmp/fux-zor-lifecycle-verification.json`. No unchanged koh runtime blocker or full reconstructed gate was rerun.
- R4 remains open: required working/blocker/idle/natural-exit/nested-tool evidence and broader passive coverage are not established by these no-input traces. R5 comparative accuracy/workflow/usability/resource measurements, R6 remote runtime acceptance and R7 final full-change review/gates remain open. No superiority claim is made from the inventory or rule counts.

### R4 progress: authenticated Codex working, directory trust and natural exit

- User explicitly authorized the Codex account and exported API keys for testing. Used only a copy of the existing Codex login cache in a fresh private HOME/XDG/CODEX_HOME and disposable work directory; no exported key, personal configuration or history was inherited. Used CLI 0.153.4 and the user's configured `gpt-6-astra` model with read-only sandboxing, approval never and one exact-response/no-tool prompt per run. Auth copying follows the official login-cache contract at https://learn.chatgpt.com/docs/auth. No authentication material is retained in the versioned evidence; the harness checks original/refreshed credential strings before publication and removes the temporary home.
- Retained `zor/tests/fixtures/agents/codex-0.153.4/authenticated.json` from `/tmp/zor-codex-authenticated-reviewed/authenticated.json`, with exact binary/fux/harness hashes, ordered viewport captures, generic input submissions and final exit evidence. It demonstrates directory-trust input, Working, an actual response and native `/quit` exit status 0. Built-in Codex app MCP startup is visible, not claimed tool execution. This is real authenticated model output, not the synthetic OpenCode provider. It does not establish native prompt binding, task verification, tool approvals or broad idle recognition.
- Initial probe stopped at the trust screen; subsequent bounded probes established the response. A combined `/quit\r` write was consumed as pasted text and did not exit: those failed runs remain diagnostics, not successful evidence. Separately typing `/quit` then Enter produced native exit 0. The final harness validates three inputs and a matching final pane/command/exit/input sequence before publishing. No user account usage-limit reset was performed.
- Added only zor rules for the recorded directory-trust screen and narrow visible Working layout. Working requires the observed CLI header/version, composer placeholder and model/footer layout. Unmatched layouts and the response screen remain Unknown. The original sign-in and all other initial-agent rules remain intact. Added production-screen positive/derived negative tests, four offline acceptance/mutation tests and their mandatory combined-gate entry. Regenerated the declared herdr/zor inventory because the zor manifest hash and rule count changed. No fux/koh production changes in this slice.
- Verification: `python3 zor/tools/test_codex_authenticated.py` passed four tests; Rust 1.91 `cargo test --manifest-path zor/Cargo.toml --locked` passed 82 library, 5 bundled-rule, 2 CLI, 6 OSC and 9 passthrough tests (`/tmp/zor-codex-auth-owner-reviewed.log`). Strict all-target/all-feature clippy passed (`/tmp/zor-codex-auth-clippy-reviewed.log`). Initial owner checks caught a stale bundled Codex rule-count assertion and a new test's indexing lint; corrected both and reran the affected owner gates successfully. Formatting, Python compilation and whitespace checks passed.
- Separate full-slice review covered credential isolation/publication, bounded requests and owned cleanup, paste/exit evidence, rule specificity/Unknown behavior, tests, fixture provenance and documentation. No further confirmed slice defect. This was a self-review fallback, not an independent reviewer; final complete-change review remains R7. No unchanged koh runtime blocker or full reconstructed build gate was repeated. R4 still needs the listed tool/idle/nested-agent evidence; R5–R7 remain open.

- Final slice packaging passed: `cargo +1.91.0 package --manifest-path zor/Cargo.toml --allow-dirty --locked`, 128 files / 1.8 MiB (`/tmp/zor-codex-auth-package.log`). Companion export and byte reconstruction both passed for zor and koh. Source/fixture hashes: `/tmp/zor-codex-auth-verification.json`. All capture, owner-check and reconstruction processes reached terminal results.

### R4/R5 progress: real command approval and paired screen conformance

- Continued from the previous verified Codex response/natural-exit slice. Used the user's authorized Codex cache with fresh private HOME/XDG/CODEX_HOME, CLI 0.153.4 and configured `gpt-6-astra` to request approval for harmless `/usr/bin/true`. Approval policy was on-request with read-only sandboxing. The harness accepts only disposable-directory trust and never answers command approval. A first bounded probe remained at trust after early input; added a pre-input settling interval in the new harness, without blind replay. Final `zor/tests/fixtures/agents/codex-0.153.4/approval.json` comes from `/tmp/zor-codex-approval-final/approval.json` and pins harness/agent/fux hashes. The dialog appears below “Running /usr/bin/true”; no tool result or execution is inferred from that label. Explicit fux release retains unknown exit status and exactly one input sequence. No account usage-limit reset was performed.
- Added a zor-only command-approval rule requiring the complete local confirmation dialog at the bottom of the recorded 23-row layout, with blocker precedence. Documented unsupported layouts and exact copied-viewport spoofing. Added four approval evidence/mutation tests to the existing mandatory Codex test entry (eight total) and a production-terminal positive/negative rule test. No agent-specific fux or koh changes.
- Added `tools/comparisons/detection_screens.py` and retained `docs/comparisons/detection-screens.{md,json}`. Both actual production file CLI matchers receive the same normalized retained viewport text, supplied agent identity and disposable default HOME/XDG. Nine real screens and six derived negative classes per screen yield 63 paired cases. The sample is explicitly conformance, with zor rules developed from these fixtures; it is not held-out accuracy, universal support or live freshness. OSC/title input, process identification and runtime arbitration are excluded. OpenCode native permission/question screens use a synthetic provider; Codex response/approval use a real model.
- Initial paired measurement found zor missing the existing OpenCode permission/question screens. Added passive rules for their real 40×23 native dialog captures, preserving report precedence and Unknown elsewhere. Final results: zor 8/9 expected real states, zero of six missed blockers, zero non-Unknown results on 54 negatives; herdr 6/9 expected states, three missed blockers, 54 non-Unknown negatives. Herdr's negatives separate into 41 Idle fallbacks without visible evidence, 12 matched visible blockers and one matched visible Working. Herdr already recognized OpenCode permission; Codex post-response input readiness remains Unknown in zor and Idle fallback in herdr. No universal win is inferred. The declared manifest inventory was regenerated after rule changes.
- The report retains case/source/JSON-pointer/text hashes, expected labels, rule IDs, herdr visibility/fallback details, binary/source/harness hashes and explicit limits. The same independently reviewed clean herdr 0.8.2 binary was used; its SHA matches the first paired comparison. Rechecked all 2,452 tracked build-copy files against the current unchanged reference, including symlink targets. Four offline report tests validate accounting/provenance and reject omitted cases, invented scores and conflating fallback with visible evidence; added the mandatory gate entry. They do not stand in for rerunning the actual CLI workload.
- Verification: eight Codex evidence tests passed (`/tmp/zor-codex-approval-evidence.log`); final Rust 1.91 library and bundled-rule checks passed 82 and 7 tests (`/tmp/zor-blockers-owner.log`). Strict all-target/all-feature clippy passed (`/tmp/zor-blockers-clippy.log`). The four paired-report tests passed. Prior unchanged CLI/OSC/passthrough and protocol-only checks remain applicable. Formatting, Python compilation and `git diff --check` passed. The final paired CLI run completed successfully and its original output is `/tmp/detection-screens-final.json`.
- Separate full-slice self-review covered credential publication/cleanup, unanswered approval and exit semantics, rule precedence/specificity, terminal normalization, paired-input fidelity, fixture-label limitations, default manifest provenance, fallback/visible accounting and gate integration. No further confirmed in-scope issue. No independent reviewer was used for this slice; complete-change independent review or the explicit fallback remains R7. No unchanged koh runtime blocker or full reconstructed build gate was rerun. R4 broader agent idle/working/nested-tool states, remaining live R5 scenarios, R6 and R7 remain open.

- Final slice packaging passed (`cargo +1.91.0 package --manifest-path zor/Cargo.toml --allow-dirty --locked`): 130 files / 1.8 MiB, `/tmp/zor-blockers-package.log`. Companion export and byte reconstruction passed for zor and koh. Source/fixture hashes are in `/tmp/zor-blockers-verification.json`. All capture, comparison, owner-check and reconstruction handles reached terminal results.

### R4 progress: authenticated Claude and generic empty argv preservation

- Continued from the verified blocker/comparison slice. The authorized exported Anthropic key was available. Real Claude 2.1.263 was invoked in a disposable HOME/XDG/CLAUDE_CONFIG_DIR/work directory with `--bare --model sonnet --tools ""`; the real footer identifies Sonnet 5. Bare mode avoids personal hooks/config/keychain loading, and the empty tool list disables tools. No user key was supplied in argv or copied into repository files. This is explicitly bare-mode evidence, not default-mode or nested-tool coverage.
- The first attempt exposed a generic launch restriction: fux configured commands rejected every empty argument, although an empty non-executable argument is valid and required by this invocation. Fixed fux configuration and control argv validation to reject an empty executable only; retained argument-count, per-argument/total byte and NUL bounds. Zor's shared launch validation now permits the same positional empty arguments while preserving its executable-name and wrapper bounds. No agent-specific fux state or policy was introduced.
- Added the mandatory real generic `empty_arguments.py` fixture for configured commands, new, split and retained final argv. Actual shells prove argument count, empty first argument and following literal argument; empty executable and NUL requests are rejected. Extended the existing real zor launch/reconciliation fixture to preserve an empty argument through CLI, durable intent, creation, replay and recovery. Initial new-harness assertions used the wrong error spelling and assumed kill returned a value; corrected both. A later stronger output assertion encountered a 19-column split clipping a long line; the fixture now emits short lines and checks every value, retaining the stronger argv assertion. These were fixture fixes, not new product defects.
- Exploratory Claude runs identified its theme, API-key, security and directory-trust dialogs. Directory trust defaults to No/exit; that exploratory run exited and was not retained as response evidence. The final harness explicitly selects only the authorized key and its disposable directory, sends one short no-tool prompt, observes working and response readiness, then types `/exit` and Enter separately. `zor/tests/fixtures/agents/claude-2.1.263/authenticated.json` comes from `/tmp/zor-claude-authenticated-reviewed/authenticated.json`: 14 retained captures, eight inputs, matching final command/pane, native exit status0 and server cleanup0. The entire key-preview line, temporary paths and emails are redacted recursively; publication rejects residual full-key/key-suffix material. Four offline provenance/negative/redaction tests are mandatory in the combined gate and require no credentials.
- Added a zor-only narrow Working rule based on the recorded CLI/model header, empty composer and manual-mode interrupt footer, independent of the random spinner verb. Response/shortcut footer remains Unknown and is not a verified task outcome. Other onboarding dialogs, layouts/models, real command approval and nested tools remain unverified beyond the earlier theme rule. Production terminal positive/negative tests cover the actual 80-column captures.
- Extended the paired screen corpus with Claude working/response and reran both production file CLIs. Final report has 77 cases: 11 real screens plus 66 derived negatives. Zor matches 9/11 expected states, herdr 8/11; missed blockers remain 0/6 vs3/6. Herdr recognizes Claude's post-response idle with a visible prompt-box rule, while zor returns Unknown. Negative herdr results are 45 Idle fallbacks without visible evidence, 12 visible blockers, five visible Working and four visible Idle matches; zor returns Unknown throughout. The report retains this herdr strength and the same conformance/OSC/freshness limitations; no held-out accuracy claim. Regenerated the declared inventory and passed the four offline report checks.
- Verification: fux Rust1.95 library84/ECS30 and ten existing CLI cases passed in `/tmp/fux-empty-argv-owner.log`; the final new CLI case passed separately in `/tmp/fux-empty-argv-integration-reviewed-final.log`. Real augmented zor launch/reconciliation passed (`/tmp/zor-empty-argv-integration.log`). Fux strict all-target/all-feature clippy and three agent-boundary tests passed (`/tmp/fux-empty-argv-{clippy,boundary}.log`). Zor Rust1.91 library83 and bundled-rule8 passed (`/tmp/zor-claude-auth-owner.log`); the final retained capture was retested in `/tmp/zor-claude-auth-bundled-final.log`. Strict clippy passed (`/tmp/zor-claude-auth-clippy.log`). Existing unchanged CLI/OSC/passthrough/protocol-only results remain applicable. Python compilation, formatting and whitespace checks passed. The updated actual paired CLI output is `/tmp/detection-screens-claude.json`.
- Separate full-slice self-review covered argv validation/OS delivery, empty/default/executable distinctions and bounds, final/reconciliation identity, capture onboarding authorization, credential redaction and cleanup, native exit evidence, rule specificity, comparison fairness/accounting and documentation. No further confirmed in-scope defect. Full-change review remains R7; no independent reviewer was used for this slice. No unchanged koh runtime blocker or full reconstructed build gate was rerun. R4 remaining tool/idle/nested states, R5 live/workflow/resource scenarios, R6 and R7 stay open.

- Final post-export packaging passed on both floors: fux Rust1.95 (162 files / 2.3 MiB, `/tmp/fux-empty-argv-package-final.log`) and zor Rust1.91 (133 files / 1.9 MiB, `/tmp/zor-claude-auth-package-final.log`). Fux reported the known nonfatal Cargo registry-cache write restriction, then verified successfully. Companion export and byte reconstruction passed for zor/koh. Source/fixture/patch hashes: `/tmp/claude-empty-argv-verification.json`. All capture, integration, comparison, build, review-check and reconstruction handles reached terminal results.

### R5 §8.6: headless setup, discovery and unavailable-service diagnostics

- Previous turn was verified progress on Claude/argv evidence. Continued with the finite controller-usability comparison. The old temporary herdr reference build was absent; excluded installed herdr0.8.0 and rebuilt reference0.8.2 from clean `git archive` under ignored `target/herdr-reference`. Downloaded official Zig0.15.2 and checked archive SHA before extraction; locked Rust1.95 build passed in 1m53s. Rechecked all2,452 tracked file bytes/symlink targets against the unchanged reference. New build/archive/toolchain/log provenance is retained in `comparisons/controller-setup-build.json`; older comparison binary hashes were not relabeled.
- Added `tools/comparisons/controller_setup.py`, using the unchanged prior synthetic-worker source with an independent60s lifetime bound. Each backend gets fresh HOME/XDG, owned endpoints, a disclosed default-command fixture config and bounded calls/cleanup. No prompt, account or real-agent inference is involved. The harness invokes actual public CLIs for absent-service error, headless startup, initial overview, reuse and zor controller-only shutdown. It checks the actual created herdr pane and zor worker PID/input_sequence0. Server exit0 and final worker absence are required; zor background-service endpoint shutdown is measured without inventing its process exit code.
- Initial probes showed zor returning an explicit initial-scan-pending snapshot and herdr returning an empty agent list. Neither establishes overview success. Tightened collection to require the actual fixture pane and retain every follow-up read; fixed the first loose nonempty-envelope assertion. Added nested cleanup so a zor-shutdown failure cannot skip owned PTY-owner cleanup. No production code changed.
- Three final paired runs passed (`/tmp/controller-setup-validated.json`, retained as `comparisons/controller-setup.json`). Non-polling headless startup invocations: fux+zor2, herdr3. Additional reads at50ms after each CLI return: zor1/1/1, herdr8/5/6; total invocations through first populated overview3/3/3 vs11/8/9. These scheduling-dependent reads are explicitly not extra intrinsic setup steps or a latency/human-effort benchmark. Ordinary interactive herdr startup and prepared fixture configuration are disclosed alternative/prerequisite scopes.
- Both unavailable-service diagnostics exit1 and provide a recovery command. Herdr also returns a structured `server_not_running` CLI error; zor returns plaintext naming `zor serve` and matching `--directory`. Repeated reads reuse the controller. `zor shutdown` preserves fux and its worker; a separate agent-controller-only stop is unsupported in the tested herdr interface, not counted as a failure. The report retains exact commands/stdout/stderr/configuration and this scope distinction.
- Five offline provenance/accounting/mutation tests pass and are mandatory in the combined gate: actual populated-overview acceptance, all six runs, semantic error identity, poll-inclusive counts and no-input/cleanup claims. The existing paired screen evidence tests also pass. Python compilation and whitespace checks pass. Updated stale zor README coverage wording to match current bundled rules. No owning production code changed; applicable previous owner test/clippy/package results remain valid. No unchanged koh blocker or reconstructed build gate was rerun.
- Separate full-slice self-review covered public-CLI semantics, initial pending versus populated success, ephemeral process identity, nested cleanup and deadlines, fixture configuration/account isolation, startup versus polling accounting, structured versus plain errors, source/build provenance and limits. No further confirmed issue. Final full-change review remains R7; this was not independent review. R5 §8.6 is evidenced for the selected headless scenario; R4 and the remaining R5 live freshness/workflow/resource outcomes, R6 and R7 remain open.

- Companion export and byte reconstruction passed for zor and koh. A separate transcript check confirmed every successful zor overview/status/shutdown in each run used one service incarnation. Slice hashes: `/tmp/controller-setup-verification.json`. All build, capture, comparison and reconstruction handles reached terminal results. The persistent ignored reference build is `target/herdr-reference/build/debug/herdr`; its source copy and build log remain available for later comparisons.


### R5 progress: native resource sampling and paired idle/burst scaling

- The preceding sampler-check slice established `proc_pid_rusage` as a working alternative to the blocked `ps` method. Retained `docs/comparisons/resource-sampler-check.{md,json}` records three CPU-clock calibrations and owned-process RSS/footprint growth for a touched 32 MiB allocation. CPU counters require Mach timebase conversion, validated locally rather than assumed.
- Added `tools/comparisons/resources.py` and retained `docs/comparisons/resources.{md,json}`: 18 passing live cases, three repetitions each for fux+zor/herdr at 1/4/8 panes in separate workspaces. Uses synthetic named-claude workers, no credentials/model calls, owned foreground services, private HOME/XDG, bounded requests and explicit normal owner/worker cleanup. CPU/RSS/footprint include both fux and zor components; herdr uses one integrated owner. Raw samples, source/binary hashes, clean-reference build provenance, calibration and actual 80×23 versus 120×40 viewports are retained.
- Each worker emits the same 16,907 application bytes per burst. API-visible output timing includes sequential submissions/captures and polling, not agent-state freshness. Idle CPU favors fux+zor at 4/8 panes in these short samples; burst CPU and memory favor herdr at all sizes; output latency varies by size. The report discloses debug binaries, fixed order, viewport mismatch, sample count, component scope and non-unique RSS sums. Observer capture traffic remains unmeasured; no zero-traffic inference from herdr's integrated architecture.
- Probe harness issues (attempting the workspace protocol on another socket, herdr integer request ID and wrong capture-source variant) were corrected before the retained run. Cleanup now attempts all owners before reporting owner exit errors. All final 18 cases passed and the run handle reached exit0. Added four mandatory offline evidence/mutation tests for complete matrix/provenance, omitted zor accounting, CPU/memory arithmetic, PID reuse and cleanup; all four passed. Python compilation and `git diff --check` passed.
- Separate full-slice self-review covered measurement scope, raw-counter arithmetic, lifecycle cleanup, private environment, source provenance and comparative claims. No additional confirmed slice defect remained. This is not an independent review; final complete-change review remains R7. No production fux/zor/koh source changed, so prior applicable owner/export results are reused. R4, the remaining R5 scenarios, R6 and R7 remain open.


### R5 progress: observer capture-traffic accounting

- Added `tools/comparisons/capture_traffic.py` with a private transparent proxy per fux workspace, bypassed by harness listing/input. Reuses the identical synthetic worker from the paired resource workload. Retained `docs/comparisons/capture-traffic.{md,json}` has nine live runs (three repetitions at 1/4/8 panes), all normal owner/worker cleanup, raw completed request/event records, phase boundaries and source/binary hashes. Zero capture requests in every 4.2-second idle window; each workspace receives fresh marker-bearing captures after its burst. Median burst captures are 2/8/16, with request/reply/text byte counts recorded separately.
- The proxy initially rejected the protocol's correct subscription `accepted` response; fixed that harness assertion before the retained run. Final nine-case run exited0. The proxy adds instrumentation overhead, so marker timing is labeled separately from unproxied API latency and classified-state freshness. Manager traffic, handshakes and in-flight requests are outside the stated counters; idle lists remain visible. No account/model usage or production-source changes.
- Source inspection confirms both herdr detection paths call `terminal.detection_text()` in-process after the scan decision; unchanged screen scans can be skipped. This experiment cannot count those internal reads through equivalent workspace IPC. The retained value is null/unmeasured, not zero; no traffic win is inferred. Together with the existing paired CPU/memory/latency/scaling report, this establishes the available bounded §8.7 measurements and explicit interface limitation. R5 live classified-state freshness and two-worker workflow comparison remain.
- Four mandatory offline evidence tests pass: matrix/source integrity, byte/count mutations, excluding harness send-keys traffic, and marker capture for every workspace. Existing four resource evidence checks also pass. Python compilation and diff whitespace checks pass. Separate full-slice self-review covered counter boundaries, stream semantics, forwarding errors, cleanup, raw accounting and comparative limitations; no additional confirmed defect remained. This is not an independent review; R7 complete-change review remains open. Applicable unchanged owner/companion verification is reused, and the blocked koh runtime was not rerun.


### R5 progress: paired two-worker verification/artifact workflow

- Added `tools/comparisons/workflow.py` and retained `docs/comparisons/workflow.{md,json}`. It reruns the full existing zor two-worker fixture, then drives the unchanged herdr reference through equivalent isolated worktree creation, concurrent live workers, result-alpha/incorrect-beta outputs, beta repair, external collection, lead handoff, dirty-tree refusal and explicit forced cleanup. All work is in disposable repositories; git commits belong only to fixture worktrees. No account/model usage or production-source change.
- Both live branches passed. Zor's pinned fixture establishes policy-enforced checks/artifacts, rejection before verified predecessors, sealed handoff/receipt replay, interrupted stop-intent recovery, main preservation and retained verified artifacts after cleanup. Herdr passes the basic worktree/delivery/dirty-removal workload, with all three workers gone and server exit0. Its verification, copied artifacts and handoff decision are explicitly external harness operations. Full herdr requests/replies and external outputs are retained. The report distinguishes these evidence strengths and does not treat a displayed response as verified work.
- Inspected herdr's default API schemas for required checks/artifacts/predecessor handoff; none are exposed. The live task.verify probe returns invalid_request/unknown variant and the supported method list. The initial harness incorrectly expected method_not_found; corrected before the retained run. The error alone proves only method absence; source inspection supports the default-policy comparison. Server live_handoff is not task handoff; external scripts/plugins could implement additional policy.
- Four mandatory offline evidence tests pass for source/build integrity, failed-check/repair coverage, exact unsupported-method semantics, and cleanup. Python compilation and diff whitespace checks pass. Separate full-slice self-review checked workload alignment, external-vs-built-in evidence, temporary ownership, cleanup, source pinning and claim limits; no additional confirmed defect remained. This is not independent review; R7 remains. Final run handle exited0. R5 now retains §8.5 evidence; live detection-state freshness remains, alongside R4/R6/R7. Broader unchanged owner and companion checks are reused rather than repeated.


### R5 completion: bounded live detection freshness

- Added `tools/comparisons/detection_freshness.py` and retained `docs/comparisons/detection-freshness.{md,json}`. Six live runs use actual signed-out Codex 0.153.4, private HOME/XDG/CODEX_HOME and no prompts/credentials. Independent initial/final captures establish the sign-in blocker throughout a three-second observation window. Zor first reports Blocked at 186.55/189.94/199.65 ms after the initial successful capture; herdr discovers Codex but remains Unknown. An unmatched blocker is null, not zero latency. Both remove explicitly closed panes; all recorded Codex PIDs disappear and all owners exit0.
- Raw controller snapshots, timing, initial/final captures, source/binary hashes and clean-reference build provenance are retained. The report discloses zor CLI versus herdr API polling, startup/scheduling, default viewport/logo differences, and end-to-end sampling bounds. Pane closure is explicit loss, not natural agent exit. This is real startup-blocker freshness, not broad working/idle transition coverage or universal support; R4 preserves those requirements.
- Four mandatory offline evidence tests pass for matrix/provenance, raw state/timing consistency, censored misses, visible screen identity and PID cleanup. Python compilation and diff whitespace checks pass. Separate full-slice self-review covered account isolation, actual-vs-replayed agent identity, clock boundaries, cleanup, mismatch handling and comparative claims; no additional confirmed slice defect remained. This is not independent review; R7 complete-change review remains. All live handles are terminal.
- R5's measurement categories now have versioned bounded outcomes: prompt boundaries, input retry, service failure, screen conformance/coverage, live blocker/loss freshness, verified two-worker workflow, controller setup, and idle/burst resource/traffic/scaling. Herdr internal detection-read counters remain explicitly unmeasured by the IPC interface. Marked R5 complete for these stated scenarios without claiming an overall performance/detection win. R4, R6 and R7 remain open; unchanged owner/companion evidence is reused.


### R4 progress: recorded Claude input readiness

- Added the zor-only claude-2.1.263-input-ready rule from the already retained real authenticated manual-mode captures. It recognizes the version/model header and complete composer/shortcut footer before the prompt and after its response, excludes recorded active-spinner lines and interrupt hints, and reports visible Idle only. No response text, “done” label or process exit is treated as verification. Other modes/layouts remain unverified.
- Extended the existing production-terminal bundled-rule test to assert both input-ready captures, flag identity and rule ID, plus synthetic prefix/suffix/quote/control and active-spinner-with-changed-footer negatives. All eight bundled-rule tests passed. Rust1.91 fmt check, binary build and strict all-target clippy passed. Four unchanged authenticated-capture/redaction tests passed.
- Reran both real production file matchers on all 77 unchanged screen cases. Retained report now has zor10/11 expected real states, zero of six missed blockers and zero non-Unknown results on66 derived negatives; herdr remains8/11, three missed blockers and66 non-Unknown negatives (45 fallback,21 matched). Claude post-response input readiness is now parity with herdr's matched idle evidence. Refreshed manifest inventory and stale coverage prose. No new agent execution or generalization claim.
- The rebuilt persistent herdr reference has a different binary hash than the removed original temporary build. Updated the screen evidence validator to anchor to its retained controller-setup build provenance instead of the old prompt-boundary binary. Four screen evidence tests pass. Source reference itself remains unchanged.
- Separate full-slice self-review covered rule scope, active-vs-idle distinction, verified-task separation, negatives, refreshed provenance and docs. No additional confirmed defect remained; this is not an independent R7 review. Companion export and byte reconstruction passed. Fux/koh production source and unchanged runtime blocker were untouched. R4 still needs Codex idle and remaining real tool-state evidence; R6/R7 stay open.


### R4 progress: Codex input readiness and requirement-scope correction

- Added zor-only codex-0.153.4-input-ready from the retained authenticated response composer. Exact version header/model footer and empty composer are required; interrupt hints, Working rows and Booting MCP server rows exclude Idle. Loading stays Unknown, trust and command approval stay Blocked, and readiness is not task verification or attributed response evidence. Extended the production-terminal test for Idle flags/rule identity and changed-hint active-state negatives; all eight bundled-rule tests pass.
- Strict clippy initially found collapsible_else_if in the changed test; corrected it without a lint allowance. Final Rust1.91 fmt check, strict all-target clippy and binary build passed. Eight authenticated/approval evidence tests passed. Reran the 77 paired production CLI cases: zor11/11 selected real states, no missed blockers, all66 negatives Unknown; herdr8/11 with unchanged fallback distinctions. Refreshed screen evidence, inventory and coverage prose. Four screen evidence tests passed.
- Since the freshness report pins codex.toml, reran all six real signed-out startup/loss cases on current source. Zor first Blocked was227.67/219.76/179.26ms, herdr stayed Unknown; all pane-loss/PID/owner cleanup passed. Updated raw report and timing table, and all four freshness evidence tests pass. No account credentials/model prompts were used for this rerun.
- Re-reading original §4 found that real nested-tool execution per agent had become an invented checklist requirement. The original requires negative fixtures including nested tools; those already exist as explicitly synthetic derived negatives. Corrected that scope in the completion checklist without weakening the actual real-state requirements. R4 remains open pending an explicit coverage-matrix audit, rather than speculative additional nested execution captures.
- Separate full-slice self-review covered rule exclusion/priority, mode limitations, evidence freshness, provenance, negatives and task-state separation. No additional confirmed defect remained; this is not independent R7 review. Companion export and byte reconstruction passed; no fux/koh production changes. R6/R7 remain open and the unchanged netmon blocker was not rerun.


### R4 completion: original-scope state evidence audit

- Audited original §4 against all initial-agent fixture categories and wrote `docs/agent-state-coverage.{md,json}` with a category-by-agent matrix and source hashes. Inspected actual native busy events, authenticated exit statuses, resize captures and final records rather than relying on filenames. All agents have recorded startup/input-required/idle/unknown/resize/exit evidence; Codex/Claude have passive working screens, OpenCode has real native busy/activity events under its explicitly synthetic provider.
- Preserved capability limits: OpenCode has no claimed passive Working rule and its forced exit status remains unknown; natural success is not inferred. Claude tool-specific approval remains unverified, while its real theme choice satisfies the original approval/input-required category. Original §4 does not require natural exit for every agent or real nested-tool execution. The six specified negative classes are present in66 derived cases; exact viewport spoofing remains possible and cannot verify tasks.
- Audit checks passed: lifecycle4, OpenCode native events8, integration88 and startup2. Current bundled-rule8, Codex authenticated/approval8, Claude authenticated4, paired screen4 and live freshness4 results are reused where source/evidence is unchanged. No new runtime or model capture was claimed; this turn made the evidence matrix and completion determination concrete.
- Marked R4 complete within this recorded original scope, with limitations stated prominently. Remaining work is R6 and R7, including the required final gates and complete-change review. No production or companion source changed; no unchanged environmental blocker was rerun. This audit is a separate self-review, not the final independent R7 review.


### R6/R7 progress: separate service grants, reconstructed prerequisites and independent review

- Added koh gateway test `attachment_peer_cannot_use_separately_authorized_real_zor_control`: actual local fux/zor, distinct gateway endpoint identities and allowlists, permitted attachment, denied attachment-peer control ping, permitted controller ping, and gateway shutdown preserving owners. Private environment and owned cleanup guards; required FUX_BIN/ZOR_BIN in combined/CI prevent skip. Cargo1.95 gateway check and strict targeted clippy passed. The new runtime test failed at netmon monitor EPERM before authorization assertions; log `/tmp/koh-separate-control-runtime.log`. R6 remains incomplete, including real beyond30-second-retention reconnect.
- Added zor/REMOTE.md and SERVICE-API link with explicit two-service wiring and authorization/retention boundaries. Network endpoint separation is not a sandbox against an interactive shell's existing same-UID authority. No control state was moved into fux or koh.
- Fixed a real reconstructed-gate prerequisite gap: source-hash validators require the read-only herdr checkout. verify_combined_build now clones the pinned reference commit into its reconstruction, and CI checks out that same commit and runs all six retained comparison validators. The milestone full gate passed comparison evidence and all17 real-zor cases, then failed the known two koh gateway-lib netmon tests; later gateway integration was not reached. Log `/tmp/fux-final-reconstructed.log`. This snapshot predates the new R6 test/environment requirement and is not the final-source gate.
- Resumed existing independent reviewers for full root and companion change sets. Root reviewer independently passed lib84/ECS30/boundary3/structure8 and all25 comparison tests. Confirmed pending issues include observer.py cleanup sequencing, store.rs root-entry capacity versus retained resources, and incorrect attention rows for closed dashboard resume history (the reviewer retracted an initially suspected overflow after checking combined session limits). These remain open for parent validation/fix and subsequent review. Final review is not complete.
- Companion export/reconstruction was refreshed after the R6 test/docs changes. No commits, pushes, PRs or external messages were made. Goal remains active with meaningful review/fix work available despite the remote runtime blocker.

### Final whole-change review fixes (2026-09-07)

- Independent root and companion reviewers completed the full intended tracked/untracked change review. All five confirmed findings were fixed and independently accepted: OpenCode stale same-message fetch admission; invalidated passive observations revived by integration heartbeat; supported journal root directory capacity; historical resume launch attention rows; and observer fixture cleanup skipping remaining owners. The proposed dashboard row-overflow finding was retracted after checking the combined session capacity.
- Zor now versions message evidence across asynchronous native reads. Four tool/subtask/error/incomplete races pass in the expanded Node suite. The dashboard refuses state/focus for problem-bearing observations even with fresh idle/working heartbeats. Store recovery preserves 128 worktree and 128 check directories plus waiter/base/temp files. All85 library tests pass, plus bundled8/CLI2/OSC6/passthrough9 and strict clippy. Logs: `/tmp/zor-review-final-owner.log`, `/tmp/zor-review-fixed-clippy.log`.
- The real OpenCode resume trace was rerun with the changed adapter; its retained fixture includes historical launch IDs and the actual dashboard without false launch attention rows. All10 provenance/mutation validators pass. Real observer cleanup and two controlled shutdown-failure regressions pass. Reviewers independently reran library, Node, resume and cleanup checks.
- Added the R6 actual-retention scenario: five short reconnects, production retention+1 second detached, old authenticated token rejected without another application connection or replay, then fresh attachment preserving PID and incrementing the existing shell counter. Review confirmed the test semantics and required a directory guard for early netmon failures; that guard is fixed and accepted. Koh strict clippy passes. Actual execution still fails creating netmon with EPERM before retention assertions (`/tmp/koh-actual-retention-runtime.log`). The separate real zor-control authorization test has the same recorded blocker. R6 runtime acceptance is not established.
- Companion export and byte reconstruction pass. Final reconstructed build/gate is running in `/tmp/fux-review-final-reconstructed.log`; no claim of final completion or remote acceptance is made before its result. Credentials cannot resolve the OS monitor restriction. No commits, pushes or PR changes were made.

- Final-gate follow-up: source-provenance validation correctly rejected the old setup report after dashboard edits. All six real paired setup cases were rerun; current report/prose agree on zor3/3/3 and herdr6/6/6 total invocations including polling (intrinsic setup2vs3 unchanged). Five validators and independent review pass. The current real OpenCode question/reload integration was also rerun, its retained producer-reload fixture refreshed, and all88 validators independently pass. Current fux/zor executable hashes match the captured report. Final gateway integration execution on settled source reports0passed/3failed, each at netmon creation EPERM before application assertions (`/tmp/koh-final-gateway-integration.log`). Final reconstruction retry is `/tmp/fux-review-final-reconstructed-v2.log`.

- Final reconstruction completed all local checks: Node suite,123 retained agent evidence tests,25 comparison validators, boundary3/ECS30/CLI11/structure8 and all17 real zor scenarios plus controlled suites (227.91s). Gateway library8passed/3failed, all netmon EPERM; separately gateway integration0passed/3failed at the same pre-assertion boundary. Final logs: `/tmp/fux-review-final-reconstructed-v2.log`, `/tmp/koh-final-gateway-integration.log`. R1–R5 complete within recorded scope; R6 acceptance and R7 full gate remain incomplete solely on required remote runtime verification. No outstanding confirmed source-review findings. See the completion checklist's final handoff for exact commands, limits and remaining work.

### Completion under explicit R6 deferral — 2026-09-07

The user explicitly deferred R6 and excluded remote authorization, reconnect, retention and
netmon work from completion. R1–R5 and R7 are complete. The five confirmed review findings
were fixed and independently accepted; final-source non-R6 reconstruction/owner checks and
refreshed evidence pass. Prior handles are terminal/missing, so no interrupted check needs
resuming. Reused unchanged passing results rather than restarting checks or benchmarks.
Only the checklist/handoff changed in this closure pass; companion reconstruction and final
formatting/whitespace checks are reconfirmed. No unresolved non-R6 blocker remains. R6 runtime
assertions remain unverified due to netmon EPERM, explicitly outside this completion scope.
