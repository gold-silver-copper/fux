# Bring fux + zor + koh to verified Herdr parity or better

Current-state revision: 2026-09-15. This is an implementation assignment for a subsequent execution session, not a claim that this documentation update implemented or verified parity.

Implement this work; do not stop at a design, scaffold, revised audit or partial milestone. The required outcome is that the composed fux + zor + koh product matches Herdr's supported capabilities and preserves or improves the stack's existing strengths. Prioritize:

1. Integrated multi-machine navigation and agent supervision.
2. Agent breadth and restart/session restoration.
3. Pane/layout controls, direct terminal takeover and transcript retrieval.

These are the first workstreams, not the entire definition of parity. Close the remaining material gaps in the capability audit before declaring the overall objective complete. Backward compatibility and breaking semver are not concerns. Preserve user data, live sessions, repository changes and the separation of responsibilities between components.

## Starting evidence and working rules

Read the [capability audit, updated 2026-09-15](../capability-audit-2026-09-12.md), current repository instructions, workspace manifests, `tools/xtask/companions.json`, required CI workflows, and actual code before changing anything. The current fux/zor workspace baseline is `d8b64bd2508501f9a8fd181420e4b3ccbedd3725` (fux 0.11.0, zor 0.6.0, local-ipc 0.3.0); the published koh pin is `da712875e4f527b718abe44e9d68f94048e916c7`; the inspected Herdr reference is `d184b41fa36923c132629af725ff98bb02aa1b61`. Reconcile these with the execution-time checkout and record dirty state separately. Old planning prompts are not implementation truth.

Use `references/herdr` as read-only behavioral reference. Active zor is `crates/zor`; `references/zor` is archived. On 2026-09-15, the authoritative [GitHub latest-release API](https://api.github.com/repos/herdrdev/herdr/releases/latest) reported [Herdr v0.9.0](https://github.com/herdrdev/herdr/releases/tag/v0.9.0), published 2026-09-07T19:21:31Z, neither draft nor prerelease. That is fresh release metadata, not a runtime comparison. Recheck the authoritative release at execution time and establish a fixed comparison baseline: inspected master capabilities plus stable-release capabilities, with exact versions/revisions and supported, experimental, conditional and platform-specific rows. Do not silently move or narrow the baseline.

Work in isolated branches/worktrees as needed. Do not reset user changes or terminate the user's running sessions for tests. Use isolated runtime directories, credentials and ports. Keep reference-only repositories unchanged; do not fetch/update an existing reference checkout as part of this assignment. A missing behavioral reference can be inspected through its pinned upstream source or a separate disposable checkout without overwriting user material. `references/koh` is the clean integration checkout for the published companion pin, not a local patch surface: develop a needed koh change in its upstream repository, obtain authorized publication there first, then update `tools/xtask/companions.json` and run the clean-pin composition gate. Until publication is authorized and available, report integration as blocked rather than substitute a dirty reference or unpublished pin. Never add local absolute-path dependencies or make a clean standalone build depend on ignored references. Do not commit, push, create PRs or releases unless the execution session authorizes those actions. If PR work is authorized, apply the user's complete PR completion gate; no draft PRs or unsolicited issue/PR comments.

Maintain a reviewable parity ledger under `docs/` with one row per capability: baseline evidence, owner, intended behavior, implementation location, automated scenario, real runtime evidence, platform/provider coverage and remaining gap. Do not require a commit without authorization. Record implementation status separately from evidence status: source-inspected, fixture-tested, real-process-tested, real-provider/platform/network-validated and comparative-pass are not interchangeable. Date evidence and attach its revision; preserve unavailable, unsupported, failed and unrun results. A documentation claim, existing test source or command stub is never a passing acceptance run.

### Shipped starting points: extend and protect, do not rebuild

- **Multi-machine composition:** `crates/zor/src/machines/{catalog,connection,supervision,intents}.rs`, `service/client.rs`, `dashboard/{multi,handoff,attention}.rs` and `main.rs` implement saved koh bindings, independent observations, machine scoping, aggregated attention/notifications, guarded task actions, exact-pane viewer handoff and durable remote resume intents/status. This is more than separate forwarded sockets, but not the full Herdr hierarchy/setup experience or live multi-host/provider acceptance.
- **Layout:** `crates/fux/src/layout.rs`, `layout/edit.rs`, `layout_cli.rs`, ECS layout/manager systems and `client/{drag,interaction}.rs` implement guarded existing-pane edits, zoom, move/swap, tab/workspace transfers, ordering, labels, archives, keyboard and mouse interactions. Preserve these paths and their process identity invariants; README key summaries are not an exhaustive inventory.
- **Native/recovery:** `crates/zor/src/tasks/{capabilities,integration,resume,recovery}.rs` and `tasks/codex/` implement OpenCode native correlation and guarded session resume, Codex app-server turn correlation, blockers, interrupt/history reconciliation and live-wrapper recreation. Claude remains passive in the built-in capability contract. Durable launch/stop/check reconciliation and remote resume intent recovery already exist; full session restoration, lost-wrapper Codex restart and sustainable archival do not follow from those mechanisms.
- **Evidence:** `docs/verification.md` indexes recorded layout/manual/performance and multi-machine milestones; reports removed from the tree are available at `483d8ca`, not current live links or fresh acceptance. The ownership document still calls remote supervision future despite the source above; layout documentation also contains old in-progress/deleted-ledger wording. Reconcile these conflicts rather than erasing implementation or promoting historical results. The index explicitly leaves live remote/paid-provider, relay/NAT, Android runtime and a complete final Linux gate unestablished.
- **Provider evidence is not absent:** `crates/zor/tests/fixtures/agents/README.md` records authenticated Codex CLI 0.153.4 and Claude Code 2.1.263 response/natural-exit viewport captures, plus real OpenCode TUI/integration/resume captures using a synthetic local provider. Preserve these narrower historical results. They do not establish managed Codex native-turn/materialized-resume acceptance, broad provider coverage, current runtime success or whole-product parity.

## Architecture constraints

- **fux:** standalone generic terminal/process ownership, retained grids, layout geometry and mutations, generic per-terminal control authority, capture/input/event primitives, terminal rendering and any generic persistence added by this work. It must still build, install and run alone, open no network listeners and need no koh keys. Keep provider names and agent workflow policy out of its core/protocol; fux never links, spawns or supervises zor/koh.
- **zor:** provider integrations, agent lifecycle authority, session identity/resume policy, supervision, machine catalog/routing policy, task/group recovery, transcript retrieval policy, verification and durable workflow history.
- **koh:** authenticated transport, endpoint grants, connection/reconnect behavior and transport diagnostics. It does not own agent semantics or workspace layout.
- **Composition:** integrated navigation, setup and orchestration belong in zor or a thin composition layer, not the standalone fux launcher/core. The existing fux viewer can remain the generic attachment surface. Users of the composed product should not need to coordinate three services or copy socket paths for normal workflows; users of standalone fux must not acquire companion dependencies.

Preserve coherent snapshots, explicit replay gaps, bounded resources and backpressure. Rework inadequate bounds with an operational storage lifecycle; do not replace them with unbounded growth. Retain the stack's prompt identity, receipt reconciliation, native correlation and source/check/artifact guarantees.

## 1. Integrated multi-machine navigation and supervision

Extend the existing saved profiles, machine-scoped CLI/service API, aggregate dashboard, notifications and exact-pane handoff into a single navigable Local/machine/workspace/tab/pane hierarchy. Inventory current commands first. Close remaining search/filter, hierarchy, navigation, remote creation/worktree actions and setup gaps rather than building another catalog, controller or dashboard. Choosing an agent must attach to its actual host/workspace/pane. Preserve existing inspect/cancel/stop/reconcile/resume/status controls and combined attention while making reconnect/authentication/errors understandable without implementation bookkeeping.

Retain koh peer connections and add the SSH-style access/setup workflows needed by users of SSH configuration, aliases and remote setup. The current catalog stores koh endpoint/key-file bindings; it is not an SSH alias importer or approved remote installer. An SSH bootstrap may establish the stack's services/transport where appropriate, but do not require a manual server migration or silently grant extra endpoint authority. Specify installation/update behavior, remote prerequisites and compatibility diagnostics. Preserve the user's authentication policy and require explicit approval before replacing a live server.

The published koh pin does not expose `gateway connect --status-file`; `crates/zor/src/machines/connection.rs` deliberately reports a generic transport failure rather than inferring unauthorized, expired or offline from a closed socket. Preserve that honesty. Rich transport diagnoses require the upstream-then-published-pin workflow, not assertions based on development-koh fixtures.

Give every route an unambiguous host/server-incarnation/workspace/pane identity. Distinguish disconnected, stale, unknown and freshly observed state. Cached metadata must never authorize input to a replacement target. Scope credentials and viewer/control grants per machine; revocation must take effect. One unreachable machine must not stall navigation, metadata updates or control of others. Handle reconnect within and beyond koh's retention window with explicit resynchronization and no duplicated input.

Acceptance: operate Local plus at least two actual remote hosts from one interface; create/attach/navigate workspaces, inspect all agents, respond to one blocker and return to the originating task. Disconnect/restart one host while another remains usable. Exercise endpoint revocation, conflicting machine/pane names, stale cached identities, gateway loss, retention expiry, NAT/relay and a real non-loopback network. Same-host gateway tests alone do not satisfy this gate.

## 2. Agent breadth and session restoration

Derive the provider inventory from pinned Herdr `docs/next/website/src/content/docs/{agents,session-state,agent-automation}.mdx`, detection/integration source and `src/agent_resume.rs`, then reconcile stable-release behavior. The inspected tables distinguish 21 principal agents, Gemini/Cline as less thoroughly tested detection, and 17 conditional resume paths; enumerate names and minimum integration/provider versions rather than treating counts as the specification. Match each provider's actual detection, lifecycle authority, needs-input, session identity, launch and supported resume behavior. Herdr's session-only integrations are not lifecycle/native-turn authorities. Do not demand imaginary native APIs or claim correlation from screen matching; label passive, integration-derived and native evidence distinctly.

Extend the existing Codex/OpenCode adapters and Claude integration only for remaining supported operations. Codex already implements literal multiline submission, correlated native responses and structured blocker evidence, interrupt, history reconciliation and recreation of a retained thread while its owned wrapper lives; blocker responses and lost-wrapper restart remain deltas. OpenCode already implements native prompt correlation and guarded `task resume`, but its capability contract advertises no native interrupt. Claude has no managed native adapter in that contract. Close provider-specific permission/question response, interrupt and resume gaps where supported, and validate shipped paths rather than replace them. Preserve operator-configured permissions and sandbox settings: the existing headless/no-approval launch policy is not permission to silently widen interactive launch authority or bypass blockers. Publish versioned capability output agreeing with runtime behavior, precise unsupported operations and fallback reasons.

Add full restart restoration on top of existing generic layout archives and live detach persistence. fux 0.11.0's documented persistence is detach-only; an archive applied to existing panes is not process resurrection. Persist generic session shape: workspaces/tabs, names/order, pane layout, focus, cwd, safe launch descriptors and optional bounded screen history. Separate viewer detach, controller restart, fux crash, agent-wrapper death, machine reboot and live server replacement. Persist atomically with a documented recovery schema; validate malformed/partial snapshots. Restored screen content must be marked historical, never fresh agent-state evidence. Herdr's screen-history replay is opt-in; native restore is conditional on valid current official integration session references, with invalid/duplicate/stale references falling back to shells. Do not promise restoration of arbitrary processes.

Extend zor's existing guarded OpenCode resume, resume-status, durable controller intents and launch/check/stop reconciliation to the remaining provider and session-shape cases. Resume through supported commands/APIs, reconcile durable task/group identities against the new fux incarnation, and provide useful recovery when cwd, worktree, provider binary or session storage is unavailable. Do not blindly replay pending prompts or relaunch arbitrary old commands with side effects. Allow explicit restore/skip/retry decisions through the integrated UI/API.

Resolve remaining grouped-attempt replacement and retained-group membership restrictions, uncertain-effect operator resolution, lost-wrapper recovery, history archival and cleanup. Preserve the existing safety guards until replacement evidence supports safe progress: marking abandoned checks uncertain does not prove their children stopped, and retaining an operation ID does not authorize repeating uncertain effects. Herdr's experimental live PTY handoff is a separate opt-in Unix/server-replacement capability, not normal restore and not zor's verified task-result handoff. Supply an equivalent separately tested applicable path before claiming that row; record updater/package-manager and compatibility conditions.

Acceptance: record real-provider launch, working/idle/blocked detection and supported resume scenarios for every provider claimed at parity. For native adapters also demonstrate correlation, multiline input, blocker response and interrupt where advertised. Kill/restart the viewer, supervisor, wrapper and terminal server independently; reboot a test host. Restore a mixed-provider layout and continue scheduled work without duplicate input or false completion. Provider access/authentication gaps remain unvalidated rows, not fixture-derived passes.

## 3. Finish and verify pane/layout controls

The existing implementation deliberately uses fux's own ECS/layout model without a hypertile runtime dependency. Retain that decision unless a demonstrated remaining gap justifies revisiting it. The requested design reference at `references/ratatui-hypertile` remains:

- Repository: https://github.com/nikolic-milos/ratatui-hypertile
- Reference commit: `92fa63300f802c4465a4fbd2b928cf3ef1b4f8d0` (crate 0.4.1).
- License: MIT; preserve its copyright/license notice for copied or substantially adapted code.
- Study `src/core/state/{mod,mutation,movement,focus}.rs`, `src/core/types.rs`, `src/core/serde_impl.rs`, `src/engine.rs`, `src/input.rs`, `src/core/state/tests.rs`, and `extras/src/runtime/{mouse,workspace,keymap,render}.rs`.

The root ignore rule excludes `references/`; this clone is local reference material, not a production dependency. If absent, inspect the recorded upstream revision or a separate disposable checkout without modifying a dirty reference. Record deliberate comparison-baseline changes.

Use its tree manipulation, directional movement, geometry/hit testing, drag interactions, serialization and tests to investigate concrete remaining deltas. It tiles widgets within an app; it is not a PTY multiplexer or proof of multi-client correctness. Do not redo the already implemented split-tree core, import the extras runtime wholesale or rewrite the renderer merely to resemble the reference. Any new dependency/algorithm change needs an explicit rationale and measured hot-path cost. Validate imported layouts independently; reference serialization does not establish safe untrusted-input handling.

Map Herdr-equivalent split/close/focus/resize, zoom/unzoom, pane swap/move, tab/workspace ordering, same-server tab/workspace transfers, layout export/apply, keyboard controls and mouse border/pane dragging to current code and existing scenarios. Implement only missing operator workflows or confirmed bugs; keep the whole list as regression obligations. Preserve stable pane/process identity; moving a pane must not recreate its PTY or invalidate task identity unnecessarily. Cross-host movement must not pretend that an OS process migrated.

Preserve and exercise the existing minimum/tiny-terminal policy, deterministic directional focus, ratio rounding, zoom scope and competing viewer resize/focus rules; change only demonstrated parity gaps. Mutations must remain atomic and observable with layout generations, rejecting stale/conflicting requests. Retain validation for duplicate/missing pane IDs, invalid ratios, excessive depth/size and references outside the target workspace. Distinguish applying geometry to existing panes from a restore template that can launch processes; plain layout import must not execute embedded commands.

Acceptance: exercise keyboard and mouse workflows on nested layouts, across tabs/workspaces and through remote viewers. Verify export/apply round trips, zoom restoration, focus after removal, tiny/zero-size resize handling, deeply nested input rejection and conflicting clients. Use property/invariant tests for unique pane identity, valid coverage/geometry and stable state after operation sequences. Demonstrate that pane PID/PTY and active zor task identity survive move/swap/zoom.

## 4. Direct terminal takeover and transcript retrieval

Reuse current exact-target viewer attachment, coherent capture, private history views, event subscriptions and frame/delta streams. These are not yet a server-enforced single-writer terminal lease or Herdr-style direct terminal attachment. Add the remaining direct per-terminal interactive and machine-readable stream contracts with explicit takeover/release, one writable controller, multiple read-only observers, disconnect behavior and resize/scroll authority. Enforce authority at the server. Revoked clients must lose authority immediately; observers cannot inject input, resize, scroll or acquire control through alternate requests. Normal workspace viewers and zor automation must participate in the same policy, not bypass it.

Human takeover must visibly pause conflicting automated input and transcript-scrolling operations. Returning control must reconcile the current foreground occupant, provider session and pending operation state. Root PID alone does not identify the foreground input consumer. Preserve terminal modes and restore the outer terminal on detach/error. Keep endpoint authorization separate from OS sandbox claims.

Extend existing coherent text/cell capture with reliable text/ANSI retrieval and explicit source, freshness, completeness and truncation. Match Herdr's conditional virtualized alternate-screen retrieval (`references/herdr/src/server/alt_screen_read.rs`): for recognized idle agents in a safe viewport state, collect overlapping pages through supported application scrolling and restore the original/bottom viewport as appropriate. Prefer native transcript access when it provides equivalent semantics, with accurate provenance. Do not claim private host scrollback, visible capture or bounded native response summaries are a complete transcript.

Bound retrieval by bytes/pages/time, detect duplicate page boundaries and concurrent output, handle wide/combining characters and wrapped lines, and stop on human input or agent state changes. Working/blocked/unknown agents and unsupported applications need explicit passive fallback or actionable refusal. Do not scroll an agent under direct human control. Interrupted retrieval must report partial data and restoration failure truthfully.

Acceptance: two controllers race for a terminal; takeover revokes the old one; observers remain read-only across all API paths; lost connections release/reconcile authority. Retrieve a real long Claude/OpenCode alternate-screen transcript beyond the visible screen, verify page boundaries against known content, and confirm viewport restoration. Inject user typing, resize, agent activity and disconnect mid-read without misdirecting input or reporting an incomplete result as complete.

## 5. Close the other gaps required by the overall claim

The priority workstreams cannot by themselves establish complete Herdr parity. Implement and validate remaining material ledger gaps, including:

- Representative-repository source-bound verification. The updated audit measures 1,112 committed blobs and 9,050,985 bytes at `d8b64bd`, including 14 files over 64 KiB and 64,241 path bytes; remeasure the execution-time committed tree. Current `crates/zor/src/tasks/source.rs` still limits a source to 256 regular files, 64 KiB/file, 256 KiB total and 32 KiB paths, and materializes files without Git metadata. Address whole-tree input support, Git-dependent checks, realistic command time/output limits, artifact storage, check cancellation, uncertain execution resolution and safe scratch/history cleanup. Current checks allow up to 300,000 ms and retain at most 4 KiB per stdout/stderr stream. Preserve coherent source/check/artifact selection and stale-evidence rejection.
- Sustainable operation beyond bounded task/check/source/group/journal and remote-intent retention. Inventory current limits in `crates/zor/src/tasks/{model,store,group}.rs` and `crates/zor/src/machines/intents.rs`; do not equate record retention with lifecycle archival. Demonstrate more than 1,000 task/check cycles over a multi-day run with archival and injected failure, without manual journal replacement, lost required evidence or unsafe worktree removal. Reuse existing durable records and recovery rather than rebuild them.
- Terminal graphics and image workflows, input/clipboard/IME behavior, configuration, plugin installation/discovery and equivalent extension actions/events/panes needed by the baseline. Preserve current bounded history/copy, mouse, paste and configuration behavior while closing actual deltas. Herdr's compatible-terminal graphics are enabled by default in v0.9.0; not every terminal supports them. Preserve licensing when studying/adapting reference code and independently implement alternatives where required.
- Native Windows runtime, installation/update and applicable remote-host/client behavior, alongside Linux/macOS. Reconcile Herdr's conflicting Windows documentation against source/runtime; do not excuse stack gaps with stale claims of unsupported Herdr Windows SSH targets. Keep true baseline platform exceptions explicit. Maintain existing Android checks and separately label runtime coverage.
- Packaging and a documented clean-install/update path for the composed product, while preserving independently installable standalone fux. Existing standalone release packaging is a starting point, not proof of integrated provisioning. A developer checkout with manually launched gateways is not product parity.

Do not silently move these to “future work” while declaring the overall request complete. If execution is blocked by unavailable platforms, provider access, infrastructure or an external decision, complete independent work and report the exact unfulfilled gates. Do not invent credentials, spend unapproved resources or count skipped scenarios as passes.

## Verification, comparison and delivery

Implement in coherent increments, keeping the product runnable. Run the execution-time required formatting/linting, targeted correctness/failure-path scenarios, broader suites, packaging and clean-pin integration gates; do not infer hosted CI success from workflow files or local runs. Reproduce the retained handshake/replay, gateway-fault, layout, multi-machine routing and provider-fixture scenarios so shipped behavior does not regress. Use real PTYs/processes and actual provider/platform/network scenarios where required; fixtures supplement those runs.

Build a reusable comparative acceptance suite against the fixed Herdr baseline. Run equivalent operator workflows with the same providers and terminal environment. Preserve raw logs, versions, commands, screen evidence where useful and failure classifications. Separate unsupported, unavailable, failed and passed results. Review screenshots/recordings of the integrated UX as well as machine-readable output.

Measure startup, steady-state CPU, total participating-process memory, rendered input-to-visible p50/p95/p99 latency, sustained output, multiple panes/viewers and reconnect. Include zor and both koh endpoints for remote comparisons. Use equal build profiles, features, hardware and network conditions. Establish measurement tolerances before comparing results. Do not substitute internal historical benchmarks or command-acceptance latency for current head-to-head evidence. Investigate material regressions rather than averaging them away. Claim “better” only for named dimensions supported by results.

Have a fresh independent reviewer inspect the complete intended diffs across all changed repositories for correctness, identity/authorization boundaries, persistence/recovery, resource bounds, terminal behavior and missing tests. If unavailable, perform an explicit separate full-diff review. Validate findings, fix confirmed in-scope issues, rerun affected checks and repeat review for new serious findings. Follow the PR completion gate if a PR is in scope.

Deliver working code, user documentation, recovery/operator instructions, changelog updates, reference/license attribution, reproducible test/benchmark tooling, the completed parity ledger and an updated capability audit. Give exact changed repository revisions, verification results, CI state if applicable and remaining risks. Report three distinct outcomes: priority-workstream completion, whole-product parity, and measured advantages. Declare whole-product parity only when every applicable baseline row passes and the stack's existing guarantees remain intact. Otherwise state that the overall objective is incomplete and list the evidence or implementation still required.
